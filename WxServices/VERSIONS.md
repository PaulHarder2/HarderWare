# Version History

This project follows [Semantic Versioning](https://semver.org/): **MAJOR.MINOR.PATCH**.
Patch releases are bug fixes, minor releases introduce new features, and major releases are reserved for large changes.

| Version | Commit  | Date       | Summary |
|---------|---------|------------|---------|
| 1.53.0  | d4f4999 | 2026-07-15 | WX-65 (containerize WxVis.Svc — Step 3 of Epic **WX-7**; the third headless service to run as a Linux container, and the most involved because the .NET worker shells out to the WxVis **Python** render scripts — matplotlib + cartopy — so the image carries a Python stack beside the .NET runtime). New `services/wxvis/Dockerfile`: multi-stage `sdk:8.0` → `runtime:8.0-bookworm-slim`, non-root UID 1000, region-agnostic; the runtime stage apt-installs `python3` + matplotlib fonts (`fonts-dejavu`, `fonts-liberation`) + `msodbcsql17` (matching the shared config's `WxVis.DbDriver`, so the Python DB path is identical to the host's but for the auth mode) via Microsoft's apt repo (`ACCEPT_EULA`), then `pip`-installs the pinned `requirements.txt` — cartopy/shapely ship manylinux wheels that bundle GEOS/PROJ, so no `-dev` libraries or compiler are needed. The WxVis Python scripts are copied to `/opt/wxservices/WxVis` (= `WxPaths.WxVisDir`). A writable `HOME=/var/cache/wxvis` (+ `MPLCONFIGDIR`) is set because the root-owned WORKDIR is read-only to UID 1000, and matplotlib/cartopy must write their font caches; cartopy's 50m Natural Earth data is **pre-baked into the image** at build time so the first render needs no runtime download (and can't fail when the host is offline after a recreate). `requirements.txt` is **pinned to the host `wxvis` conda env** (the render reference) for render equivalence + reproducibility, and gains `scipy` (imported directly by the scripts, previously only a transitive dep). New `wxvis` service in `services/docker-compose.yml`: `plots` mounted **read-write** — WxVis is the *producer* of the synoptic/forecast/meteogram PNGs (WxReport, WX-64, mounts the same host dir read-only to inline them) — plus `Logs` read-write and the read-only `appsettings.local.json`. **The substance — WxVis reaches SQL Server twice:** the .NET `WeatherDataContext` *and* the Python scripts' own SQLAlchemy/pyodbc engine (`db.py`). The host uses Windows Authentication for both, but a Linux container has no Windows identity, so `db.py` + `WxVisConfig.BuildPythonEnv` gained SQL-login support: when the container connection string carries a `User Id`/`Password` they flow to Python as `WXVIS_DB_USER`/`WXVIS_DB_PASSWORD` and `db.py` authenticates with `UID`/`PWD` (ODBC Driver 17 defaults to no encryption, so no `TrustServerCertificate` is needed); a Windows host has no such keys and keeps `Trusted_Connection` — the connection string is the single source of truth for the auth mode. Container config overrides: `WxVis:CondaPythonExe` → `/usr/bin/python3` (else `MapRenderer` looks for the host's Windows conda path and every render fails), and `Program.cs` adds `AddInstallRoot` (the WX-64 parity fix) so the three map workers resolve the script + plots dirs to `/opt/wxservices` instead of the shared-config `C:\HarderWare`. `MapRenderer` guards its conda-`PATH` augmentation to `OperatingSystem.IsWindows()` (the `;`-joined Windows conda dirs are meaningless on Linux). `Deploy-WxService.ps1`: `WxVisSvc` pulled from the Windows-service map and routed to `Invoke-ContainerDeploy`; the vestigial `WxVis` Python-scripts target **retired** (once containerized, nothing on the host reads `InstallRoot\WxVis`); the `all` deploy runs the WxVis container after WxParser and before WxReport; and a new **consolidated end-of-run status summary** prints `[OK]/[FAIL]` per component (the per-step lines otherwise scroll off under each container's docker-build output). `Invoke-ContainerDeploy` also **stops + disables a same-named native Windows service** once the container verifies, so deploying a container automatically retires its native counterpart (the reversible cutover, applied to all containerized services). DESIGN.md: new *WxVis.Svc container (WX-65)* subsection + deployment-mode labels swept. §13: `docker compose up wxvis` → `Application started`, container-rendered PNGs land in the host `plots` (the Linux `/opt/wxservices/plots` path in the render log proves container authorship), the Python SQL-auth DB path loads GFS grid + METAR rows, and Grafana receives `wxvis_*` metrics. MINOR: new containerized-deployment capability for WxVis; the Python SQL-auth is additive (Windows host unchanged, keeps Trusted_Connection), no schema change. |
| 1.52.0  | bb20b71 | 2026-07-15 | WX-64 (containerize WxReport.Svc — Step 2 of Epic **WX-7**; the second headless service to run as a Linux container, reusing the WX-63 multi-stage Dockerfile + compose + InstallRoot-env-seam pattern). New `services/wxreport/Dockerfile` (multi-stage `sdk:8.0` → `runtime:8.0-bookworm-slim`, non-root UID 1000, region-agnostic — no connection string/secrets/install-root baked in; **no `chown`**, because unlike WxMonitor's `wxmonitor-state.json` WxReport persists nothing to the image layer — it only reads baked-in content, every runtime write goes to a bind mount) + a `wxreport` service in `services/docker-compose.yml` with **three** bind mounts: `Logs` read-write (log + heartbeat, read by WxManager/WxMonitor), `plots` **read-only** (the meteogram PNGs WxVis renders and WxReport inlines into report emails — WxReport only reads them), and `translation-qa` read-write (the operator "Rerun QA" HTML/JSON packages, WX-235). WxReport's secrets — SMTP creds, the Claude API key, the Gemini key — are read from the `GlobalSettings` DATABASE row, not a file, so `appsettings.local.json` stays as lean as WxMonitor's (container DB connection string via `host.docker.internal,1433` + the `:4318` OTel endpoint only). **The substance — one source of truth for the install root:** the workers resolve runtime paths via `IConfiguration["InstallRoot"]` (from `appsettings.shared.json` = `C:\HarderWare`), NOT the env-aware `WxPaths.ReadInstallRoot()` that WX-63's container seam overrides. On Windows the two coincide, so the split was invisible; in a container the workers would resolve plots/heartbeat under a non-existent `C:\HarderWare` — reports would send **silently without meteograms**, and the heartbeat would be written outside the mounted `Logs`, so WxMonitor would read WxReport as **permanently down**. New `WxConfigurationExtensions.AddInstallRoot` (WxServices.Common) projects the resolved root into configuration as the last source, so `IConfiguration["InstallRoot"]` == `ReadInstallRoot()` everywhere (env → shared-config, the only sound authority since it also decides where `appsettings.local.json` loads from); WxReport's `Program.cs` calls it, and WX-65/66 reuse it. No-op on Windows (identical value). **Added scope — folds in a fix for a live WX-63 regression:** WxMonitor's `MonitorWorker` resolves the *watched* services' heartbeat + log paths via `IConfiguration["InstallRoot"]` too, so the containerized monitor (WX-63, no `AddInstallRoot`) had every file watcher looking under a non-existent `C:\HarderWare` — it logged `heartbeat file not found` **every cycle regardless of service health** (only the DB-based METAR-staleness watcher still worked; this is the persistent WARN seen during the 2026-07-15 WxParser/WxReport/WxVis outage, present even after the services were restored). The same one-line `.AddInstallRoot(installRoot)` is added to `WxMonitor.Svc/Program.cs`, repairing the monitor's file watchers in-container. `Deploy-WxService.ps1`: the WX-63 `Invoke-MonitorContainerDeploy` is generalized to `Invoke-ContainerDeploy -ComposeService -DeployApp` (DRY — WxMonitor + WxReport both call it, WX-65/66 next); `WxReportSvc` is pulled from the Windows-service map and routed to the container path; the `all` deploy runs the WxReport container after the Windows services that feed its DB/plots and before WxMonitor; `WX_HOST_{LOGS,PLOTS,QA}_DIR` are set for the mounts. §13: `docs/test-procedures/WX-64.md` — `docker compose up wxreport` then observe: `Application started` banner, DB connectivity, a completed report cycle, a fresh heartbeat in the host `Logs`, and a meteogram inlined into a report (the end-to-end proof the InstallRoot seam resolves in-container). MINOR: new containerized-deployment capability for WxReport; the InstallRoot-config unification is behavior-preserving on Windows (same value), no schema change. |
| 1.51.0  | 5675075 | 2026-07-14 | WX-63 (containerize WxMonitor.Svc — Step 1 of Epic **WX-7**; the first of the four headless services to run as a Linux container instead of a Windows service, establishing the multi-stage Dockerfile + compose + InstallRoot-env-seam pattern WX-64/65/66 reuse). New `services/wxmonitor/Dockerfile` (multi-stage: `sdk:8.0` publishes `linux-x64`, `runtime:8.0-bookworm-slim` runs it as non-root UID 1000; the image is region-agnostic — no connection string or secrets baked in) + `services/docker-compose.yml` (DB + OTel collector reached via `host.docker.internal` with `extra_hosts: host-gateway`; `services/wxmonitor/appsettings.local.json` bind-mounted read-only for secrets + the container connection string + the `host.docker.internal:4318` telemetry endpoint; `C:\HarderWare\Logs` bind-mounted read-write so WxManager still reads the container's `wxmonitor-svc.log` + heartbeat) + a repo-root `.dockerignore` (lean context; stops a Windows-built `bin/obj` from poisoning the Linux build). The one source seam: `WxPaths.ReadInstallRoot()` honors a `WXSERVICES_INSTALL_ROOT` env var (→ `/opt/wxservices` in the container) before `appsettings.shared.json`'s `C:\HarderWare` — same binary, both deploys, no config fork; Windows services leave it unset (no behavior change). The container runs on Compose's default (project-scoped) network — a shared/named network is deferred to the OpRegion work (the four services coordinate via the database, not container-to-container). `Deploy-WxService.ps1` gains `Invoke-MonitorContainerDeploy` (`docker compose up -d --build` → verify the `Application started` banner → identical `WxMonitorSvc` deploy-history line, same version + git commit) and pulls `WxMonitorSvc` out of the Windows-service map; the Windows service is disabled at cutover as a reversible fallback (then `sc delete`d once proven). **Folded in:** the four services' startup banner is centralized in a new argument-free `WxPaths.StartupBanner()` (name self-derives from the entry assembly, no per-service literal) and **drops the git commit** — unavailable in a container build, and per-deploy provenance already lives in `deploy-history.log`; `WxPaths.GitCommit` is kept for the WPF About screens. Tests: 4 new `StartupBanner` tests (format + the commit stays out) + Common suite green (31). §13: `docs/test-procedures/WX-63.md` — `docker compose up` then observe: `Application started` banner, `wxmonitor-svc.log` accumulating, fresh heartbeat, Grafana receiving container metrics. MINOR: new containerized-deployment capability for WxMonitor; the env seam + banner change are behavior-preserving for the Windows services (identical filenames; banner drops only the commit). |
| 1.50.2  | 31e2b98 | 2026-07-13 | WX-290 (unify service naming on one canonical per-service token; **clean fix for WX-106**, the heartbeat blind spot; Task under **WX-254** Maintainability). A single background service had two divergent name tokens feeding `WxPaths`: WxParser *wrote* its heartbeat via token `"wxparser"` (`wxparser-heartbeat.txt`), but WxMonitor *sought* it via `svc.Name.Replace(".","-")` = `"wxparser-svc"` (`wxparser-svc-heartbeat.txt`), so the monitor never found the file and logged `heartbeat file not found` **every cycle** for WxParser and WxReport (27k+ occurrences on the live box) — blind to their liveness (**WX-106**). New single-source **`WxServiceToken`** (WxServices.Common) holds the one canonical token per service (`wxparser`/`wxreport`/`wxvis`/`wxmonitor`) and a `FromConfigName` that maps a monitor `WatchedServices[].Name` back to that constant; both the writers (`FetchWorker`, `ReportWorker`) and the monitor now derive log + heartbeat paths from it (writer literals + the monitor's `Name.Replace` both replaced), plus a new `WxPaths.ServiceLogFile(token)` = `{token}-svc.log` so the four services' `Program.cs` log inits derive from the token too. **No file renames** — every derived name is byte-identical to the pre-WX-290 names (`{token}-svc.log`, `{token}-heartbeat.txt`), so no verify script, log target, or deploy-history component moves, and the Windows `ServiceName` is deliberately untouched (it's the deploy-history/COMPONENTS identity; renaming it was out of scope). The explicit per-entry `LogFile`/`HeartbeatFile` config override is preserved (token derivation is only the default) — the escape hatch for a non-standard or future non-service watched target, which otherwise routes through its own `IWatcher` (service identity and watch target are separate axes). 13 anti-drift `WxServiceToken` tests (writer token == monitor-resolved token; identical heartbeat/log paths; no-rename filename pins; unknown-name normalization); WxMonitor characterization suite green (the override path). §13: `docs/test-procedures/WX-290.md` (+ `-verify.sh`) — post-deploy monitor-log check: zero `heartbeat file not found` WARNs while cycles complete. PATCH: WxMonitorSvc behavior fix (WX-106), writers/log inits behavior-preserving (identical filenames); no schema change. |
| 1.50.1  | 6ffd883 | 2026-07-13 | WX-293 (reconciler Closing prose stops using "storms" for a non-severe window; **WX-153** editorial-clarity epic). A recurring production content degrade: the model wrote a multi-window Closing — _"rain possible through the morning, then **storms** possible through the afternoon and evening"_ — using "storms" for a later day-part with no severe block. The WX-284 storm-vocabulary validator (`CheckSevereStormVocabulary`) correctly rejected it, the retry-with-feedback loop failed 3× (the model kept the wording), and WX-189 dropped the Closing section — so the fail-closed machinery worked, but recipients lost the Closing. Root cause is upstream: the snapshot legitimately carries a non-severe `precipPhenomenon=thunderstorm`, and the model rendered "storms" from it into the Closing as stylistic variety on "rain." **Fix = prompt-strengthening, not a deterministic rewrite** (Paul's ruling: a blanket "storms→rain" post-pass is phase-blind and unsound — in snow/wintry latitudes a "storm" can be a snowstorm/blizzard/thundersnow, and collapsing those to "rain" would be flatly wrong). Five edits: (1) `ReconcilerPrompts` gains an **absolute storm-word gate**, scoped to LIQUID convective words ("storm/storms/stormy/thunderstorm/squall") — they require `severeFlag`, so a non-severe window is plain "rain" even when `precipPhenomenon` is thunderstorm; the gate keys on `severeFlag`, not precip phase, and frozen precip is explicitly left to keep its own words (snow/wintry mix/freezing rain), so it never conflicts with the deterministic validator's deliberate frozen-compound allowance (a legitimate "winter storm" without a severe block stays legal); (2) a **closing-scoped multi-window reminder** — the gate applies to EVERY clause of a multi-day-part Closing, so "rain" is not varied to "storms" for contrast; (3) the `CheckSevereStormVocabulary` rejection message (embedded verbatim in the WX-189 retry feedback) is **sharpened** to name the multi-window trap, strengthening the retry lever; (4) the **CAPE guidance reconciled** (CodeRabbit-caught, a pre-existing WX-284 residual): the top-level system prompt (`BuildReconcilerSystemPrompt`) told Claude "low CAPE warrants at most a mention of an isolated storm" — recipient-facing storm licensing that contradicted the gate and misfired in winter (convective snow/thundersnow runs at low CAPE), so it now uses CAPE only to judge severity → set `severeFlag`, with recipient storm wording reserved for a severe CONVECTIVE window ("severe storms" only for a severe thunderstorm; a severe non-convective wind event is "severe weather" — `severeFlag` alone does not authorize storm wording, mirroring WX-284); (5) the deterministic `CheckSevereStormVocabulary` validator is brought into line with that rule (CodeRabbit-caught) — it gated storm wording on `severeFlag` alone, but `DeriveSevereFlag` also trips on a wind-only event (wind ≥ threshold), so a severe non-convective wind block could validate "severe storms"; it now requires a severe **convective** block (`SevereFlag && precipPhenomenon == Thunderstorm`, via a shared `IsSevereConvective` predicate). Tests: 3 prompt-text guard tests (storm-word gate, multi-window rule, CAPE-gates-on-severeFlag) + a `ForecastReconciler` multi-window repro + a severe-non-convective-wind repro; full WxReport suite green, format clean. The only new deterministic reject is "severe storms" over a severe wind-only window (previously wrongly accepted); every legitimate severe-convective case still validates (existing suite green — the "no new false-rejects" proof). §13: `docs/test-procedures/WX-293.md` (+ `-verify.sh`) — post-deploy trustworthy-absence monitor: zero exhausted "storm wording … no severe block" degrades over an active day past the deploy boundary. PATCH: recipient-visible correctness fix (fewer dropped Closings), backwards-compatible; prompt + message only, no schema change. |
| 1.50.0  | a6383aa | 2026-07-12 | WX-291 (WxManager & WxViewer remember their last window position and size across launches; standalone Story, `wxmanager`/`wxviewer`). Motivated by moving the dev PC to a new docking station (a 1920x1080 monitor + a 3840x2160 4K TV): neither WPF app cooperated with the extra terrain — **WxManager** always opened at a fixed 1050x720, **WxViewer** always force-opened maximized, and neither restored where it was last placed. Now both save their placement on close and restore it on launch, including the maximized state; on first run (no saved state) both default to **up to 1920x1080, capped to the primary monitor's work area** (so it's 1920x1080 on a larger monitor, and 1920 x (1080 − taskbar) on an exactly-1080p monitor — never sliding under the taskbar), windowed, centered. **Architecture:** the placement math is a WPF-free `WindowPlacement` record in `WxServices.Common` (`Left/Top/Width/Height/Maximized` + `Load`/`Save`/`ClampToWorkArea`/`CenteredDefault`), unit-tested in a new `WxServices.Common.Tests` project (filling the one-test-project-per-source gap); the thin WPF glue (`WindowPlacementExtensions.RestoreAndPersistPlacement`) is authored in WxViewer and **compiled into both apps** (the same link pattern WxManager already uses for `HarderWare.ico`), resolving the target monitor's work area via the Win32 `MonitorFromRect`/`GetMonitorInfo` pattern WxViewer/WxManager already use for their maximize clamp, DPI-corrected off the window's own device scale. **Two hard safety guarantees** (the reason a naive "restore the saved rect" is not enough after a monitor layout change): every placement we apply — restored *or* the default — passes through `ClampToWorkArea`, so (AC5) a window last placed on the now-disconnected 4K TV opens fully on the nearest connected monitor, never stranded in off-screen null-space, and (AC6) a window is never larger than the monitor it lands on — a saved 4K rectangle, or even the 1920x1080 default on a lone 1366x768 laptop panel, is shrunk to fit. Storage is one JSON file per app under `%LOCALAPPDATA%\HarderWare\`; a missing/corrupt/degenerate file is treated as "no saved state" (falls back to default, never crashes, never blocks shutdown). Removed WxViewer's XAML `WindowState="Maximized"`. Tests: 13 new `WindowPlacement` unit tests (clamp inside/oversized/off-screen/non-zero-origin, centered default, JSON round-trip, corrupt/zero-size rejection) + full suite green (WxReport 572, MetarParser 261, WxMonitor 69, WxInterp 62, …); CI format gate clean. §13: manual — `docs/test-procedures/WX-291.md` (WPF apps have no UI harness and aren't CI-built, WX-135); the pure logic is unit-covered, the live restore/persist round-trip, first-run default, off-screen fallback, and oversized-shrink are the manual procedure. MINOR: new user-facing feature in the management apps, backwards-compatible; no schema change, no service-binary behavior change. |
| 1.49.0  | 8db1bbf | 2026-07-11 | WX-168 (per-language deterministic timing/claim prose-validator parity — the es plugin; **WX-137** multi-language epic). The reconciler's timing/claim validators — WX-149 (`{q:time}`↔day-part), WX-151 (`{chN}`-anchored timing), WX-152/WX-177 (closing / aggregate precip-at-a-dry-time) — were English-only: for every other language they no-op'd safely (no false rejects) but gave **no deterministic guarantee**, leaning entirely on the language-agnostic prompt rules. This makes them **language-pluggable** and adds the **Spanish** plugin. **Architecture (Paul's directive):** a new `ILanguageLexicon` plugin contract + `LanguageLexicons` registry — a language is one implementation registered by ISO code, `For(iso)` returns `null` for any language without a plugin (every validator then no-ops for it, the safe residual, unchanged), and **adding a language needs zero validator changes**. The validators' LOGIC stays language-agnostic (sentence splitting, time resolution, proximity, the "never reject what you can't prove wrong" policy); the day-part hour ranges are universal (`PartHourRange`); only the word-lists move behind the plugin. **`EnglishLexicon` extracts the current en words verbatim → behavior-preserving** (proven: all pre-existing tests pass unchanged through the seam; the one Spanish word the old shared list carried, `madrugada`, moved to the es plugin — en prose never contains it). **`SpanishLexicon`:** the residual policy bites harder in es and that is deliberate — `mañana` (morning|tomorrow), `tarde`, `noche` are AMBIGUOUS → omitted from the day-part set (only the unambiguous `madrugada` remains), and because the one es word for "tomorrow" *is* that ambiguous `mañana`, es has **no deterministic tomorrow trigger**; the es time resolver fires only on the unambiguous `hoy`/`esta noche`, and the real es coverage this adds is the closing/aggregate precip lexicons (lluvia/tormenta/…, no/sin/…, seco/seca). **Scope (grooming option a):** the abstraction + es only; the other live target languages (eo/da/de each have a recipient) get their own plugins as follow-ups, **each gated on a native/QA-validated lexicon** (a wrong foreign entry FALSE-rejects, worse than the safe no-op) — bounded by the QA-judge-first direction (WX-238/246). Also folded in: `CheckSevereStormVocabulary` (WX-284) now resolves the es lexicon, so es storm sentences get window-scoping too; and `WX-177-verify.sh`'s WX-283 precip-at-a-dry-time count was scoped to `narrative 'en'` so the es hits this ticket newly introduces (identical inner string, `'es'` tag) don't inflate that en trend. Tests: 572 green — es acceptance (caught closing precip-at-a-dry-time, caught es madrugada/token contradiction, ambiguous-`mañana` skip, legitimate es passes) + the full en suite unchanged. §13: `docs/test-procedures/WX-168.md` (+ `-verify.sh`, `'es'`-scoped) — post-deploy trustworthy-absence monitor for es false-rejects. MINOR: recipient-affecting new deterministic coverage (fewer incorrect es reports ship), backwards-compatible; no schema change. |
| 1.48.0  | 79dddc8 | 2026-07-11 | WX-284 (confine recipient precipitation terminology to what the reader acts on; **WX-153** editorial-clarity epic). Three code-reviewed steps in one PR, all presentation-layer — the `PrecipPhenomenon`/`PrecipExpectation` data model and persisted `changes[]` are unchanged; the collapse happens only at the recipient surfaces (deterministic renderer, the reconciler prompt, and the change-significance gate), routed through one shared classifier (`RecipientPrecip`) so the renderer, the `DeterministicChangeDetector`, and the consistency oracle (`ForecastReconciler`) always agree — the WX-189/151 detector↔oracle tautology stays green. **Step 1 — phenomenon collapse** (shipped in `50ae3fb`): non-severe liquid precipitation (rain / showers / drizzle / a non-severe thunderstorm) all read as plain **"rain"**; "severe storms" wording is reserved for a `SevereFlag` block; frozen precip (snow / wintry mix / freezing rain) keeps its own words (this collapses convective *intensity*, never precip *type*). A Rain↔non-severe-Thunderstorm swap is a non-change; a Rain↔severe crossing is narrated once on the severe axis, not as phantom "rain clearing." **Step 2 — likelihood collapse**: "possible" and "likely" read as the same register to most recipients (Niki), so they collapse to one word — the surviving hedge word is **"possible"** ("likely" never reaches a reader); "expected" is reserved for a `Certain` non-severe block; and **severe is always "possible"** at every tier (we warn that severe is possible, we never promise it — never "likely"/"expected"). The collapse propagates into change-significance: a possible↔likely move no longer warrants an unscheduled send, and a severe block is pinned to the top of the intensity ladder (`RecipientPrecip.Expectation`), so only a `SevereFlag` onset/clearing drives a severe change — a tier bump on a block that stays severe is inert. **Step 3 — change-band reframe**: the amber band's label becomes **"Why this update:"** (a data-only EF migration relabels the `WhatsChangedLabel` en token; `SeedTemplateStore` parses the relabel so tests + goldens track it; no `SchemaVersion` bump — that lives on the snapshot/send bodies, not `LanguageTemplates`), and the `changeSummary` prompt contract now names the single triggering change rather than enumerating every difference, deferring the rest to the forecast grid. The WX-177/165 real-differences prose hygiene is preserved throughout. Tests: 563 WxReport + 261 MetarParser green — new/inverted detector, oracle-fold, prompt-lock, and renderer tests; 20+ en goldens re-recorded, each verified token-exact against the intended collapse. §13: `docs/test-procedures/WX-284.md` (post-deploy — confirm the migration relabel applied, smoke the new vocabulary on a live report, opportunistically confirm the reframed band). MINOR: recipient-visible presentation behavior change, backwards-compatible; data model, `changes[]`, and `SchemaVersion` untouched. |
| 1.47.2  | 482f8b7 | 2026-07-10 | WX-177 (reconciler prose self-consistency, round 2; **WX-153** editorial-clarity epic). Two narrative-vs-snapshot defects, both deterministically checkable with **no extra Claude call** (the three-layer LLM output-integrity pattern). **Defect A** — a closing that calls an *aggregate period* dry while a day within it is wet ("The weekend stays dry" over a stormy Sunday) is self-contradictory. `ValidateClosingClaims` didn't catch it: "weekend" isn't a precip word and the claim is *negated* (dry), both of which `CheckClosingSentence` skips. New **`CheckAggregateDryClaim`**: a "the weekend stays **dry**" claim (unambiguous dry words only — `AggregateDryWords`) contradicted by precip on a Saturday/Sunday block in the `final_snapshot` → `NarrativeProseException` → existing retry-with-feedback → WX-189 section degrade. Weekend-only (Sat+Sun are the one aggregate we can resolve unambiguously; "this week"/"the next few days" lean on the prompt), **en-only** (English lexicon, like `CheckClosingSentence`; es leans on the prompt), with a negation guard so "the weekend won't stay dry" (which asserts *wet*) is never false-rejected. **Defect B** — a "What's changed" figure that conflicts with the day's forecast-grid headline (narrative "8 mph" vs grid "13 mph", both individually true) — handled **prompt-side** (cite the day's grid-headline figure, not a sub-period reading), since the two figures are legitimately different quantities and a validator can't demand equality. Two prompt rules added to `ReconcilerPrompts` (the aggregate-dry rule beside the closing-scope rule; the grid-figure rule in the change-band section). **Deferred to WX-283** (Low, gated on this ticket's §13 evidence): the deterministic weekday-pinned / multi-window resolver for the category case — both real repros are multi-time-reference sentences the "exactly one reference" safety rule skips, so it would add false-reject surface for an unproven gain; the prompt rules cover the category case meanwhile and §13 measures whether that's enough. 3 new `ForecastReconciler` tests (weekend-dry-over-stormy-Sunday drop, all-dry-weekend survives, negated-claim survives); full suite green, format clean; no regression to the WX-149/151/152/165 validators. §13: `docs/test-procedures/WX-177.md` (+ `-verify.sh`) — post-deploy, zero exhausted "calls the weekend dry" degrades over an active day (keyed on the inner string so it never conflates with WX-139's synoptic degrade or the precip-at-a-dry-time family), plus a before→after count of the precip-at-a-dry-time degrades as WX-283 evidence. PATCH: recipient-visible correctness fix, backwards-compatible; prompt + validator only, no schema change. |
| 1.47.1  | 069b6ff | 2026-07-10 | WX-139 (WxReport prose must not attribute a synoptic *mechanism* it cannot evidence; **WX-153** editorial-clarity epic). Four delivered reports (2026-06-07) explained grounded effects with an **invented cause** — "gusts ... as a frontal boundary pushes through", "showers ... as a front pushes through" — while the active TAF held winds steady southerly (no frontal veer) and GFS showed no wind shift. The model's inputs are single-point data (no pressure field, no upstream stations, snapshot blocks carry **no wind direction**), so it structurally cannot evidence a "why"; a confidently-wrong cause reaching recipients is a credibility defect of the WX-108 family (prose must not claim what the data cannot support). The existing narrative rules policed the **effect** (reality, timing, certainty, jargon) but left the **cause** ungoverned — this closes that gap. **Two layers, mirroring the WX-128 unit-rule precedent. (1) Prompt rule** (`ReconcilerPrompts`, cached block, sibling to the WX-108 certainty rule): state the effect, never a synoptic mechanism, in any language — with a spelled-out example list (fronts, pressure systems, troughs/ridges, shortwaves, jets, convergence zones, up/down-slope + on/off-shore flow, "system"/"disturbance"/"impulse"/instability/lift/forcing) so Claude avoids them up front and the validator rarely has to fire. `reasoning_trace` is exempt (internal). **(2) Deterministic validator** (`CheckProse` in `ForecastReconciler`): two compiled regexes (`SynopticEnglishMechanism`, `SynopticSpanishMechanism`) scan the narrative sections per language and throw `NarrativeProseException` → the existing WX-189 retry-with-feedback → independent-section degrade, fail-closed so no mechanism reaches a recipient. **Unambiguous terms only** — multi-word phrases spelled out so an ambiguous stem never over-matches ("convergence **zone**", not "convergence"); the bare temperature words *high*/*low* are never touched (only "…pressure"); the "front"/"frontal" arm is lookbehind-guarded so the positional idiom **"in front of"** stays legal; the Spanish arm matches only "frente" + a weather adjective or article ("un/el frente"), so the positional **"frente a la costa"** (= off/facing) stays legal. Bare *system/wave/trough/ridge/boundary/cap* are prompt-only (too many innocent uses to hard-reject). en/es only (the enabled languages); other languages lean on the prompt (the WX-264 pattern). WX-111 (wind-shift detection) is the designed rehabilitation path — once a detector can evidence a frontal passage, the rule relaxes to "only when it fired" (relates-link). 6 new `ForecastReconciler` tests (front-in-closing/changeSummary drops, multi-word "low pressure" drop with bare-"low" safe, "in front of" legal, es "un frente frío" drop, es "frente a" legal); full suite green, format clean. KDWH replay fixtures re-recorded (material prompt change). §13: `docs/test-procedures/WX-139.md` (+ `-verify.sh`) — post-deploy, confirm live Claude complies (zero exhausted synoptic-mechanism degrades over an active day), keyed on the inner "synoptic mechanism" string so the pre-existing precip-at-a-dry-time `ValidateClosingClaims` degrades (WX-177) are never conflated. PATCH: recipient-visible correctness fix, backwards-compatible; prompt + validator only, no schema change (`SchemaVersion` untouched). |
| 1.47.0  | 29681df | 2026-07-09 | WX-279 (grafana zero/low-dashboard watcher — the observation-plane backstop to WX-278's prevention-plane bake; **WX-7** containerize epic). A new `GrafanaDashboardWatcher` on the WX-275 framework probes the local grafana each cycle via `GET /api/search?type=dash-db` and raises an email alert when the served-dashboard count falls **below a configurable floor** (default 4). Motivation: the 2026-07-09 boot race left grafana reporting `/api/health` **healthy** while provisioning **zero** dashboards — a silent failure found ~3h later only when a bookmark broke; health can't see it, so a **count-based** signal is the instrument. **Direct `IWatcher → Finding → EmailSink`** (not the ticket's original "route through WX-273" idea — `ReportErrorWatcher` scans shipped report bodies, never WxMonitor's own state, and the WX-275 seam already gives direct escalation). **Auth = a Grafana Viewer service-account token** (least-privilege, read-only; bearer header, so the colon in the admin password is a non-issue), read from the host-only `appsettings.local.json` — **no secret in git and no schema change** (the non-secret `GrafanaUrl`/`GrafanaDashboardFloor` default in `appsettings.shared.json`; the token never leaves the host). **Startup suppression via a 2-reading debounce** (`AlertThreshold`=2): a shortfall must persist two consecutive cycles before alerting, which rides over grafana's own boot/provisioning window without tracking its start time (new `MonitorState.GrafanaSubFloorStreak`, reset on a healthy reading, **left unchanged on a probe failure** — an unreachable/booting grafana is an *inconclusive* reading, deliberately **not** a confirmed zero, so a down grafana never false-alerts). Signal is `/api/search`, **not** the `grafana_stat_totals_dashboard` gauge — prometheus scrapes only itself + the otel-collector, so the "gauge is cheaper/no-auth" premise didn't hold. Config-gated **dormant** (blank `GrafanaUrl` or non-positive floor → the watcher no-ops), so it ships inert until the token is set. `MonitorState` gains two additive fields (`LastGrafanaDashboardAlertSentUtc` cooldown + `GrafanaSubFloorStreak`), both JSON-backward-compatible — **no `wxmonitor-state.json` format break, no migration, no `SchemaVersion` bump**. 12 new tests (disabled gates, healthy/reset, debounce, alert + cooldown wiring, partial + zero shortfall, streak cap, probe-failure inconclusive, response parsing); WxMonitor suite green. §13: `docs/test-procedures/WX-279.md` (post-deploy calibration — force a shortfall via a floor bump against live grafana). MINOR: new feature, backwards-compatible; no schema change. |
| 1.46.0  | f57591c | 2026-07-08 | WX-273 (report-error watcher — first product watcher on the WxMonitor framework; **WX-23** monitoring epic). A new `ReportErrorWatcher` scans shipped report bodies (`CommittedSend.EmailBody`) for operator-defined, language-scoped error patterns and records new hits to a JSONL findings file — permanent visibility into the intermittent, LLM-generated translation/wording errors that QA sampling can't reliably catch (the WX-239 class). **Patterns** load from `report-error-patterns.json` under InstallRoot (`{id, language, regex, description, severity}`, re-read each cycle → live reload, no redeploy/UI); `language` is an ISO code or `*`. **Language is attributed live** from each send's recipient (`CommittedSend.RecipientId → Recipient → Language.IsoCode`), dissolving the "CommittedSend has no language column" caveat. **Forward-only:** a per-cycle watermark on `CommittedSend.SentAtUtc` (new nullable `MonitorState.LastReportScanUtc` — keyed on send-time, not `Id`, since `Id` is assigned at provisional insert while `SentAtUtc` is set at send completion, so send-time is monotonic with ship order and a lower-`Id` send that ships later isn't skipped) baselines to the latest send-time on activation and scans only newer qualifying sends (`SentAtUtc` set, non-diagnostic, body present); dedup is one finding per `(reportId, patternId)`. **Output** = one JSON line per finding (`{timestamp, watcher, reportId, recipient, language, patternId, severity, snippet}`) appended to `findings.jsonl` in the logs dir — a durable record, not the rolling log and not email; so no cooldown. Unicode-correct (regex over the EF string — `eo` diacritics match). Built on the WX-275 framework: `ReportErrorWatcher : IWatcher` returns `Finding`s routed to a new `JsonlSink` via the now-widened per-watcher sink set (`Finding` gained an optional `Fields` bag; `WatcherContext` gained `Paths`). No schema change (JSON config + one additive nullable state column, no migration; no `SchemaVersion` bump). 7 new SQLite-backed tests (baseline, match+fields, benign, no-re-log, live language attribution, Esperanto diacritics, end-to-end JSONL write); 53 WxMonitor tests green. §13: `docs/test-procedures/WX-273.md`. MINOR: new feature, backwards-compatible. |
| 1.45.6  | 6fe70a4 | 2026-07-08 | WX-275 + WX-276 (WxMonitor watcher framework + the latent METAR-gating bug it surfaced; **WX-23** monitoring epic). **WX-275 (behavior-preserving refactor):** the three hand-unrolled checks in `MonitorWorker.RunCycleAsync` (log-scan, heartbeat, METAR-staleness) become `IWatcher` implementations behind a small framework — `IWatcher` / `WatcherContext` / `ISink` / `Finding` + `EmailSink` (which owns cooldown via a typed `CooldownSlot` and marks-on-**successful**-send, so a failed SMTP attempt doesn't burn the window) — and the worker shrinks to a scheduler that builds a per-cycle context, runs each watcher, routes findings to the email sink, and persists state if dirty. The persisted `wxmonitor-state.json` / `MonitorState` format is **byte-for-byte unchanged**, which is what lets a characterization test (log-error, cooldown suppression, stale/fresh heartbeat, stale/fresh METAR — asserted through injected emailer/clock/state seams) stay **identical and green before *and* after** the restructure — the proof it preserved behavior. Landed as two commits (a pure-DI seam commit that establishes the "before" baseline, then the restructure). Sinks are set-valued (the and/or seam for a future JSONL report-watcher, WX-273) but wired one-per-watcher today; genericizing state *storage* was deferred (it would be a format change, i.e. itself a behavior change). **WX-276 (the bug it surfaced):** `RunCycleAsync` returned early when zero watched services were configured, silently skipping the independent METAR-staleness check; the coupling is removed so METAR-staleness runs regardless. **Latent only** — production always lists WxParser.Svc + WxReport.Svc, so **no recipient-visible or production-behavior change**. Adds the WX-210 SQLite in-memory harness to `WxMonitor.Tests`; a regression test reproduces the gating bug (red→green); 46 green. No schema/migration/UI change. §13 = **N/A** (behavior fully unit-covered; the zero-watched-services condition does not occur in production config). PATCH: WX-276 is a backwards-compatible bug fix; WX-275 is behavior-preserving and ships with it. |
| 1.45.5  | ebb0306 | 2026-07-06 | WX-253 (a blocked token disables its language non-destructively, with a deliberate re-enable recovery path; **WX-270** RAS epic). Before, a non-representable token hit during the WX-250 top-up marked the language **BLOCKED** but left `IsEnabled` **true** — a dead-but-enabled limbo: it could not send (the WX-171 gate fails its recipients closed), yet it stayed in the enabled/generation set and the tab's own guidance told the operator to *manually* click Disable; re-enable cleared the error but the blocked placeholder rows lingered, so the token was never re-attempted. WX-253 closes both ends. **(1) block → disable** (`ReportWorker.ApplyTopUpAsync`): a surviving block now flips `IsEnabled` to false **non-destructively** (every row kept — reviewed edits, the clean fills, the blocked placeholders) with a legible `GenerationError` naming the blocked tokens and the recovery hint; the language leaves the enabled/generation set at once, no manual step. **(2) re-enable re-attempts** (`LanguageToggle.SetEnabledAsync`, the deliberate operator action after a renderer/code fix makes the token expressible): it now **deletes the generator's own blocked placeholder rows** (not-`Representable` **and** never-`ReviewedBy` — Claude's empty rows) so those tokens become *no-row* and the existing WX-250 auto-scan re-attempts them, while **preserving every human-`ReviewedBy` row** (WX-249 durability) — including a value an operator typed into a blocked token, which stays non-representable (the send gate excludes it) and simply re-blocks next cycle rather than being silently destroyed. **Self-limiting**: if a token is still blocked after the re-attempt, block→disable simply re-fires — no per-cycle retry, no single-slot starvation. Scope is **service + data only**; the operator-facing surfacing of the disabled *reason* folds into **WX-261** (already chartered for the disabled-list guidance panel — now carrying a third sub-state, *auto-disabled-by-block*, beyond Dormant/Available), and stays legible in the interim via the WxReport log line and diagnostic. No schema/migration/UI change. Tests: block→disable-non-destructive and re-enable-deletes-blocked-keeps-representable added; the pre-existing `KeepsPreExistingBlockedState` retargeted to the new **Disabled** contract (WxReport 538 green). §13 = **N/A** (service behavior fully unit-covered; the live disabled-state observation is deferred with the UI to WX-261). PATCH: reliability behavior refinement, backwards-compatible, no schema change. |
| 1.45.4  | 886fa3a | 2026-07-06 | WX-269 (top-up prioritizes enabled languages by current-subscriber count; **WX-270** RAS epic — first child). The single per-cycle WX-250 generation slot was ordered by urgency-tier then **alphabetically by iso**, ignoring recipients — so a subscriber-**less** enabled language (`da`/`sq` with no recipients) could take the slot ahead of one serving real recipients (`de`/`es`/`eo`), and a 0-recipient language was ranked tier-0 "actively SUPPRESSING live reports" though it suppresses nothing. Now ordered: **subscribed languages first** (0-subscriber last — never urgent), then the existing **urgency tier** (hard-suppression stays dominant *among* subscribed), then **descending subscriber count** (replacing the alphabetical tie-break), then iso. Subscriber counts come from **one grouped query** over `Recipients` (explicit `LanguageId` — exact for every language that can enter the work set; a null `LanguageId` falls back to the baseline en default, which is always complete and never topped up, so it never skews a target's rank). The ordering was extracted to a pure, testable `OrderGenerationCandidates` seam and `GenerationSlotTier` made state-pure. Two unit tests (subscribed-first + most-subscribed-first; and hard-suppression outranks a higher-subscribed soft-only language — settling the one debatable composition question: no jump). No schema/migration/UI change; recipient-facing effect is faster recovery of subscribed languages after a new token is added. §13 = **N/A** (deterministic ordering fully unit-covered; no live top-up in flight to observe — all languages currently complete). PATCH: internal scheduling refinement, backwards-compatible. |
| 1.45.3  | a282a45 | 2026-07-06 | WX-264 (daypart/day span labels made consistent with their own {q:time} tokens; WX-153 epic). The free narrative labeled a time window with a day/daypart word inconsistent with its own tokens — a **cross-midnight day-label** ("the early hours of Tuesday" dropping the Monday-evening start) and a **within-day daypart word** ("afternoon around 6:00 AM"). Fixed via the idiomatic-vocabulary approach (Paul; **no rigid renderer token** — placeholder templates would drift from the words that fill them): **(1)** `DayPart1` ("early hours"/"madrugada"/"frühe Morgenstunden") added to the `PromptGlossaryTokens` registry so the narrative anchors on the approved 00–06 word (the piece WX-265 deferred); **(2)** the reconciler prompt rewritten — the stale "00–06 has no safe word" rule now uses the approved word, plus a span-labeling rule: label a window by **both** ends (day + daypart), and a window crossing ≥1 local-day boundary names the day **and** daypart of **both** termini ("Monday evening into the early hours of Tuesday"), a day-only terminus permitted when the whole day is implied ("Tuesday into the early hours of Wednesday"), never the tail day alone; **(3)** the WX-149 prose-hygiene validator strengthened as the deterministic guarantor — **4a** relaxes the adjacency gate so a daypart word bound across a single connective ("afternoon **around** {q:time}") is checked (the exact hole that shipped the bug), keeping a two-word gap non-binding; **4b** adds a cross-boundary **both-days** check (an English window whose `{q:time}` tokens span ≥2 local dates must name both bounding days — daypart optional per terminus; conservative, a relative-day word skips the sentence; **English-only** like `ValidateClosingClaims`, other languages lean on the prompt whose localized day names come from `day_name_reference`). "early hours" added to the validator's daypart-word set. Both checks route through the existing retry-with-feedback → per-section degrade. Migration additive (1 glossary row), no schema change; no renderer goldens change (prompt + validator only). New `ForecastReconciler` tests: connective-mismatch reject, cross-midnight tail-day-only reject, both-days day-only-terminus accept (WxReport 530 green). §13: `docs/test-procedures/WX-264.md` — confirm the DayPart1 glossary row + eyeball live/QA daypart-and-day labeling. PATCH: recipient-visible narrative-accuracy fix, backwards-compatible; prompt + validator + additive glossary, no report-body schema change. |
| 1.45.2  | d36cf89 | 2026-07-06 | WX-265 + WX-267 (rationalize daypart vocabulary + a new-token runbook; WX-153 epic / bundled Task). **WX-265:** renamed the three daypart tokens `PartMorning`/`PartAfternoon`/`PartEvening` → ordinal keys `DayPart2`/`DayPart3`/`DayPart4` (enum `DayPart.Overnight/Morning/Afternoon/Evening` → `DayPart1..4`, each doc-commented with its local-hour block), and **added `DayPart1`** ("early hours") for the 00:00-06:00 block that never had a token. A data-only EF migration (`RenameDaypartTokens`) renames the `Token` key in **both** `LanguageTemplates` and `PromptGlossaryTokens` (the latter fail-closed-validated against `Tok` at load, so a one-table rename would silently drop daypart anchoring) and seeds the en `DayPart1` row; the historical seed migration is left untouched. **Rendered output is unchanged** — `DayPart1` is seeded but not consumed by the deterministic renderer (the 00-06 block stays clock-bound "00-06" per WX-190) until WX-264 wires it into the narrative; goldens pass without re-record. `DayPart1` ships **Required** (not `Soft`): deployed during a window with no scheduled sends pending, so the WX-250 top-up fills es/eo/de one-language-per-cycle (~minutes at the 5-min cycle) with no recipient impact — the Required-vs-Soft trade is documented in WX-267. Test-infra: `SeedTemplateStore` gains a `RenameToken` parser (symmetric to its `Relabel` handling) so `TokSeedParityTests` stays green **without** editing the historical migration; a new parity test asserts the rename + the "early hours" seed. **WX-267 (bundled, pure-docs):** new runbook `docs/adding-or-changing-a-report-token.md` capturing the new-token lifecycle — en-seed → top-up one-per-cycle, the Required-vs-Soft decision (with the WX-256 zero-window precedent), suppression-window management, the current Vocabulary-tab limitation (edits existing rows only), and the log-watch + per-language QA loop. No `SchemaVersion` bump (data-only, model snapshot unchanged). §13: `docs/test-procedures/WX-265-267.md` (+ `-verify.sh`) — confirm the rename landed in both tables, en carries `DayPart1..4` with no `Part*` remaining, and es/eo/de acquire `DayPart1` via top-up. PATCH: internal vocabulary refactor + one additive dormant token, **no recipient-visible change**, backwards-compatible; migration data-only, no report-body schema change. |
| 1.45.1  | b56dd46 | 2026-07-04 | WX-257 (report clarity: two-line legends; WX-153 epic). Two single-line legends in the email wrapped awkwardly at whatever width the client gave them; each now breaks at its natural point. **(1) Reported-Conditions attribution** — station on line one, observation date/time on line two (the hardcoded `·` join dropped in favor of the line break). **(2) Grid time legend** (`GridTimeLegend` token) — "…24-hour clock:" on line one, "00 = midnight, 12 = noon, 24 = midnight." on line two, split on the phrase's first colon. Both fixes are **code at the two `StructuredReportRenderer` emit sites** — a `<br/>` in a vocab string can't work (the `HtmlText()` escaping turns `<`/`>` into visible text, and legend 1 is a 3-piece code-assembled join, not one template). Each piece stays HTML-escaped; the `<br/>` is raw markup. Legend 2 degrades to one line if a phrase has no colon; the ASCII `:` is the colon-equivalent for the Latin-script languages we support (en/es/de/da/eo/sq) — a non-Latin culture is deferred to the per-culture locale work (WX-138, noted there). No vocabulary/token/migration/wording change. 45 en renderer goldens re-recorded; the direct legend test asserts the split + no escaped `&lt;br/&gt;`. §13 = N/A (deterministic HTML fully golden-covered). PATCH: recipient-visible readability polish, backwards-compatible, no schema change. |
| 1.45.0  | 9f5298a | 2026-07-04 | WX-258 (editable English source + idiomatic root transformation of dayparts/day-names; WX-137 epic). Two gaps from today's reports, both downstream of WX-244 anchoring the daypart words into the free-narrative glossary. **(A) The Vocabulary tab's English source is now editable in place.** The daypart tokens were seeded capitalized (en `PartAfternoon`="Afternoon", es "Tarde") but read lower-case in normal use, and English was the one value the editor couldn't reach (`IsoCode != "en"` selector filter). The read-only "English (source)" column becomes a two-way, live-validated column (header "English"; the target column relabels "Phrase"→"Selected Language"): an English edit writes the shared `en` `LanguageTemplates` row and is guarded by the same Mirion validation as a target phrase — non-empty + **must keep the token's original `{n}` placeholders** (every target was validated against them, so a placeholder change here would silently desync all languages — that stays a migration). The save path is **per-field gated** so an English-only edit writes only the `en` row and does **not** re-stamp the target language's `ReviewedBy`/`ReviewedAtUtc`. English is still not a selector entry (no English "language" is picked); Rerun QA is unchanged (target-vs-English back-translation has no meaning for English). English edits are durable (top-up is fill-only, en is never regenerated). **(B) Daypart sources corrected to canonical lower-case** via the now-editable column — live-DB edit only, no seed-fix migration (the deterministic path already lower-cases and Part C licenses idiomatic recasing, so the stored casing is no longer load-bearing). **(C) The narrative vocabulary-usage license is broadened** in `ReconcilerPrompts` + `NarrativeGlossary` from "inflect for agreement + recase" to the full same-root idiomatic set: use the approved wording's ROOT, rendered however idiom requires — inflected, **compounded** with an adjacent word, or **derived into another part of speech** (e.g. an adverb) — capitalized to fit; no synonym, no forced dictionary/citation form (en "Friday afternoon" → de "Freitagnachmittag" compounded, eo "vendrede posttagmeze" derived to adverbs). No judge-prompt change (the QA judge already scores in-context naturalness). Also marks **dormant (disabled) languages** in the Vocabulary picker as "— disabled" (WX-249 keeps their rows; WX-261-adjacent). New `Guidance_LocksIdiomaticVocabularyLicense` test; WxManager compiles, WxReport 526/0. §13 manual: re-render the daypart scenario in enabled+ready languages with a recipient and eyeball the narrative. No schema change (no migration). MINOR: recipient-visible narrative idiom + a new editor capability, backwards-compatible. |
| 1.44.0  | 0daab17 | 2026-07-04 | WX-249 (non-destructive language disable; WX-137 epic). Disabling a report language used to **delete all of its** `LanguageTemplates` rows and revert it to needs-generation (WX-222: "templates exist iff enabled+generated"). Under the WX-247 en-only model, where every target row is generated + human-QA-curated (`ReviewedBy`/`ReviewedAtUtc` — e.g. Albanian `VisHazy→Turbullt`, es `Tarde-Noche`), that purge is a silent data-loss hazard: a single disable incinerates all curated review with no undo, and re-enabling forces a full regen + re-review. New invariant: **curated templates are durable data; `IsEnabled` gates _use_, not _existence_.** (1) Disable becomes non-destructive — flip `IsEnabled=false`, keep the rows and the `GeneratedAtUtc` stamp; the language is dormant. (2) The WX-171 startup completeness check is scoped to **enabled** languages, so a dormant-incomplete language raises no false `INCOMPLETE` ERROR (the send gate already keys on `IsEnabled`). (3) The generator no longer discards a top-up for a language disabled mid-call — the stamp never writes `IsEnabled`, so the concurrent disable survives EF's column-scoped UPDATE and the freshly-generated rows are kept (dormant _and_ generated). Re-enabling reuses the rows in place; WX-250 top-up fills only baseline tokens added while disabled, so prior review survives untouched. The DB persistence is extracted from WxManager's WPF code-behind into a testable `LanguageToggle` seam (5 new `LanguageToggleTests` prove disable→re-enable preserves a reviewed row + its stamps). No schema change (no migration). MINOR: new durability guarantee + non-destructive-disable behavior, backwards-compatible; retires the WX-222 purge-on-disable rule. |
| 1.43.0  | 3e69ddf | 2026-07-04 | WX-256 (noon/midnight words + the **soft-token** class; WX-153 epic). "12:00 PM" for noon is wrong (noon is the meridian, not _post_ meridiem); reports now render noon — and, in a schedule statement, midnight — with the language's own word. **Two rendering contexts:** an EVENT time in the body (obs "Reported Conditions" line, `{q:time}` narrative tokens, subject) renders the **bare noon word** ("noon" / "mediodía"), keeping midnight as the date-bound culture form ("12:00 AM" — a floating "midnight" would drift across the day); a SCHEDULE time (welcome "you'll receive your report at …") renders a precise **HH:MM + designator** where noon → "12:00 noon" and midnight → "12:00 midnight". Non-noon/midnight times follow the culture convention; per-recipient/location locale override is deferred (WX-138). **The key mechanism — SOFT tokens.** The noon/midnight words are curated `LanguageTemplates` tokens (`CultureInfo` has no noon/midnight designator, so this truth must live in the DB — en-seeded, top-up-generated + QA-reviewed for targets), but a cosmetic time word must not fail-closed like a hazard. A new **`Tok.Soft`** allowlist (`{ noon, midnight }`) is subtracted from the suppression set: **`Tok.Required` = `Tok.All` − `Tok.Soft`**, and the two send gates + the startup completeness check switch to `Tok.Required`. A language missing ONLY soft tokens still **sends** (the renderer degrades via `TemplateSet.Has` to the culture 12-hour form) instead of suppressing the whole report; hazard tokens stay hard fail-closed (softness is an explicit opt-in — a token is required unless listed). Parity + top-up still use the full set, so the words are still seeded (en), parity-checked, and generated for every enabled language — the DB stays the source of truth. This ships the "token-severity fallback" WX-247 deferred, and **deploys with no suppression window**: on deploy the target languages lack the words and simply render the 12h fallback while WX-250 top-up fills them over the next cycles (the live demo of the en-only architecture). One central `FormatEventClock`/`FormatScheduleClock` helper routes the four time-format sites; the forecast grid is 24-hour and untouched. A migration adds the two en tokens (additive). New `SoftTokenTests` (classification, send-gate behavior, both clock contexts, fallback); the two welcome goldens re-recorded ("6 AM"→"6:00 AM"). §13: functional-verify the deploy soft-token behavior + top-up generation. MINOR: new recipient-visible capability + the soft-token class, backwards-compatible; migration additive, no report-body schema change. |
| 1.42.3  | 3590204 | 2026-07-04 | WX-251 + WX-252 (English-only seeding — seed migrations author en only + test-infra flip; WX-247 sub-tasks, WX-137 epic). Completes the WX-247 en-only-seeding architecture: **English (LanguageId 37) is the only seeded language**; every target language (es/de/da/eo/…) is generated on enable and curated in the DB via WX-250 top-up, not authored by hand in the migrations. **WX-251 (seed):** the five seed/token migrations are edited **non-destructively** — editing an already-applied migration neither re-runs nor deletes prod rows, and seed `InsertData` is not in the model snapshot, so there is no Designer/snapshot churn. `AddLanguageTemplates` drops the ~100 es template rows, the es `CultureName` seed, and trims its drift guard to en (references no non-en id); `AddMeteogramCaptionTokens` / `AddMeteogramLabelTokens` / `AddPromptGlossaryTokens` drop the hardcoded `{37,39,32,31,38}` THROW guards and the non-en `InsertData` rows / caption relabels (Down() cleanup loops narrowed to `{ 37L }`; the language-neutral `PromptGlossaryTokens` table + registry seed untouched); `RelabelCurrentToReportedConditions` drops its now-inert non-en UPDATEs; `BackfillSeededLanguageReady` needs **no SQL change** — its set-based stamp (any enabled language whose representable templates cover the full en set) simply stops marking es once es has no seed rows, so "stop stamping es READY" falls out of removing the seed. es stays **enabled** (WX-166 registry untouched) and reaches READY via generation; its regional locale (`es-US` vs `es-ES`) is no longer pinned by the seed — top-up sets a valid culture, and per-recipient locale is **WX-138**. **WX-252 (test infra):** `SeedTemplateStore` yields en-only; the parity gate is renamed `Tok_matches_en_seed_exactly` (en-only, es SetEquals dropped); pure-Spanish-content tests are removed (translated content is the QA harness's concern) while mechanism tests self-gate on `LoadedLanguages`; the golden corpus is en-only — the 46 `*.es.golden.html` fixtures deleted, the 47 en goldens **byte-identical** (no re-record). **Deploys dormant** — the seed edit is a no-op on the already-migrated prod DB and the test-infra ships in no binary, so there is no runtime behavior change (the live functional proof of en-only generation already rode WX-246). PATCH: no recipient-visible or schema change; the `MetarParser.Data` binary differs (migration source), so a versioned release; refactor toward the en-only architecture, backwards-compatible. |
| 1.42.2  | 34b44b7 | 2026-07-03 | WX-244 (extend the WX-190 daypart discipline into the free narrative; WX-153/WX-137). The free-composed prose rendered the **evening** daypart with a **night** word in es — "el martes por la tarde y **la noche**", even "hasta **la noche del martes**" — which in our daypart model reads as the *next* day's wee hours (Tuesday night = Wednesday 00-06), mislabeling the day. The deterministic side was already correct (WX-190 clock ranges; es `PartEvening` = "Tarde-Noche" after a Copy→DB); only the free narrative lacked the discipline (same deterministic-vs-free split as WX-238). **Fix (one layer, prompt):** (1) the three named dayparts (`PartMorning`/`PartAfternoon`/`PartEvening`) are added to the language-neutral `PromptGlossaryTokens` registry, so `NarrativeGlossary` anchors the prose on the approved daypart word per language (es evening = "Tarde-Noche", not "noche") — values sourced from `LanguageTemplates`, DRY-clean; (2) a time-of-day directive in the reconciler system prompt: never attach a night-word to a day name ("`<day>` night" = the following night); evening → the approved word; the 00-06 block is the **early-morning hours of its own day** ("la madrugada del sábado") — a clock band, not sunrise-relative ("pre-dawn"), distinct from the 06-12 morning, never "`<prior day>` night". 00-06 stays token-less (WX-190) — handled by the rule, not a token, because most languages lack a clean single word (es *madrugada* is the exception; en/de/da/eo/sq express "early morning" naturally). New `ReconcilerSystemPromptTests`/`NarrativeGlossaryTests` lock the directive + daypart anchoring. Migration additive (3 registry rows), no schema change; §13 re-runs Translation-QA es + regression (`docs/test-procedures/WX-244.md`). PATCH: recipient-visible narrative-accuracy fix (es), backwards-compatible; prompt + additive data, no report-body schema change. |
| 1.42.1  | 07e7249 | 2026-07-03 | WX-246 (anchor the free narrative on CultureInfo day names; WX-137 epic). The free-composed narrative (`changeSummary`/`closing`) could name the **wrong day of week** — the WX-214 Albanian QA caught English "Tuesday" rendered as `e mërkurën` (Wednesday), a wrong day *root* — because the prose had no authoritative day-name reference. The deterministic grid/obs were already correct (they render day names from `CultureInfo`); only the free narrative drifted. **Fix (prompt injection, no tokens):** at reconcile time, `DayNameReference.Build` computes each narrative language's `CultureInfo` day name for every local date the forecast spans and injects a `day_name_reference` block into the reconciler's **uncached** user message, instructing the LLM to use those exact day words **inflected for grammatical agreement**. Day names are deterministic facts from `CultureInfo` — computed per report, **never persisted as tokens** (the design pivoted away from a controlled-vocabulary token approach mid-grooming: no seed / generation / top-up / migration / parity-gate / deploy-choreography). Grid and prose now draw from the same source and agree by construction; a newly-enabled language just works. New `DayNameReference` + `DayNameReferenceTests` (date→day-name mapping, tz/DST conversion, dedup, chronological order, empty cases; ICU-robust); `BuildUserMessage` made `internal` for the wiring. §13: re-run Translation-QA against `sq` (`docs/test-procedures/WX-246.md`). PATCH: recipient-visible narrative-accuracy fix (all languages), backwards-compatible; prompt-only, no schema/migration/vocab change. |
| 1.42.0  | 2a908ac | 2026-07-03 | WX-250 (top-up generation mechanism — the crux of WX-247 English-only seeding; WX-137 epic). Generalizes generation-on-enable into one **fill-only top-up** rule: each cycle, for every enabled language, translate only the baseline tokens it is *missing a row for* and insert only those — replacing the old PENDING-only delete-all-then-reinsert. A present row (reviewed or not, representable or blocked) is **never** overwritten (`ApplyTopUpAsync` never-clobber guarantee), so curated/reviewed vocabulary survives and a new baseline token propagates to already-enabled languages; the language is re-stamped READY/BLOCKED from its **full** row set (a clean top-up onto a language with a pre-existing blocked token stays BLOCKED). Blocked tokens are deliberately **not** auto-retried here (they have rows → not "missing"); that recovery path is **WX-253**. The single per-cycle generation slot is prioritized to a formerly-READY language now fail-closed suppressing live reports (via the canonical `Language.GenerationState`). Efficiency: the redundant per-cycle `_templates.Reload()` is dropped (completeness computed straight from DB rows), and the free READY-stamp's blocked check is a cheap `AnyAsync` run only for the rare unstamped candidate. The never-clobber guarantee extends to `Language.CultureName` — a top-up sets it only when the language has none yet, so a one-token top-up can't silently re-derive an established language's curated locale (and a transient top-up failure on a formerly-READY language no longer mislabels it BLOCKED/won't-retry). New `TopUpGenerationTests` (SQLite-backed: fill-only insert, reviewed-row survival, new-token propagation, pre-existing-blocked survival, culture preserve-vs-set). No schema change. **Deploys dormant** — no new baseline tokens exist yet, so no behavior change on the live DB until WX-246 adds the day-name tokens (WX-250's live functional proof rides WX-246). MINOR: new backwards-compatible mechanism. |
| 1.41.3  | 624f612 | 2026-07-01 | WX-238 follow-up (reopened from its own §13 verification). The v1.41.2 **phenomenon** anchoring held, but the QA re-run showed the free narrative **violated the probability glossary directive**: es rendered English "likely" as the un-anchored "se espera" (the *expected* register) instead of the anchored "probable", overstating the certainty tier — a miss of WX-238 AC #1 (an approved token must reach the output). **Prompt correction:** the reconciler guidance now states the probability words (possible/likely/expected) name distinct, **non-interchangeable** forecast-confidence tiers — render the source's tier with that tier's own approved word, never a higher/lower one, and never an "is expected"/"is forecast" verb construction (e.g. es "se espera") in place of the anchored word for "likely"/"possible". Prompt-only (no schema/migration/vocab change); still probabilistic, so verified by re-running Translation-QA ×4. The guaranteed deterministic-backstop fallback stays tracked in WX-243 if this doesn't hold. PATCH: recipient-visible narrative-accuracy fix, backwards-compatible. |
| 1.41.2  | 5e22eb2 | 2026-07-01 | WX-238 (translated report free-generates target terms instead of approved vocabulary; WX-137 epic). The free-composed per-language narrative (`changeSummary`/`closing`, WX-128) free-generated target-language weather terms and drifted off the curated `LanguageTemplates` vocabulary — es "llovizna engelante" for approved "llovizna helada", da "Isregn" for "frysende støvregn" — even though the vocabulary judge verdicts were **clean**. The structured body is token-substituted and immune; only the free narrative bypassed the vocabulary, so a reviewed term wasn't guaranteed to reach the reader. **Fix:** inject an **approved-vocabulary glossary** into the reconciler's per-report (uncached) prompt block, anchoring the narrative on the curated terms — including **English** (so the QA judge's back-translation baseline can't drift) — as `concept → «approved term»`. Chosen over lowering the sampling temperature because that lever is **removed on Sonnet 5 / Opus 4.7+**, so anchoring must survive a model migration; the cached guidance tells the narrative to use the approved wording (inflected for agreement, capitalized for position) but never a synonym. A new **language-neutral `PromptGlossaryTokens`** registry (curatable data) lists which concepts to anchor, validated fail-closed against `Tok.All`. **Phenomenon separated from probability (Paul):** the deterministic renderer keeps its fused atomic composites (`rain_likely`, "Severe storms possible") because it can't glue phrases safely across languages, but the narrative composes from the two separated axes — **6 new first-class `Tok` tokens**: phenomenon `storms`/`severe_storms`/`severe_weather` + probability `possible`/`likely`/`expected` (the ladder tops out at "expected" — as certain as a forecast states) — seeded in all 5 languages (en/es reference; de/da/eo extracted from the reviewed composites, advisory until the next Translation-QA run reviews them). New `PromptGlossaryTokens` table (bigint identity, unique `Token`) + 30 vocab rows via migration `AddPromptGlossaryTokens` (additive, auto-applies at startup). New `NarrativeGlossaryTests` + `ReconcilerSystemPromptTests` (a no-op-regression guard that the glossary reaches the prompt); `TokSeedParityTests` accepts the new tokens. §13: re-run Translation-QA all four languages and confirm the divergences clear (`docs/test-procedures/WX-238.md`). PATCH: recipient-visible narrative-vocabulary fix, backwards-compatible; migration additive, no report-body schema change. |
| 1.41.1  | f63fa89 | 2026-06-30 | WX-235 (reopened from §13 IN TEST — restart-recovery fix; WX-137 epic). The first live verification's **step 9 (restart recovery)** exposed a bug: stopping WxReport.Svc **mid-rerun** marked the run **`Succeeded`** over an **empty package** (no judged.json) instead of releasing it for re-run — the operator saw **✓ Updated** while the package never regenerated, silently re-introducing the stale-package problem WX-235 exists to fix. Two layers swallowed the host-shutdown cancellation: (1) **`ClaudeClient`** caught the shutdown `OperationCanceledException` and returned `null` (indistinguishable from a real Claude failure); (2) **`TranslationQaRunner`** then logged each scenario's reconcile as failed-no-artifact, finished with **`Judged=false`** and a stamp, and **`QaRerunWorker`** marked it `Succeeded`. **Fix:** ClaudeClient now **propagates** the shutdown cancellation (so the worker's existing cancel-catch **releases the claim** and re-runs on the next start, per step 9; the report cycle's `stoppingToken`-filtered catches unwind quietly and a spurious "reconciliation failed" shutdown log is removed), and `QaRerunWorker` gates `Succeeded` on `result.Judged` via a tested **`OutcomeFor`** seam — a run that produced no judged package is now **`Failed`**, never a ✓ over an empty package. Tests: `ClaudeClientRetryTests` updated to assert the shutdown cancellation propagates (was: returns null); new `QaRerunWorkerOutcomeTests` (judged→Succeeded, unjudged→Failed). No schema change, no new Claude/judge calls. PATCH: bug fix, no behavior change for a clean run. |
| 1.41.0  | ec11039 | 2026-06-30 | WX-235 (operator "Rerun QA" — regenerate a translation-QA judge package on demand; WX-137 epic). After a Copy→DB or Vocabulary-editor change leaves a judge package **stale** (verdicts still read "Wrong", and Copy→DB trips the changed-since-produced guard), a **"Rerun QA"** button on both the **Translation-QA** and **Vocabulary** tabs regenerates a fresh package for the selected language so the verdicts reflect the updated DB. **Architecture — option C (service-side):** WxManager writes a per-language `QaRerunRequest` row; the WxReport.Svc **`QaRerunWorker`** (a `BackgroundService` independent of the report cycle) atomically claims it (`Status=Running AND StartedAtUtc IS NULL`), runs the **`TranslationQaRunner`** (render scenarios → judge), and records Succeeded+stamp / Failed+reason (with a 30-min stuck-sweep and a shutdown claim-release). WxManager's app-singleton **`QaRerunCoordinator`** polls the row every 10s, rehydrates at startup, and drives the UI. The translation-QA **generate pipeline was extracted** from the console tool into `WxReport.Svc.TranslationQa` (shared by the tool's Phase 1 and the worker); the Gemini judge key moved into **`GlobalSettings`** (single DB source of truth). The path stays **judge-agnostic** via the `IJudge` seam — nothing outside the worker names a concrete judge. **UI:** a shared animated **`RerunQaButton`** (right-pointing chevrons sweep continuously while running, decoupled from poll cadence; transient **✓ Updated HH:mm** on a live completion; persistent **⚠ Rerun failed** with the reason in the tooltip), per-language binding + mutual disable across both tabs, in-flight read-only gating (Copy→DB on Translation-QA, grid on Vocabulary), reload-from-DB on press (Vocabulary, dropping unsaved edits so the judged state == the DB), and guarded auto-reload of the fresh package (Translation-QA, only when its selected language completed). **Schema:** new `QaRerunRequest` table (bigint identity, unique `IsoCode`, enum-as-string status) + `GlobalSettings.GeminiApiKey` (migration `AddQaRerunRequestAndGeminiKey`; trivial, auto-applies at startup). New `QaRerunStoreTests` (SQLite-backed: atomic claim / one-pickup, stuck-sweep, claim-guarded completion, claim-release). Two PRs (#148 service infra + the WxManager UI). MINOR: new operator-facing capability, backwards-compatible. |
| 1.40.1  | ef77fe2 | 2026-06-30 | WX-234 + WX-236 (Extended Forecast grid clarity; WX-153 epic) — two editorial fixes to the same grid temperature cell, shipped together. **WX-236 (order):** the grid's daypart columns run chronologically left-to-right (00-06 … 18-24 local), but the temperature pair printed **High then Low**; since the Low we report is this morning's minimum — the *earliest* point of the day — High-then-Low on a chronological strip read as "today's high, then tomorrow-morning's low." The pair now reads **Low then High** (`StructuredReportRenderer.cs:457`), matching the time-arrow. **WX-234 (partial-day pseudo-extreme):** every report's GFS horizon ends on a partial trailing local day with only its pre-dawn/morning block, and the grid showed that day's morning max as the daily **High** (a steady 98-99 °F week ending in a bogus `High: 90°F`). The grid now gates each extreme on the band that holds it — a High needs an afternoon (local 12:00-18:00) block, a Low needs a pre-dawn/morning (00:00/06:00) block — and renders a suppressed extreme as an **em dash** (`High: —`), keeping the row's other extreme, wind, and conditions. The gating predicate was extracted to a shared `DayPartBands` helper used by **both** `StructuredReportRenderer.AggregateDays` and `TemperatureRangeSummarizer.DailyHighsLows` (WX-230's summary gating), so the grid and the prose derive extremes by one rule and cannot drift. New `StructuredReportRendererTests` (ordering, partial-day em-dash, grid⇔summary agreement, `DayPartBands` predicate); 89 golden HTML fixtures re-recorded for the new order + em dashes. No new Claude calls, no schema change (WX-153 standing constraint). PATCH: recipient-visible presentation fix, backwards-compatible. |
| 1.40.0  | f8b8847 | 2026-06-29 | WX-233 (WxManager Vocabulary maintenance tab; WX-137 epic): a per-language editor for the report vocabulary (`LanguageTemplates`), the direct-edit complement to WX-219's package-driven copy-to-DB. Pick a target language → an editable grid of its tokens; **Phrase and Note are editable**, Token / English source / Context / Representable are read-only (cross-language attributes — changed via migration). English is excluded from the selector (it's the source, not a target). **Live validation (Mirion pattern):** each Phrase edit runs a validator chain (empty; `{n}`-placeholder-must-match-the-English-source) via `INotifyDataErrorInfo` — invalid cells flag red with a fast (~0.2s) explanatory tooltip, and **Save stays disabled until there is ≥1 change and zero errors** (multiple simultaneous errors all block; dirty tracking is value-based, so manually reverting an edit re-disables Save). Clicking back into a flagged cell restores its pre-edit value. Save writes the dirty rows and stamps `ReviewedBy`/`ReviewedAtUtc`. The `{n}`-placeholder guard was extracted to the shared `WxServices.Common.TranslationQa.TemplateValidation` and WX-219's copy-to-DB refactored to use it (one validator for both write paths; future WX-173 "sneer" checks can join there). New `TemplateValidationTests`. MINOR: new operator-facing capability, backwards-compatible, no schema change. |
| 1.39.1  | 8f6eee6 | 2026-06-29 | WX-232 (translation-QA per-check subfolders + structured file names; WX-214 epic): the flat `C:\HarderWare\translation-qa\` folder grew unwieldy as checks accumulated (~8 files per check in one directory). Each check now writes into its own per-check subfolder named `<iso>.<stamp>` (e.g. `de.20260627-192255`), with files named `<lang>.<stamp>.<purpose>.<ext>` inside — purpose being a scenario name, `request`, `response`, or `judged` (the per-scenario reports carry the render language as the prefix: `en.<stamp>.<scenario>.html` / `<target>.<stamp>.<scenario>.html`). The TranslationQa tool (`Program.cs`) creates the subfolder and emits the new names; `WxServices.Common.JudgePackageStore.Discover` now enumerates subfolders (pairing request+judged within each) so the WxManager review tab reads the new layout. Existing de/es/eo packages were migrated in place. `JudgePackageStore` unit tests updated for the subfolder layout. PATCH: backwards-compatible tooling/management-UI usability change, no recipient-facing or schema change. |
| 1.39.0  | 58b0a25 | 2026-06-29 | WX-219 (WxManager Translation-QA review tab — the operator-facing evaluator for enable-time translation-QA judge packages; WX-137 epic): a new WxManager tab reads the judge packages the TranslationQa tool writes to `C:\HarderWare\translation-qa\` (a `<iso>.<stamp>.request.json` + `.judged.json` pair) and presents, per scenario, the **English reference / target report / independent-model back-translation** side-by-side — the two reports rendered in **WebView2** exactly as a recipient sees them — plus the report-level findings **grouped by location** in a selectable/copyable RichTextBox, and a **vocabulary table** joining each request token with its verdict (derived OK/Warn/Wrong/not-judged/unrepresentable status, suggestions-first sort). Where a verdict suggests a phrase differing from the current target, a **Copy → DB** button applies it to `LanguageTemplates` (human-adjudicated, never auto-applied) behind a `{n}`-placeholder-integrity guard + confirmation. The **judge-package contract** (`JudgeResponse` / `JudgingRequest` / `VocabularyPair` + the shared camelCase JSON options) moved to **WxServices.Common** so the producer tool and this consumer share one definition (consumed later by WX-173); the verdict gains an optional `JudgedBy` source stamp. Bundled (off-scope, pre-existing): a **maximize-over-taskbar** fix — MainWindow (`WindowStyle=None` + `WindowChrome`) now clamps maximized bounds to the monitor work area via `WM_GETMINMAXINFO`, so no maximized view sits behind the taskbar (surfaced by this tab's bottom-anchored findings). New `WxReport.Tests` cover the Common join/discovery; the WPF UI is verified by `docs/test-procedures/WX-219.md` (WxManager is not CI-built). New dependency: `Microsoft.Web.WebView2` (WxManager only). MINOR: new operator-facing capability, backwards-compatible, no schema change. |
| 1.38.3  | b1a6699 | 2026-06-28 | WX-230 (Highest-priority bug): the temperature summary invented a phantom "cooldown later in the week" — a steady ~99°F-every-afternoon forecast read as "highs run 97–100°F … then ease back toward 81–84°F … a modest step down." Root cause: every report's forecast horizon ends on a **partial trailing local day** with only a pre-dawn/morning block, whose max is a pre-dawn temperature, **not** a daytime high; `TemperatureRangeSummarizer.DailyHighsLows` counted it as a daily high, and WX-228's range-splitter then split it off as a real cooldown the reconciler narrated ("clouds build"). Fix (symmetric): a day feeds the **highs** series only if it has its **afternoon (local 12:00–18:00) peak-heating block**, and the **lows** series only if it has a **pre-dawn or morning (local 00:00 or 06:00) block** (the dawn minimum sits on that boundary) — so neither a partial trailing day (no afternoon → bogus low high) nor a partial leading day (today, sent past the morning → no overnight low) can contribute a bogus extreme. A day can yield a high, a low, both, or neither, so the series can differ in length; the trailing pre-dawn-only day still yields a valid low. Surfaced on the 1.38.2 diagnostic; independent of WX-228's contract (its §13 PASS stands). The Extended Forecast grid's display of the partial last day is unchanged (separate, pre-existing concern). PATCH: recipient-visible forecast-accuracy fix, backwards-compatible, no schema change. |
| 1.38.2  | 5f298d4 | 2026-06-28 | WX-228 (editorial clarity, WX-153 epic): the reconciler had only a single-**point** temperature token (`{q:temp:NN}`, canonical °C), so summarizing a week of highs/lows it wrapped a point in a fuzzy band word — "well into the upper `{q:temp:36}` range" — which rendered as the meaningless "the upper 97°F range" (and the same in every language, each independently authored). Root cause: there was no unit-neutral way to say a temperature **span**. Fix: a new `{q:temp_range:lo:hi}` token (two canonical-°C endpoints, rendered as one converted span — "97–99°F" / "36–37°C", `StructuredReportRenderer.FormatTempRangeC`), plus a deterministic pre-call characterization (`TemperatureRangeSummarizer`) that turns the per-day highs/lows into one or two ranges and hands Claude the ready tokens to weave into native closing prose. Algorithm (calibrated with Paul): scan every prefix split, break into an early/later pair only when the two halves' midpoints separate by ≥ 3 °C (≈ 5 °F — the smallest change a reader registers, and where the ±-implied bands stop overlapping), tie-break toward the tightest half; each range is the exact rounded °C min–max, a dead-flat run widened ±1 °C. The per-day high/low uses the same max/min-across-blocks aggregation the Extended Forecast grid renders, so prose and grid agree. The `temp_range` token is a special grammar form, **not** a new `QuantityKind` — no snapshot-schema/SchemaVersion change. Zero added Claude cost (no new LLM call). PATCH: a recipient-visible prose-quality fix (all languages), backwards-compatible; the new token is additive and unread by older data. |
| 1.38.1  | ccb9434 | 2026-06-27 | WX-224 follow-on (meteogram caption legend readability): the HTML caption beneath the meteogram image — the legend that defines the chart's abbreviations in the recipient's language (WX-223 text, WX-224 abbreviation definitions) — was styled smaller and grayer than the report's other small-print captions (`font-size:11px;color:#888`), making it the least readable line in the email. Restyled the caption `<span>` in `ReportWorker.InsertMeteogramImage` to **13px / `#6b8fa8`** italic, matching the "Reported Conditions" station-and-time note (`StructuredReportRenderer.cs:355`) and the forecast-grid "Hours" legend (`StructuredReportRenderer.cs:442`) — the same styling WX-184 (1.36.0) applied to the Hours legend, which had itself once matched this meteogram caption (WX-195). The caption stays **centered** (parent `<p style="text-align:center">` untouched): a legend beneath an image is expected to be centered, unlike the left-justified in-flow captions. All three small-print captions are now consistent in size and color. PATCH: cosmetic styling, recipient-visible (all languages), backwards-compatible; no schema/contract change. |
| 1.38.0  | fcfc02a | 2026-06-26 | WX-224 (Phase B of WX-221 — localize the in-image meteogram labels via per-language render): the meteogram chart's own Wind / RH / temperature labels and day-of-week ticks rendered in English for every recipient (WX-223 localized only the surrounding HTML caption). `meteogram.py` now accepts `--label-wind/-rh/-temp` + `--day-labels` (seven Monday-first weekday abbreviations; English defaults preserve current output, a malformed list degrades to the C locale). `MeteogramWorker` re-keys its render dedup from `(ICAO, TempUnit, Timezone)` to add **Language**, grouping over recipients' resolved languages so only languages in demand render (no blind 5×) — resolving the three new `meteogram_wind` / `meteogram_rh` / `meteogram_temp` tokens from `LanguageTemplates` (fail-soft to the English label on a missing token) and the weekday abbreviations from `CultureInfo` (`CultureName → IsoCode → Invariant` fallback) — and tags PNG filenames + the manifest with the language. `ReportWorker.FindMeteogramAbbrevPath` matches the manifest on language too (backward-compatible: a pre-WX-224 manifest entry with no Language stays language-agnostic across the deploy transition). The migration also relabels the WX-223 `MeteogramCaption` to **define** the abbreviations ("temperature (T), relative humidity (RH)…") consistent with each language's chart token; `SeedTemplateStore.ParseRelabels` generalized to parse the token-named relabel. de/da/eo RH abbreviations (RF/RF/RH) flagged for the WX-214 back-check; "T" is a token seeded "T" for the Latin-5 (non-Latin future-proofing). MINOR: backward-compatible localization; new tokens + per-language render, no schema-shape change. |
| 1.37.1  | 57d17e1 | 2026-06-26 | WX-222 (orphaned LanguageTemplates lifecycle): disabling a language now **purges its report templates** and reverts it to needs-generation, establishing the invariant *a language's templates exist iff it is enabled and generated*. Re-enabling regenerates a complete, **current** set (picking up tokens / baseline changes added since) instead of reusing a stale, now-incomplete one; and disabled languages no longer load or trip the WX-171 startup completeness check (the disabled fr/pt orphans had begun logging recurring `INCOMPLETE` ERRORs after each new token — e.g. WX-223's two). A one-off migration purges the existing orphan templates for all disabled languages and nulls their `GeneratedAtUtc`. Bundled (no runtime effect): a `.coderabbit.yaml` exclusion for golden fixtures + EF Designer/snapshot files (cuts CodeRabbit review surface), and a WORKFLOW §7a clarification (set Test Proc before the push). PATCH: orphan-lifecycle defect fix; no recipient-facing behavior change. |
| 1.37.0  | 6533f0a | 2026-06-26 | WX-223 (Phase A of WX-221 — localize the meteogram caption + alt text): the forecast meteogram's HTML caption ("Forecast of temperature, humidity, and wind over time…") and image alt text rendered in English for every recipient. Added two vocabulary tokens (`MeteogramCaption`, `MeteogramAlt`) seeded for the five enabled languages (en/es/de/da/eo; de/da/eo Claude-generated, flagged for WX-214) and substituted them in `ReportWorker.InsertMeteogramImage` from the recipient's `TemplateSet` (HtmlEncode-guarded against future translations carrying markup). The in-image chart labels (Wind/RH/axes/day-of-week) remain English pending WX-224 (Phase B). Also generalized the test `SeedTemplateStore` to scan every migration, so future vocabulary-insert migrations are picked up by the parity gate without per-migration edits. MINOR: backward-compatible localization; new template tokens, no schema-shape change. |
| 1.36.0  | 9bb4aa5 | 2026-06-26 | WX-184 (report clarity: Current Conditions obs-time + relabel, suppress fallback prose, reposition forecast-grid legend): a four-part editorial pass on the recipient report. (1) The forecast-grid 24-hour-clock legend moved from below the grid to directly beneath the "Forecast for …" heading rule, left-justified to match the Current Conditions station line (supersedes WX-195's centered styling that matched the meteogram caption). (2) The "Current Conditions" heading is relabeled **"Reported Conditions"** — and its per-language equivalents for the five enabled languages (en/es/de/da/eo) via the data-only `RelabelCurrentToReportedConditions` migration, IsoCode-keyed — because an emailed report is never live; the `en` row also seeds future-language generation, so newly-enabled languages inherit the new label. The disabled fr/pt template orphans are left for WX-222. (3) The Current Conditions block now shows the observation's absolute local time beside the station ("at <Station> · <local time>"), so freshness is read at the data rather than inferred from the header — no new vocabulary. (4) The reconciler no longer instructs Claude to narrate a station fallback (the "routine fallback behavior, nothing to be concerned about" prose); the station is already named in the structured display. The now-vestigial `previousMetarIcao` plumbing was removed end-to-end and the operational station-change log retained (wording made truthful). MINOR: new backwards-compatible behavior plus a data-only template migration; no schema-shape change. |
| 1.35.3  | c5c8108 | 2026-06-25 | WX-213 (WxManager Languages detail panel unreadable in dark theme): a WX-172 defect found during WX-212 testing. The Languages tab's status/guidance `DetailPanel` hardcoded a light `Background="#FFF7F7F7"` / `BorderBrush="#FFCCCCCC"`, but WxManager runs a dark theme (`#1a1a1a` window bg, `#c8c8c8` light text), so the inherited light text rendered light-on-light — the header + guidance were set but invisible. Repainted the panel for the dark theme (`Background="#252525"`, `BorderBrush="#404040"`) so the guidance reads. WxManager only (not CI-built). PATCH: UI defect fix, no logic/schema change. |
| 1.35.2  | a1df3ab | 2026-06-25 | WX-212 (auto-refresh the WxManager Languages tab while visible): a WX-172 follow-up UX polish. The Languages tab read generation status only on its `Loaded` event / after a WxManager action, so a language sitting at `[Generating…]` did not update while the operator stayed on the tab — it refreshed only on tab re-entry (the default WPF `TabControl` unloads/reloads tab content). `LanguagesTab` now runs a `DispatcherTimer` (10s) that re-reads the registry **only while the tab is visible**: the default WPF `TabControl` unloads a tab's content on switch-away and reloads it on return, so `Loaded` (reload + start the timer) and `Unloaded` (stop it) bracket visibility — no background DB query while the operator is on another tab, and a return reloads by itself. A language flips `[Generating…] -> [Ready]/[Blocked]` in place. `ReloadAsync` is now single-flight with coalescing (a tick can't race an explicit reload on `_all`), and `ApplyFilterAndBind` preserves the current AllList/Supported selection **by ISO code** across the rebind so the auto-refresh never deselects the row the operator is reading (the detail panel updates live instead). WxManager only (not CI-built). PATCH: UX refinement, no schema/contract change, backwards-compatible. |
| 1.35.1  | e4ffb5e | 2026-06-25 | WX-211 (run template generation off the report send-path; fix generation cost instrumentation): a WX-172 follow-up. The generation-on-enable pass was running at the **top** of `RunCycleAsync` (a WX-172 CodeRabbit fix had moved it ahead of the no-recipient/no-locality early returns so a recipient-less system still generates), so a pending-language generation — a multi-second Claude call up to a 32K-token output — **delayed that cycle's report sends** (observed live generating Esperanto). The send phase is now extracted into `RunReportSendsAsync`, whose "no recipients / no locality members" early-exits skip only the sends; `RunCycleAsync` runs the sends **first**, then generation, then counts the cycle — so reports are never delayed by a translation, generation still runs on a recipient-less cycle, and it stays capped at one Claude call per cycle (this also restores the "generation runs after the sends" behavior the 1.35.0 changelog already described). Plus two instrumentation fixes: a generation call now increments `wxreport.claude.calls.total` (it previously bumped only the token counters, so generations were invisible on the "Claude API Calls" panel) and the input/output token-counter descriptions note generation; and the per-call DEBUG line reads "Claude tokens" instead of the misleading "Claude reconciliation tokens" for a `translate:<iso>` generation. PATCH: behavior refinement + observability, no new feature, backwards-compatible. |
| 1.35.0  | cc1c92a | 2026-06-25 | WX-172 (generation-on-enable for localized templates): enabling a supported language no longer requires pre-existing templates — it marks the language **PENDING** and WxReport.Svc, on its next cycle, calls Claude to translate the baseline (en) report vocabulary into that language via one batched `translate_templates` tool-use call (`TemplateTranslator` / `TranslatorPrompts` / `ClaudeClient.InvokeTranslationAsync`, the POST/retry/parse loop refactored into a shared `SendForToolUseAsync`). Each token carries its English phrase + a context sentence (or English hint) and gets a **representability self-check**: a token that can't be filled by simple substitution (word order / agreement / a restructuring the atomic-token renderer can't do) is flagged not-representable with a note rather than invent a broken literal. The return is validated **fail-closed** (exact baseline token set, non-empty representable phrases, `{n}` placeholders preserved as a multiset, column-length bounds, all-control-char rejection on both the representable and blocked paths, a note required on every blocked token), with the same retry-with-feedback loop the reconciler uses. Outcomes persist as **READY** (assignable), **BLOCKED** (≥1 non-representable token — needs a renderer/code change, **not** auto-retried), or **FAILED** (transient — retried next cycle), encoded in `Language.GeneratedAtUtc` / `GenerationError` (new computed `GenerationState` / `IsReady`). Recipients may only be assigned a **READY** language (Recipients-tab dropdown gated on `IsReady`); the Languages tab shows each supported language's status + operator guidance, enabling a BLOCKED/FAILED language requeues it, and the enable gate no longer demands complete templates (supersedes WX-171's). Generation runs **at most once per cycle, after the sends** (bounds latency/cost; token usage recorded on the Claude cost counters), in its own DbContext. The `BackfillSeededLanguageReady` migration stamps the seeded en/es READY so they are not re-translated. New `TemplateTranslatorTests` + `LanguageStateTests`. MINOR: new operator-facing capability, backwards-compatible. |
| 1.34.4  | b4f01fa | 2026-06-24 | WX-210 (METAR insert — dedup within-response duplicates + correction overwrite): a 12 h log review found ~56 ERROR/12 h, all KJXI. `MetarFetcher.FetchUrlAndInsertAsync` deduped parsed observations only against rows **already in the DB**, never within one fetch response, so when AWC re-serves the same line (KJXI comes back byte-for-byte ~3×/cycle — verified live: a 12 h feed is **102 lines / 34 distinct / 0 COR / 0 SPECI**; a normal station like KIAH shows no duplication) both copies passed the not-in-DB check, both were `Add`-ed, and `SaveChanges` violated `UX_Metars_Station_Time_Type` — and because the batch is one transaction, that one duplicate **rolled back every co-batched station's inserts**. The insert is now **reconciled by correction rank**: new pure **`CollapseByKey`** collapses same-key duplicates within a response to a single survivor (a `COR` beats a non-`COR` regardless of feed order; two differing CORs keep one + WARN), and new pure **`Reconcile`** decides insert/overwrite/skip against the stored row — a new observation **inserts**, a later-arriving `COR` **overwrites the stored uncorrected row in place** (same primary key + unique key; child sky/weather/RVR rows replaced; scalars copied via `SetValues`), a stored `COR` is **never** clobbered by a later non-`COR`, and an identical re-arrival is skipped. The stored-COR invariant is guarded twice — at the planning layer and, defense-in-depth, at the point of mutation in `ApplyPlan`. A row-level `DbUpdateException` now retries inserts one-per-context (`InsertPerRow`) so a single bad row can't discard the co-batched rows. This also closes a **latent data-integrity gap**: before, a correction arriving a cycle after the original (its key already in the DB) was silently dropped, leaving the report on uncorrected values. New `MetarFetcherDedupTests` — 9 pure decision tests + 4 SQLite-backed tests exercising the in-place overwrite, the guard, and the per-row recovery (adds a `Microsoft.EntityFrameworkCore.Sqlite` **test-only** dependency; the harness remaps the model's `nvarchar(max)` columns to SQLite TEXT so `EnsureCreated` succeeds — the unique index + FK cascades the tests rely on are preserved). No schema change (the unique index and `IsCorrection` column already exist). PATCH: bug fix + latent-correctness fix, no recipient-visible feature, backwards-compatible. |
| 1.34.3  | 2993353 | 2026-06-19 | WX-207 (tier-'Safety' over-escalation — detector tier-axis sibling of WX-204) + WX-208 (test-procedure convention docs). Surfaced by the WX-204/205/206 verify run on 1.34.1: `DeterministicChangeDetector.PrecipTier` tiered a precip change **Safety** when EITHER the final OR the prior block carried a safety-grade signal (severe / freezing / snow / ≥34 kt wind) — but applied the **prior**-block check for *every* direction, so an **Appearing/Strengthening** change inherited Safety from a prior hazard the final no longer carries (a block going Snow→Rain: the appearing rain is not itself a safety event). `ValidateChangeSnapshotConsistency` correctly requires the **final** block to carry the signal for an Appearing/Strengthening Safety change, so it rejected the over-tiered change → `ChangeConsistencyException` → report degrade ("tier 'Safety' (Rain) but no final_snapshot block … carries a safety-grade signal … the tier is over-escalated"); it recurred **every cycle** on a Snow→Rain locality (Watonga, OK), WX-205 degrading it immediately. **WX-207:** `PrecipTier` now inherits Safety from the prior block **only for Weakening/Clearing** (a removed hazard is safety news, and the validator doesn't require final backing there); for Appearing/Strengthening, only the final block's hazard qualifies — matching the validator, restoring the tautology on the tier axis. Regression test added. **WX-208 (process):** documented the Test Proc convention in WORKFLOW.md §7a (procedure-doc path, or literal `N/A` when unit tests suffice; bundled-PR tickets share one procedure named with all keys); renamed the WX-204/205/206 shared artifacts to `WX-204-205-206.md` / `-verify.sh` and fixed that script's exercised-marker (`reconciled` success-line → `Claude reconciliation tokens`, which fires on degrade too — it was under-counting). PATCH: bug fix + process docs/tooling, backwards-compatible. |
| 1.34.2  | cc85e39 | 2026-06-19 | WX-203 (es localized time at sentence end rendered a double period). The es time designator ends in a period (`9:00 p. m.`); when Claude's es prose ends a sentence right after a `{q:time}` token, substitution produced `…tras 9:00 p. m..` — the sentence period colliding with the abbreviation's own period (surfaced in WX-171's CodeRabbit review; the golden corpus was reworded to avoid baking it, but the production behavior remained for real es recipients). `ReportTokens.Substitute` — the single chokepoint every prose render flows through — now collapses a doubled period to one after token expansion, via a source-generated `(?<!\.)\.\.(?!\.)` regex that matches only an **isolated pair**, so an intentional ellipsis (`...`) is left intact. Spanish typography treats the abbreviation's trailing period as also closing the sentence, so one period is correct. Language-general (en's `9:00 PM` has no trailing period, so it never fires there); applied at the chokepoint so both the closing and the change band get it. PATCH: cosmetic es-typography fix, recipient-visible (es only), backwards-compatible. |
| 1.34.1  | 36c9264 | 2026-06-19 | WX-204 / WX-205 / WX-206 (reconciliation degrade + wasted-Claude-call fixes; cost epic WX-122). A post-1.34.0 log analysis found recurring reconciliation **degrades** dominated by a phantom "Strengthening": since WX-189 `changes[]` is computed deterministically *after* the Claude call, but `DeterministicChangeDetector` classifies **per block** (and groups consecutive blocks into one window) while `ValidateChangeSnapshotConsistency` checked the **window aggregate** — so a real interior-block rise (block-0 expectation Likely→Certain, block-6 severe False→True) whose window max/severe looked flat was false-rejected as "not a real change," degrading the report and wasting **3 Claude calls** per cycle (the retries can't fix a change Claude didn't author). **WX-204:** the consistency check now backs a change **per block** (mirroring the detector) so the defense-in-depth net is tautological again while still rejecting a true phantom (no block moved); a regression test reproduces the prod case. **WX-205:** a `ChangeConsistencyException` (a computed-change fault) now degrades on attempt 1 — no 2nd/3rd Claude call — cutting per-cycle cost from 3 calls to 1 on such faults. **WX-206:** the closing-claim guard's message now names the exact offending sentence so the retry feedback pins it (and WX-204 fixes the wholesale closing degrades, which were the same detector bug re-failing the section-degrade re-validation). None of this was introduced by 1.34.0 — it is WX-189/148/151 machinery, untouched by WX-171. PATCH: bug fix + LLM-call efficiency, no recipient-visible feature, backwards-compatible. |
| 1.34.0  | f628308 | 2026-06-19 | WX-171 (atomic-token vocabulary reshape — the renderer reads localized phrases from the DB, no longer glues vocabulary): `StructuredReportRenderer` now takes a `(TemplateSet, CultureInfo)` resolved per language from the new `LanguageTemplates` table and looks up every grammar-sensitive combination as a single **atomic token** via `Tok.*` (e.g. `rain_freezing`, `sev_storms_likely`, `sky_overcast_low`), interpolating only runtime data into translated format strings — it never concatenates two vocabulary items. The hard-coded `ReportVocabulary` is **deleted**; the DB is the sole source of phrases. This fixes the latent Spanish word-order/agreement defects as a side effect (**WX-174** "Helada lluvia" → "Lluvia helada", plus `Bajo nublado`→`Nublado bajo`, `Ligera lluvia`→`Lluvia ligera`, and singular-noun agreement `lluvia probables`→`lluvia probable`); English is byte-identical bar two intended rewordings (`NoObservationNote`, and `Showers rain`→`Rain showers`). **Fail-closed posture** (defense in depth, never silent): a build-time `Tok`↔seed parity gate, a startup completeness check that ERRORs (WxMonitor-alerted) on any incomplete language, and a per-recipient send gate that fails *that* recipient closed (others still send) when its language is incomplete — no silent English substitution. The template cache **reloads once per report cycle** (picking up a newly-enabled language or an edited phrase without a restart; the event-driven trigger is deferred to WX-179). `SupportedLanguages` now gates language-enable on a **DB template-existence check** (coverage of the `en` baseline) instead of a hard-coded `{en, es}` constant, so the enable gate and the renderer read one source of truth. A 93-case golden no-regression corpus locks the rendered output; the changed fixtures equal the intended-delta inventory in `docs/test-procedures/WX-171.md` exactly. MINOR: new DB-backed localization capability + WX-174 Spanish-grammar fix, recipient-visible (Spanish only), backwards-compatible. |
| 1.33.1  | 037982c | 2026-06-17 | WX-195 (today's Conditions cell dropped already-elapsed dayparts): the WX-190 clock-band grid led **today's** row with bands already in the past — at 10:53 AM CDT it showed `00-12 — Rain likely`, naming the elapsed 00-06 and most of 06-12. `AggregateDays` now builds the Conditions `Bands` from **non-elapsed** blocks only (`SevereBlocks.NotFullyElapsed`), so today leads with the current daypart; the day's **High/Low and peak wind still span the whole calendar day** (computed from all blocks), preserving the WX-176/WX-188 invariant. The coverage rule is now symmetric — the first day-row may start later than `00` (elapsed dayparts dropped) just as the final may end before `24` (horizon). Also folds two verify-script fixes (same artifact): the `WX-190-verify.sh` run-log now reports the true cycle count as `exercised` with the ≥3-cycle gate as a `precondition` flag (was the contradictory `exercised=0 … 1 so far`), and `GRID_ES` reverts to the ASCII tail `stico para ` because `sqlcmd` output mangles the accented heading. Also restyles the 24-hour-clock legend to match the meteogram caption (centered, italic, 11px, `#888`) so the report's two explanatory captions read as one voice. PATCH: bug fix, recipient-visible, backwards-compatible. |
| 1.33.0  | 41dfa12 | 2026-06-17 | WX-190 (daypart clarity — the 00-06 block now binds to its calendar day): the forecast grid's Conditions cell is re-architected from precipitation-episode lines to a full-day **clock-band tiling**. Each local day is partitioned into its four native 6-hour blocks, labeled on a 24-hour clock (`00-06` / `06-12` / `12-18` / `18-24`); adjacent bands sharing a condition merge, and a uniform day collapses to a single `00-24` line — so the cell always covers the whole day with no gaps. A legend beneath the grid keys the clock (`00 = midnight, 12 = noon, 24 = midnight`), and the contradictory `12 AM` / `12 PM` are never emitted. Severe bands are emphasized and bound to their calendar day by the row date plus clock range. In prose with no grid row to anchor the day (the degraded hazard banner and the WX-189 deterministic fallback band), the 00-06 pre-dawn block reads `{weekday} 00-06` instead of a floating `overnight` / `{weekday} night` that a US reader hears as the *following* day; morning/afternoon/evening keep idiomatic wording. The reconciler prompt now instructs Claude to bind the pre-dawn block to its day and never use a bare `overnight`. MINOR: recipient-visible grid/format change, backwards-compatible. |
| 1.32.1  | b92eebb | 2026-06-16 | WX-193 (diagnostic "What's changed" band suppression — fixes a WX-189 regression): a diagnostic (deploy-verification) report surfaced a "What's changed" band for ordinary non-severe changes. WX-189 made the change set deterministic, so the band now renders from the computed `changes[]` via the fallback even when Claude wrote no `changeSummary` — but the WX-178 strip that gates the band was `Scheduled`-only and the diagnostic path (`ReportWorker.SendStartupReportAsync`) never stripped at all. Fix: extend `SuppressesScheduledChangeBand` to accept `ReportKind.Diagnostic` (its first-send, near-term severe-onset, and bandless short-circuits are already kind-agnostic) and apply the same strip in `SendStartupReportAsync` before rendering, mirroring the scheduled path. A diagnostic now carries the band **only** for a newly-appearing near-term (local Day 1-3) severe onset, identical to a scheduled report; severe still shows in the grid, subject, and closing. PATCH: bug fix, no recipient-visible feature, backwards-compatible. |
| 1.32.0  | b6ad3cf | 2026-06-16 | WX-189 (deterministic change detection — compute the "What's changed" set in code; leave Claude to select + narrate): the reconciler asked Claude to author the structured `changes[]` (phenomenon, direction, window, tier) and the WX-148/149/151 validators recomputed the real prior→new diff to **reject** phantoms — but the invention itself persisted (~33 degrade events / 2 days, the WX-165 measure), the validators only catching it downstream. This moves change **detection** out of the LLM entirely: new **`DeterministicChangeDetector`** computes `changes[]` from (prior committed snapshot, reconciled `final_snapshot`) **after** the call — the generative inverse of `ForecastReconciler.ValidateChangeSnapshotConsistency` — so a structural phantom is impossible by construction. **Coverage:** precip ×5 + standalone severe by inverting the oracle; temperature + wind via the WX-160 significance-gate thresholds (freeze/thaw at 32 °F, heat-advisory, wind-advisory, per-tier magnitude), so the band itemizes the same class of change the gate wakes Claude for; obscuration excluded (the GFS provisional never populates it, always `None`); WindShift the sole residual (→ WX-191). **Pass structure (Option C):** Claude returns only `final_snapshot` + narrative prose; `changes[]` is removed from the `submit_reconciled_report` tool schema and the guidance text's change-authoring + anchor rules deleted (its significance-hierarchy / directional-asymmetry / anti-reversal framing kept verbatim). `{chN}` anchoring is retired — `StructuredReportBody.Validate` now forbids anchors in **both** prose sections, `ValidateAnchoredProseTiming` is deleted, and the consistency validator runs only as tautological defense-in-depth on the computed set. **Band presence** stays deterministic by report kind (unscheduled always; scheduled none except the WX-178 near-term severe onset); when a band must show but Claude's `changeSummary` prose is rejected or absent, `StructuredReportRenderer` renders a terse, **day-anchored** localized fallback line from the computed changes ("Severe storms — Saturday afternoon." / "Tormentas severas — …"), so a prose fault costs Claude's flavor, not the report's information. **Prose-retry pin + independent-section degrade:** a prose fault now throws a typed `NarrativeProseException` (carrying its section) and the replayed feedback tells Claude to keep `final_snapshot` byte-identical and re-author **only** the prose; on retry exhaustion the fault drops **only its own section** (the `changeSummary` → the deterministic band fallback, the `closing` → a short safe localized line) and the report still sends — so a closing fault no longer takes the band down with it, and a wholesale narrative degrade happens only when `final_snapshot` itself is unusable. The precip-vs-dry guard (`ValidateClosingClaims`) now also covers the `changeSummary` band. **Temperature payoff:** `ReconcilerTemperature` raised 0.25 → 0.5 — structural correctness no longer rides on sampling, so the prose breathes (final value calibrated post-deploy, §13). New `DeterministicChangeDetectorTests` (per phenomenon × direction, severe de-dup, temperature/wind thresholds, windowing, ranking, and the **keystone tautology invariant** — every emitted change passes its inverse validator) + `ChangeConsistencyValidatorTests` (the validator's reject paths preserved as direct unit tests); the obsolete Claude-authored-change pipeline tests retired. No persisted-schema change: the `submit_reconciled_report` **tool input** schema dropped `changes[]`, but the persisted `StructuredReportBody` / `ForecastSnapshotBody` shapes are unchanged, so `SchemaVersion` stays v5 (older rows still load). MINOR: change-detection behavior change (band content now deterministic), backwards-compatible. |
| 1.31.1  | 14ffc45 | 2026-06-15 | WX-165 (reconciler invention reduction — root-cause pass): the reconciler intermittently authored phantom "What's changed" items the snapshot data did not support (rain "appearing" where rain dropped out; severe "appearing/strengthening" on an already-severe window; a time-of-day word contradicting the change's own window). The WX-148/149/151 validators correctly caught and degraded them, but the invention itself persisted at ~33 degrade events / 2 days — and on the **diagnostic/startup** path a degrade is a hard abort, so a deploy verification was suppressed entirely (observed 2026-06-15: the 1.31.0 startup diagnostic degraded three times on three *different* invented changes and never sent, though the deploy was healthy). Three coordinated generation-side fixes: **(1)** the reconciliation call now pins `temperature = 0.25` (`ClaudeClient.InvokeReconciliationAsync`) — it had run at the Anthropic default (1.0), which maximized variance and let each retry roll a fresh phantom; **(2)** the `Diagnostic` report kind now receives the WX-178 near-term-severe-onset band rule (`ForecastReconciler.BuildReconcilerSystemPrompt`) — it had fallen through to an EMPTY instruction, the one report kind never told "an empty changes array is the correct answer"; **(3)** a change-consistency rejection now throws a typed `ChangeConsistencyException` carrying the offending change token, so the replayed retry feedback **names** that change and instructs correct-or-remove ("do not replace it with a different change") instead of the generic line — a low-temperature retry then converges rather than inventing afresh. The five change-anchored validator throws became `ChangeConsistencyException` (a `JsonException` subtype), so the retry-then-degrade path and exhaustion messages are unchanged; only the retry feedback differs. No schema change. Durable follow-up: WX-189 (deterministic change *detection*) moves change-finding out of the LLM entirely. PATCH: bug fix, no recipient-visible feature, backwards-compatible. |
| 1.31.0  | 9b3387d | 2026-06-15 | WX-178 (scheduled reports suppress the "What's changed" band except for near-term severe-onset): a Scheduled Report could carry a "What's changed" band for an ordinary, non-severe change — e.g. a rain→thunderstorm upgrade (send 2285) — making it read like an unscheduled update (the WX-153 founding defect). The band now rides a scheduled report **only** when a NEW severe hazard appears in the near term: a block going `severeFlag` false→true versus the prior committed snapshot, within **local Day 1-3** (today through the day after tomorrow). **Deterministic guarantee:** new pure `ReportWorker.SuppressesScheduledChangeBand` (reusing `ForecastSnapshotBody.HasSevereEscalationOver` with a new local-Day-4 UTC-cutoff overload) strips the band via `WithoutChangeBand` on the scheduled send path when there is no near-term severe onset. It is a **post-reconcile** strip, so the LLM always runs and any severe still surfaces in the per-day grid, the WX-156 subject, and the closing. **Prompt alignment:** the reconciler's scheduled-report instruction now tells Claude to emit a band only for a near-term severe onset (empty `changes` + null `changeSummary` otherwise), minimizing wasted generation; the deterministic strip backstops it. The Day 1-3 horizon is deliberately distinct from `DegradeHazardHorizon` (~36 h, which governs sending a narrative-less hazard alert now). Unscheduled updates unchanged. No schema change. MINOR: recipient-visible behavior feature, backwards-compatible. |
| 1.30.1  | 138ab00 | 2026-06-15 | WX-188 (Extended Forecast grid renders a calendar day already wholly past at send time — "leads with yesterday"): a report built after local midnight on a GFS **00Z** run led its forecast grid with the prior local day — send 2270, built 01:01 CDT, opened with "Sun Jun 14" while its header and prose correctly anchored on Jun 15. `StructuredReportRenderer.AggregateDays` bucketed every snapshot block by local calendar day with **no trim relative to send time**, and a 00Z-run snapshot begins at the day-part block in progress at run time — in an offset timezone the *previous evening* — so a wholly-elapsed prior day surfaced as a forecast row. **Fix:** thread the send instant (`nowUtc`) into `Render` / `RenderDegraded` → `AppendExtendedForecast` → `AggregateDays`; **drop a local day only when every one of its 6-hour blocks has fully elapsed** (`StartUtc + 6h ≤ nowUtc`), reusing the `SevereBlocks` active-window kernel (factored out as `NotFullyElapsed`) so the grid and the hazard banner agree on what "past" means. **Day-level only:** a retained day — always including the current day — keeps **all** its blocks, so its High/Low/wind still span the whole calendar day (a 1 PM report still reports this morning's low as today's low); the period over which a day's extremes are computed is deliberately unchanged. Deterministic renderer change — no Claude call, no schema change. A separate partial-day understatement (a current day summarized from only its surviving blocks, e.g. 2270's Jun 14 evening-only 85°F vs the GFS daily 91°F) is out of scope and tracked under WX-176. PATCH: bug fix. |
| 1.30.0  | 42b1419 | 2026-06-14 | WX-182 (degrade circuit-breaker — stop re-reconciling a degraded identical input without dropping guaranteed scheduled reports): completes the WX-180 cost-incident arc. WX-180 fixed the *trigger* (folded gust clamped); this closes the *amplifier* — a degraded **scheduled** slot never advances its last-sent and the scheduled path bypasses the input pre-filter, so before this it re-paid a full (deterministically failing) Claude reconcile every cycle. **Scope:** the unscheduled path already pre-filter-skips an identical degraded input, so this is a **scheduled-path circuit breaker**. New nullable `LocalityState.LastDegradedInputHash` (one light migration) records the input of the most recent **degrade-to-no-send**; set on that branch, **skip the Claude reconcile** when the current input equals it (strict equality — load-bearing, keeps it orthogonal to the WX-157 quiet window: never fires on an advanced input). On a due scheduled/first slot with that identical input, **send-from-cached-snapshot** (WX-157's on-time principle): re-issue the locality's last good report — cached `ForecastSnapshot` + its narrative with the "What's changed" band stripped (`WithoutChangeBand`) + **live** current conditions — rendered as a `Scheduled` report with **no Claude call**, delivered to served members. No usable cached snapshot (e.g. a first-ever-degrade locality) → skip and self-heal when the input next changes. The cached send deliberately **does not clear** the breaker (`AdvanceBaselineAfterSendAsync(clearDegraded:false)`), so successive scheduled slots on the same stuck input all serve from cache at **zero Claude cost**; a genuine Claude-reconciled send (normal or degraded-hazard) clears it. Severe is unaffected: the WX-148 degrade-hazard path still sends and clears, and the cached path itself re-checks — if a severe block fixed in the cached forecast has crossed into the hazard horizon as the clock advanced on the stuck input, it does **not** serve a routine cached report but falls through to a normal reconcile so the degrade-hazard banner is delivered (safety over cost). New `wxreport.degrade_breaker.total` counter (outcome=cached_send/skip). Schema-shape change (one column) → `SchemaVersion` unchanged (the snapshot/report bodies are untouched). MINOR: new cost-control behavior, backwards-compatible. |
| 1.29.0  | c8fefed | 2026-06-14 | WX-157 (send-timing: scheduled report takes precedence — day-banded pre-scheduled quiet window + severe-onset punch-through + late-scheduled deferral fix): builds the **Y-side** WX-181 deferred and unifies both send-timing rate-limits under one shared, **minutes-encoded** day-banded schedule. **(1) Late-scheduled fix:** `ReportWorker.ShouldSend` evaluated the `MinGapMinutes` gap *before* the scheduled trigger, so an unscheduled send shortly before a scheduled slot deferred the scheduled report (a "7 AM report" could arrive up to 90 min late). The scheduled block now precedes — and is exempt from — the gap, so a due slot fires on time; the gap still rate-limits the unscheduled/arrival path. **(2) Pre-scheduled quiet window:** new `Report.PreScheduledQuietSchedule` (`"day:minutes,…"`, default `"1:90,3:180"`) holds back a significant, non-severe-onset unscheduled update when the next scheduled slot falls within the change's day-band quiet window (near-term change → 90 min before the slot, day-3+ → 180 min) — its content rides the upcoming scheduled report (which renders "What's changed"). New pure `ShouldQuietBeforeSlot`, evaluated at the WX-114 significance-gate stage **before the Claude call** (admission control — a quieted update spends no tokens), honoring the gate's enforce/shadow mode; new `wxreport.suppressed.quietwindow.total` counter. **(3) Severe punch-through:** a not-severe→severe onset (`gate.SevereEntered`, the same signal WX-181 uses) punches through the quiet window and sends immediately. **(4) Shared schedule, minutes:** `UpdateDebounceScheduleFormat` → neutral **`DayBandedSchedule`** with `Step(FromDay, MinMinutes)` / `MinMinutesForDay`, shared by both config keys; WX-181's `UpdateDebounceSchedule` default normalized `"1:6,3:12"` → **`"1:360,3:720"`** — *identical durations* (360 min = 6 h, 720 min = 12 h), zero behavior change, so WX-181's verify fingerprints are intact. **(5) `MinGapMinutes` retained** and re-documented as the hard floor of last resort: it still binds severe-onset punch-throughs (the only limiter inside 90 min) and the gate-Off / schedule-parse-failure cases (both day-banded windows live inside the gate stage). Folded in as documented out-of-scope (see WX-181): WX-181's `UpdateDebounceSchedule` minutes normalization (config default, debounce reason string `…h`→`… min`, verify-script header comment, and the debounce/format unit tests — all behavior-preserving). No schema change. MINOR: new recipient-affecting send-timing feature, backwards-compatible. |
| 1.28.0  | 4d6b622 | 2026-06-13 | WX-181 (send-timing rate-limits made time-block- and severity-aware): unscheduled updates now debounce on a **day-banded schedule**, a deterministic suppressor evaluated at the WX-114 significance-gate stage **before the Claude call**, so a debounced update spends no tokens. New `Report.UpdateDebounceSchedule` (`"day:hours,…"`, default `"1:6,3:12"`: a change reaching recipient-local day 1–2 needs a 6 h gap since the last unscheduled send, day 3+ needs 12 h; parsed strict/fail-closed — first day must be 1, days ascending). A **not-severe→severe onset punches through**; the 90-min `MinGapMinutes` stays a hard floor beneath it. `SignificanceGate` now also surfaces the earliest-changed recipient-local day + a `SevereEntered` flag (its existing `severe-add`); the debounce keys on `LastUnscheduledSentUtc`, never the Claude-input hash (the WX-180 prong-B lesson — that hash advances on not-news/degrade and would drop guaranteed sends). Folded in as documented out-of-scope (see WX-181): a shared **`SevereBlocks`** helper extracted from the duplicated degrade-path / hazard-banner severe scans, and the **WX-180 post-deploy verify artifact** (`docs/test-procedures/WX-180-verify.sh` + `WX-180.md`) the cost hotfix had merged without. MINOR: new feature, backwards-compatible. |
| 1.27.1  | 283f1a5 | 2026-06-13 | WX-180 (cost incident — reconciler retry-storm, ~$45/day): a gusty forecast made the windKt-sustained check (WX-160) reject Claude's folded gust on every attempt, so the locality never converged, **degraded each cycle**, and — because a degraded *scheduled* report never advances its slot's last-sent and scheduled reports bypass the input pre-filter — re-reconciled every ~5 min, burning Claude calls and sending nothing (Watonga: 34 of 38 degradations in a 6 h window; output tokens ~60% of cost). **Prong A (convergence):** `ValidateWindKtSustained` → **`NormalizeWindKtSustained`** — a folded gust above the per-block GFS+TAF sustained ceiling (raised, for the obs block, by the observation's *sustained* wind) is now **clamped to that ceiling in place** rather than rejected → retried → degraded; the ceiling IS the correct sustained `windKt.max`, and the gust still belongs in the `{q:gust}` narrative token. Reverses WX-160's *reject* response while preserving its sustained-only invariant. A degrade circuit-breaker ("prong B" — skip re-reconciling identical input) was prototyped but **reverted before merge**: the broad form could suppress a *guaranteed* scheduled report when the prior Claude call was not-news/degrade with no new input since; it is being re-scoped with precise degraded-input tracking. No schema change. PATCH: bug fix |
| 1.27.0  | dfcce2a | 2026-06-12 | WX-166 (recipient language as a constrained selection — Languages registry + supported-language gate + WxManager Languages tab): `Recipient.Language` was a free-text string ("English"/"Spanish"), so a typo silently flowed into report generation and, post-WX-129, into the per-locality language set. **New `Languages` table** (identity PK, unique `IsoCode`, `DisplayName`, `IsEnabled`) seeded with the full **ISO 639-1** list ("AllLanguages", 184 rows) of which only `en` + `es` are enabled ("SupportedLanguages" — the languages the renderer has templates for, gated by the new **`WxServices.Common.SupportedLanguages`**; WX-167 replaces the constant with a DB-template check). `Recipient.Language` (string) → **`Recipient.LanguageId` FK** (nullable; null = service default), with a data migration backfilling `English→en`/`Spanish→es` and dropping the old column (other/blank values fall to NULL → service default). **Principle established and enforced:** the ISO code is the canonical internal language identity — every lookup, narrative/vocabulary selection, the `html lang` attribute, and the subject key on it; `DisplayName` is for human display only. The WxReport read-path was rewired to it: a `langById` dict resolves each recipient's FK once per cycle (the codebase's dict-lookup convention, no EF navigation load), and the renderer + envelope/subject helpers take the resolved **code** rather than re-deriving it from a display name via `LanguageHelper.ToIetfTag` (which now survives only to convert the one configured default *name* → code) — closing a latent split-source-of-truth where a body could render in one language while `html lang`/subject silently fell back to English. **WxManager** gains a **Languages tab** (two-list shuttle: enabling is blocked when no report templates exist; disabling is blocked while any recipient is still assigned the language), and the recipient editor's Language field becomes a dropdown over enabled languages — with a "(service default)" sentinel mapping to a null `LanguageId` — writing the FK; the Announcement tab resolves each recipient's language through the registry. The management UI itself stays English-only by design (localization targets recipient reports, not the UI). **Folded in (documented off-scope fix):** the migration also carries a cosmetic `ForecastSnapshots.SchemaVersion` default 4→5 that had drifted out of WX-155 without its own migration (the column is always written explicitly from `SchemaVersionCurrent`, never the default). MINOR: new feature, backwards-compatible |
| 1.26.0  | dde83a0 | 2026-06-11 | WX-160 (make the WX-114 significance gate TAF-aware on sustained wind — kill the `taf-fresh` blanket bypass): the gate's deterministic provisional body is GFS-only, so to avoid suppressing a real short-term TAF update WX-114 (1.13.2) had it presume **any** fresh/amended TAF significant (`taf-fresh`) and defer to Claude. But TAFs reissue routinely, so that bypass barely filtered: one day's enforce data (2026-06-11) showed **81%** of the cycles the gate admitted to Claude were `taf-fresh`, and **69%** of all Claude calls returned "not news" — and the discretionary sends it let through were TAF-driven micro-updates (a paul_en scheduled report's 23 mph "today" wind became 21 mph in an unscheduled update 110 min later, a 2 kt **gust** tick: stored snapshots 3165 `windKt.max 20` → 3175 `windKt.max 18`, both the gust, sustained ~12 kt unchanged). Root cause beneath the bypass: `windKt` is documented as **sustained** wind with no gust field, yet Claude folds the TAF gust into `windKt.max`, so the gate's `windKt.max` comparison was GFS-sustained-current vs gust-laden-prior — apples-to-oranges, masked only because `taf-fresh` skipped exactly the TAF cycles. **Fix (gate threshold logic byte-for-byte unchanged — only its inputs made honest):** **(1)** new **`TafBlockProjector`** deterministically overlays the parsed TAF (`snapshot.ForecastPeriods`) onto the GFS provisional — TAF prevails within its validity window (matching the reconciler, hence the stored baseline's provenance), max-across-overlapping-groups per Paul's forecaster mapping (TEMPO/PROB count toward the block's worst case, BECMG prevails from its ValidFrom), temperature/sky stay GFS — and the gate now evaluates that **GFS+TAF merged** body, so the `taf-fresh` early-return and its `freshTafSinceLastSend` parameter are **removed**. A fresh TAF that moves no sustained band, adds no precip, and crosses no advisory is suppressed deterministically; only a real change reaches Claude. **(2)** Wind in every gate decision is **sustained only** (forecaster rule); a gust gates solely through severity — a wind ≥ 50 kt, sustained **or** gust, is severe by definition (`max(sustained,gust) ≥ 50` → the existing `severeFlag` criterion, OR-ed with GFS's CAPE-driven flag). **(3)** new fail-closed **`ForecastReconciler.ValidateWindKtSustained`** pins Claude's `windKt.max` to the maximum sustained wind any source (GFS provisional / TAF periods / observation) forecasts for the block, plus a 4 kt rounding tolerance — a folded gust overshoots and is rejected and replayed with feedback (the WX-148 retry-with-feedback path; on exhaustion the existing tier-aware degrade still sends hazards), so the stored baseline the gate compares against is itself sustained, gust kept to the narrative `{q:gust}` token. Reconciler prompt + tool-schema gain the two wind rules. Straight to Enforce (gate skips log the trigger + that the TAF-merged forecast showed no material change). New tests: `TafBlockProjectorTests` (gust-only nudge suppressed, sustained band crossing fires, new precip fires, gust≥50 severe with windKt staying sustained, TEMPO max, TAF-dry-prevails, far-block-keeps-GFS) and reconciler windKt-validator tests (folded gust → retry → corrected, persistent fold never a clean Success, rounding tolerance accepted). The TAF→block projection is **clamped to the TAF's validity end** (`TafValidToUtc`) so an expired TAF's last group never overrides the live GFS forecast for far blocks (a TAF is ~24-30h; the gate horizon is 120h) — without the clamp a genuine GFS precip/wind escalation past TAF validity would be silently suppressed; and the windKt validator reads its sustained ceiling straight off `TafBlockProjector.Merge` (not a re-derived per-period overlap), so the validator's TAF-coverage model is identical to the gate's by construction. **Recipient-visible (Option A):** `SevereSubjectPrefix` (WX-156) now treats `SevereFlag` as the authoritative severe signal — a block carrying it gets the severe subject prefix — instead of re-deriving from `windKt ≥ 50 ∥ precip ≥ Likely`; WX-160's 50-kt *gust* rule made the old premise ("sub-50 sustained ⇒ CAPE-driven") false (a dry gust-severe block was left unmarked while the body banner showed it), and this re-aligns the subject with the body banner (which already keys on `SevereFlag`). It also prefixes the CAPE-severe-with-only-Possible-precip case WX-156 had excluded — accepted, since a flagged-severe block is severe. **Constant hygiene:** the fixed deterministic bright lines the forecast logic keys off — `SevereWindKt` (50), `SafetyWindKt` (34), `FreezeDegF` (32), the windKt `SustainedCeilingToleranceKt` (4), and the gate's `TierUpperBoundHours` (an immutable `ImmutableArray`) — are consolidated into a single new **`WxServices.Common.WxThresholds`**, eliminating the prior `SevereWindKt`-in-`GfsSnapshotBuilder`-mirrored-into-config duplication; the gate's *tunable* knobs (per-tier delta arrays, advisory lines, mode) stay on `SignificanceGateConfig` (bound from `Report:SignificanceGate`). No schema change. MINOR: new feature + defect fix, backwards-compatible |
| 1.25.0  | 2a11e42 | 2026-06-10 | WX-159 (surface OTel exporter failures in the service log): on 2026-06-10 a reboot left the Docker observability stack down for ~8 h while the services ran and delivered reports normally — but the OTLP exporter **fails silently** when the collector is unreachable, so there was **zero log signal**; we found out only from an empty dashboard. A telemetry pipeline that can't report its own breakage is a latent hazard. **`WxServices.Common/OtelExportDiagnostics`** (an `EventListener`) now subscribes to the OTel SDK's internal `OpenTelemetry-*` `EventSource`s at `Warning` and routes Warning/Error to log4net at the SDK event's own level, so an unreachable collector — which the exporter emits at **Error** — appears in the service log within ~10 s **and crosses WxMonitor's ERROR+ alert threshold**, so it emails (rather than dying in the log, the trap a WARN would have fallen into). Exporter-source events (which repeat every 10 s export interval ≈ 8,600/day while down) are **throttled per distinct event** (source+id+name) to onset + a 5-min heartbeat via the new `LogThrottle` — a silent blackout becomes a heartbeat, not a flood, while a genuinely different exporter failure isn't masked by an ongoing one. Email cadence is bounded by WxMonitor's per-service `AlertCooldownMinutes` (≈ 2 emails/hour during a sustained outage, not hundreds). **`WxServices.Common/WxTelemetry.AddWxTelemetry`** consolidates the telemetry block that was copy-pasted across all four services (the `Telemetry:Enabled` gate, the OTLP HTTP exporter to `:4318/v1/metrics` on a 10 s interval, and the new listener); each service supplies its own meter(s)/view(s) via a callback, so WxReport/WxParser/WxVis keep their histogram views. The OTLP exporter + hosting package refs **moved to Common** (services keep `OpenTelemetry` core for their view lambdas); `Microsoft.Extensions.Configuration.Binder` bumped 8.0.0→8.0.2 in `MetarParser.Data`/`WxManager` to satisfy the exporter's transitive floor. New tests: `LogThrottle` (incl. an 8-hour-outage replay proving ~1 log/window, not per-attempt) and the listener's source/format helpers. **Detection half** of the 2026-06-10 incident; the *prevention* half (boot-start the Docker stack without an interactive login) is a separate infra ticket. Standalone High-priority Story, not under WX-68/WX-89. MINOR: new behavior, backwards-compatible |
| 1.24.0  | 9112e8c | 2026-06-10 | WX-156 (severe-weather subject prefix — deterministic, no LLM in the decision): the subject signalled provenance (scheduled/unscheduled) but said nothing about hazard — severe weather lived only in the body's red hazard banner. A front-loaded severe noun is now prepended to the subject when a forecast block carrying `SevereFlag` falls in the next 24 h and meets the rule: **wind branch** (`maxWind ≥ 50 kt`, the shared `GfsSnapshotBuilder.SevereWindKt`) qualifies standalone; **convective branch** (sub-50 kt, so `SevereFlag` can only be CAPE-driven since blocks carry no CAPE) additionally requires `PrecipExpectation ≥ Likely` (storms expected, not mere instability). No persistence requirement (severe episodes are often ≤ 6 h = one block). Snow/freezing/fog/34–49 kt wind are excluded — `SevereFlag` already excludes them. Subject grammar becomes `[hazard noun] — [Type] — [Locality] (time)`, prefix front-loaded for mobile truncation; the noun comes from `ReportVocabulary.SevereNoun` — the **same source as the body hazard banner** (lifted out of `StructuredReportRenderer` to the vocab), so subject and body never name the hazard differently ("Severe storms"/"Severe weather"; "Tormentas severas"/"Tiempo severo"). The earliest qualifying block supplies the noun, matching the banner's choice. Covers the **degraded safety send** (WX-148), re-arming the subject-level urgency WX-154 stripped, and the startup diagnostic send. **Hysteresis is inherited implicitly (Option 1):** the prefix is a pure function of the reconciled snapshot — the every-cycle whipsaw vector (unscheduled sends) is already gated by `EvaluateUnscheduledSuppression` (a severe de-escalation on an observation-only advance doesn't send), and computing the prefix from the same snapshot the body renders keeps subject↔body coherent (the WX-153 anti-drift goal). New `SevereSubjectPrefix` helper + tests (wind-only, convective-Likely, convective-Possible-no-prefix, offset-locality, earliest-block noun, out-of-window, Spanish). WX-153 standing constraint satisfied: deterministic on existing snapshot data — no added Claude calls. (Third WX-153 editorial-clarity child; was blocked by WX-154.) MINOR: new recipient-visible feature, backwards-compatible |
| 1.23.0  | 4a3a0a2 | 2026-06-10 | WX-154 (report-type coherence — single-source report kind): the WX-155 "7 a.m." defect's deeper cause was that a report's *type* was signalled by three independent, drift-prone switches — the subject word (`ChangeSeverity`), the header note (`isUnscheduled`), and the "What's changed" band (narrative presence) — so a scheduled report carrying a change band read as an unscheduled update. **`ChangeSeverity` is replaced by `ReportKind` { Scheduled, Unscheduled, Diagnostic }** — the single source of truth for both the subject word and the rendered header label, so they cannot disagree (the welded `Alert` value is dropped; severe-weather subject signalling becomes WX-156's separate hazard axis). Every report now carries an explicit, always-shown header label — "Scheduled Report" / "Unscheduled Update" / "Diagnostic" (En; "Reporte programado" / "Actualización no programada" / "Diagnóstico" Es) — and the subject word derives from the same `ReportKind` ("Weather Report" / "Weather Update", title-cased to match). The degraded safety send (WX-148) is now typed Unscheduled (its subject-level urgency returns with WX-156's severe prefix). The "What's changed" band stays orthogonal (it renders whenever the narrative carries a change summary), but a scheduled report bearing one no longer masquerades as an update — the type label states what it is. **Folded in:** a no-jargon prose rule — internal data-source / aviation acronyms (TAF/METAR/GFS/CAPE/ICAO) must never reach the reader (paul_en send 2050 closed with *"the afternoon TAF carries a 30% chance"*); the reconciler prompt now forbids them (use "the latest indications", never "the latest forecast" or "guidance"), backed by a deterministic whole-word leak-detector in `CheckProse` (fail-closed → retry → degrade, the WX-149 pattern; case-insensitive for the homograph-free acronyms — TAF/METAR/GFS/ICAO catch lower-case leaks too — but CAPE is matched case-sensitively so "cape" the landform doesn't false-positive). Render/labeling + prompt only — **no schema change, no SchemaVersion bump**. (Second WX-153 editorial-clarity child; blocks WX-156.) MINOR: recipient-visible labelling + subject change, backwards-compatible |
| 1.22.4  | 128ccab | 2026-06-10 | WX-155 (wrong day-part words in offset timezones): the GFS provisional snapshot bucketed hours into 6-hour blocks aligned to the **UTC** grid (00/06/12/18Z), so for any offset locality a block **straddled two local day-parts** — `12Z–18Z` is 7:00 AM–1:00 PM in CDT (five hours of morning, one of afternoon). Claude, though given the timezone and told to phrase day-parts in local time, still labeled such a block "afternoon", and the WX-149/151 prose validators (deliberately overlap-tolerant) accepted it because the window's tail touches afternoon — paul_en diagnostic send 2050 read *"a likely thunderstorm during the afternoon hours around 7:00 AM–1:00 PM"*. Fixed at the source rather than by tightening prose validation: **`GfsSnapshotBuilder.Build` now takes the locality `TimeZoneInfo`** and buckets by **local** day-part (00–05 overnight / 06–11 morning / 12–17 afternoon / 18–23 evening), so each block **is** exactly one local day-part and the straddle is impossible; `ForecastSnapshotBlock.StartUtc` is now a timezone-dependent UTC instant on a local boundary (`Build` requires the tz; passing `TimeZoneInfo.Utc` reproduces the pre-WX-155 grid). `ForecastSnapshotBody` schema **v4→v5** (block-boundary semantics changed); the tool-schema `@const` follows the constant and the reconciler prompt's input/window descriptions + examples were updated to "aligned to the locality's local day-parts". **`ForecastReconciler.IsBlockAligned`** now validates change-window endpoints in **local** time, and a pre-v5 prior snapshot (UTC-aligned, incomparable to local-aligned blocks) is **dropped for one cycle as a baseline reset** so the deploy doesn't fire a spurious "everything changed" blast. Bucketing is DST-safe: a boundary that lands in a spring-forward gap (zones whose transition is on a boundary, e.g. America/Santiago) is stepped forward an hour rather than throwing, and a block's hour count varies ±1 across a transition. Day-grid (`AppendExtendedForecast`) and hazard-banner (`PartOf`) consumers already localize `StartUtc`, so they only get cleaner. New `GfsSnapshotBuilder` tests: offset-tz bucketing (the CDT repro), UTC-default backward-compat, and DST-boundary invariance. (First WX-153 editorial-clarity child; supersedes the lighter prose-validator-tightening alternative.) PATCH: defect fix |
| 1.22.3  | a2f8c86 | 2026-06-10 | WX-152 (closing-claim validation): the **closing** ("In summary:") paragraph is the one narrative section nothing reconciled against the snapshot — `ValidateChangeSnapshotConsistency` works on `changes[]`, and the WX-149/151 `{q:time}` / `{chN}`-anchored prose checks cover only the `changeSummary` band — so the closing could assert a precipitation/storm **event** the `final_snapshot` doesn't carry. paul_en diagnostic send 1995 closed with *"Tonight carries a modest chance of a storm"* while the only precip in the whole 6-day snapshot was a Tue **afternoon** thunderstorm; Tue evening/overnight onward was dry. (Distinct from WX-139, which bans synoptic **mechanisms/fronts** the schema can't represent — this is an **event** the schema can carry and the blocks contradict.) New **`ForecastReconciler.ValidateClosingClaims`**: per closing sentence, if it ASSERTS a precip/storm phenomenon (not negated) at a resolvable local time (`tonight`/`today`/`tomorrow`/this-`morning`/`afternoon`/`evening`, resolved against the first block's local day) whose snapshot blocks are **all dry**, reject (fail-closed → retry → tier-aware degrade). Conservative by design: negated ("stays dry", "no rain expected"), un-timed ("any storm that develops"), weekday-pinned ("Saturday afternoon"), and beyond-horizon references are skipped and lean on the prompt rule — precip-vs-dry only (a wrong-phenomenon claim at a wet time isn't caught). Reconciler prompt gains a closing rule (summarize only what the blocks carry, at the time they place it; "stays dry/quiet" always fine). KDWH replay fixtures re-recorded under the updated prompt. Validation-only on the existing live artifact — **no schema change, no SchemaVersion bump**. (Sibling of WX-148/149/151, adjacent to WX-139, from the WX-130 seam.) PATCH: defect fix, no behaviour change for well-formed output |
| 1.22.2  | 8d9bf17 | 2026-06-09 | WX-151 (prior-aware change verification): the reconciler narrated **fabricated** "What's changed" changes with no prior→new basis — paul_en send 1977 announced a storm "downgrade" and rain "appearing" over a cycle whose only real movement was sky-cover wobble plus a 2 kt wind nudge (the prior committed snapshot was identical to the new one on every precip block). WX-148 backed only the NEW snapshot for appearing/strengthening (and exempted weakening/clearing); WX-149's prose check needed a `{q:time}` token — neither held the **prior**, so a change identical in prior and new sailed through. **`ForecastReconciler.ValidateChangeSnapshotConsistency`** now takes the prior snapshot and rejects any change that is not a real prior→new difference of its claimed direction within its window: appearing/strengthening requires the in-window expectation to EXCEED the prior's (or `severeFlag` to rise false→true), weakening/clearing requires it to FALL (or severe to drop) — generalizing and **subsuming** WX-148's new-only backing. `severeFlag` is a first-class strength axis, so a flat-expectation severe escalation (the WX-148 worked example) still counts; the standalone `Severe` phenomenon compares `severeFlag` prior→new. Covers precipitation phenomena + severe (scope call); wind/windshift/temperature/fog stay prompt-governed (documented residual). A new **`ValidateAnchoredProseTiming`** extends WX-149's day-part check from `{q:time}` tokens to `{chN}`-anchored sentences — a change's narrated day-part words must agree with its window's local buckets — conservatively: it rejects only when a sentence's day-part words ALL fall outside the window, and skips words pinned to another day ("Friday evening"). Reconciler prompt gains: never narrate a change unsupported by a prior→new difference; sky-cover wobble and sub-band wind drift are not precipitation news. The KDWH replay fixtures were re-recorded under the updated prompt (the prior storm fixture's `ch2` prose said "midday-to-evening" over a morning/early-afternoon window — the very narrative/window inconsistency the new check rejects; the fresh model output times it correctly). Validation-only on the existing live artifact — **no schema change, no SchemaVersion bump**. (Sibling of WX-148/WX-149 from the WX-130 locality-batching seam.) PATCH: defect fix, no behaviour change for well-formed output |
| 1.22.1  | f3c9fe1 | 2026-06-09 | WX-149 (reconciler prose-hygiene defects): three reader-facing faults the post-WX-130 survey found shipping in the `structured_report`, all fixed on WX-148's validator scaffold (prompt self-check + deterministic fail-closed assertion + retry-with-feedback → tier-aware degrade). **(a) Phantom / mis-typed phenomenon** (send 1938, Watonga: a change typed `thunderstorm` over a day whose only precip block is `rain`) — already rejected by WX-148's phenomenon-backing match; WX-149 pins it with a regression test. **(b) Tier over-escalation** (send 1938: "possible rain" emitted at the `safety` tier): `ForecastReconciler.ValidateChangeSnapshotConsistency` now rejects a `safety`-tier change unless a block in its window carries a safety-grade signal — `severeFlag`, freezing precip or snow, or sustained wind ≥ 34 kt. The backing set is deliberately wider than `severeFlag` alone because `severeFlag` is narrow (severe convection or wind ≥ 50 kt per `GfsSnapshotBuilder.DeriveSevereFlag`, missing dense fog / freezing precip / 34–49 kt winds the significance hierarchy still calls safety-critical); phenomena the snapshot can't encode (fog/haze/smoke/dust — no block field — and temperature — no threshold) are exempt and lean on the prompt rule. **(c) Raw internal UTC block-notation leak** (send 1938: "the Wednesday afternoon block (12-18Z)…") and **prose time-word ↔ `{q:time}` disagreement** (send 1927, Spring: "develop Saturday afternoon, {q:time:…T12:00:00Z}" where the token renders to 7:00 AM local): a new `ValidateProseHygiene` pass over the narrative prose strips `{…}` tokens then rejects any `NNZ` block shorthand, and for each `{q:time}` token buckets its local hour into a day part (mirroring `StructuredReportRenderer.PartOf`) and rejects only an **unambiguous** contradicting day-part word in the same sentence — genuinely ambiguous words (English "tonight"; Spanish "mañana"/"tarde"/"noche") and Spanish prose broadly fall to the prompt rule (documented residual). The reconciler prompt gains matching rules (phenomenon/tier must be block-backed; never emit raw UTC in prose; prose time words must match the token's local rendering). Validation-only on the existing live artifact — **no schema change, no SchemaVersion bump**, no new plumbing (the feedback/degrade enablers come from WX-148). PATCH: defect fix, no behaviour change for well-formed output |
| 1.22.0  | 94c937f | 2026-06-09 | WX-148 (reconcile the change narrative with the day-grid; tier-aware degrade): the WX-130 split left two semi-independent views of one forecast — Claude's change narrative (`changes[].window`) and the deterministically-rendered day-grid (`final_snapshot` blocks) — with nothing reconciling them, which shipped self-contradictions ("a chance of showers this afternoon" in the narrative vs "morning rain possible" in the grid, same day). Four-part fix. **Grid (`StructuredReportRenderer`)** now splits each day into **episodes** — maximal runs of consecutive same-phenomenon blocks — instead of collapsing to the single highest-expectation one, so a day with morning rain *and* afternoon storms surfaces both; the Conditions cell renders one labeled line per episode (`Afternoon — rain likely`), a range label when an episode spans buckets (`Overnight–morning`), and a single timed sentence on severe days (`Severe storms likely in the afternoon, then rain in the evening`); capped at two episodes, a third logged not silently dropped. **Validator (`ForecastReconciler.ValidateChangeSnapshotConsistency`)** fails closed when a change window is off the 6-hour block grid (forbidding sub-block precision — the root of the 6/9 17–21Z contradiction) or when appearing/strengthening precip is not backed by a `final_snapshot` block carrying it in-window. **Retry-with-feedback (`ClaudeClient`)** replays each rejected attempt as `tool_use` + `tool_result(is_error)` so a validation retry corrects the specific fault rather than blindly resampling (the cached system + first-user prompt prefix is preserved; only the appended corrective turns are uncached, only on a failed cycle). **Tier-aware degrade** (new `ReconcileResult.Degraded`): when reconciliation exhausts its retries but the snapshot itself parsed cleanly, a **safety-critical** forecast (a severe block in the next ~36 h) still sends a narrative-less hazard report — a deterministic banner (`Severe storms in your forecast — Saturday afternoon.`) plus current conditions + grid, the summary omitted and **not** apologized for — rather than withhold the hazard; non-safety-critical cycles self-heal next cycle. The per-recipient send loop is refactored into a shared `DeliverWeatherReportAsync` used by the normal and degraded paths. Reconciler guidance + tool `window` schema now require block-aligned windows. Render/validation only — **no schema change, no SchemaVersion bump**. (Sibling WX-149 — prose-hygiene validator assertions — builds on this scaffold.) MINOR: visible grid-format change + new validation/degrade behavior, backwards-compatible |
| 1.21.0  | 1ba576f | 2026-06-08 | WX-130 (per-locality batching — banks the WX-123 cost lever): the expensive Claude reconciliation now runs **once per locality**, then a deterministic renderer fans it out per recipient with **no further LLM call**, so cost scales with *locations*, not *people*. `ForecastReconciler.ReconcileAsync` becomes **recipient-agnostic** (drops language / recipientName / units / isFirstReport / scheduledHour), and `ReconcileResult.Success` **drops `EmailBody`** — the HTML body is no longer a Claude artifact. `StructuredReportBody` is the **live, fatal** rendering source again (best-effort during the WX-144 additive window): `Deserialize` + the per-call narrative-contract validation route through the existing retry → skip/Failure path, and the WX-120 content-less fall-safe is carried forward as a per-language narrative degeneracy guard (`DegenerateNarrativeException`: skip-with-trace on an `allowSkip` cycle, Failure on a guaranteed send). The narrative slims to the **two judgment sections** — `changeSummary` + `closing` (Current Conditions table and per-day grid are rebuilt deterministically), so `ForecastSnapshotBody`/`StructuredReportBody` schema move in lockstep to **v4**. `ReportWorker` is rewritten outer-locality / inner-recipient: one snapshot per locality (locality stations + **centroid** GFS point), the send decision + WX-80/108/114 gates keyed on a new **`LocalityState`** (per-locality baseline + cadence, replacing per-recipient `RecipientState`, now unused and dropped in a later cleanup), one reconciliation (narrative carries the locality's distinct recipient languages), then a per-recipient render+send loop writing one `CommittedSend` each (sharing the one reconciled snapshot; `ReasoningTrace`/`StructuredReport` copied per row for self-contained audit). A recipient **with no locality receives no report and a logged ERROR** (firm rule — WxMonitor surfaces it). First contact is a standalone **welcome-only email** (`StructuredReportRenderer.RenderWelcome`) — a greeting + what-to-expect (daily reports for the locality at its scheduled times, in the recipient's language), **no weather**; a never-contacted member is onboarded promptly with no Claude call and no disturbance to other members (welcomes don't advance the locality's baseline/cadence), so the prior per-recipient onboarding latency is restored. The WX-129 weather renderer also regains the **station-attribution subtitle** ("at &lt;city&gt;, &lt;airport&gt;" when the observing station differs from the locality). The **startup report** is rewired to a single-recipient **diagnostic** send (first valid recipient only, `CommittedSend.IsDiagnostic = true`) so its snapshot is excluded from the baseline and can't seed a later unscheduled update. Schema: new **`LocalityStates`** table + **`CommittedSends.IsDiagnostic`** column (one additive `AddLocalityStateAndDiagnosticFlag` migration, startup auto-apply); `CommittedSend` row shape v2→3. KDWH replay fixtures re-recorded per their README (material prompt change). MINOR: new feature + additive schema, backwards-compatible |
| 1.20.1  | dd13c43 | 2026-06-08 | WX-144 (availability fix): a validation failure on the **dormant** WX-128 `structured_report` no longer aborts the live `email_body` send. WX-128 added the structured report as additive/unread, but its `StructuredReportBody.Deserialize` + per-call narrative-contract validation ran inside `ForecastReconciler`'s fail-closed retry path — so Claude writing visibility/cloud-height as bare unit-bound prose in `narrative.currentConditions` (`'10 miles'`, `'1,700 feet'` — quantities with no `{q:}` token kind) failed the whole reconciliation and skipped real sends (`niki_en`, `kayla_en`, …, self-healing only next cycle). Now the structured-report extraction is non-fatal: on any failure (missing field, schema/token validation, or the narrative contract) the reconciler logs a WARN, returns `Success` with a **null** structured report (`ReconcileResult.Success.StructuredReport` is now nullable; `ReportWorker` persists null) and does not waste retries on an otherwise-good `email_body`. `email_body`/`final_snapshot` stay fail-closed — they are the live artifacts. Interim for the WX-130 build window: WX-130 re-couples the structured report (fatal again) once it's the live rendering source and drops the `currentConditions`/`extendedForecast` sections that trip this. PATCH: defect fix, no schema change |
| 1.20.0  | 68f5025 | 2026-06-08 | WX-129 (deterministic renderer): new `StructuredReportRenderer` (WxReport.Svc) turns the WX-128 unit-neutral `StructuredReportBody` into each recipient's HTML body with **no LLM call** — the cheap inner step of WX-123's one-call-per-locality lever. Keeps the established layout (data in tables, judgment in prose, decided 2026-06-08): the Current Conditions **table** is rebuilt deterministically from the shared observation; the Extended Forecast **grid** (one row per local calendar day, aggregating the reconciled snapshot's 6-hour blocks à la `SignificanceGate`) carries a deterministic Conditions phrase composed from sky + precip + severe; the change-summary band and closing are the language-keyed narrative prose with `{q:...}` tokens substituted into the recipient's units (incl. `PrecipUnit`, WX-142), locale, and language. New `ReportTokens.Substitute` walks the validator's own grammar so renderer and validator can't drift; new `ReportVocabulary` holds the en/es deterministic chrome (section/row labels, sky/weather/visibility/conditions words) — the WX-137 template seed, gated by `SupportedCodes` (es is an AI-authored draft, dormant until WX-130, review optional per WX-137). Qualitative visibility bands; language-localized date/month names with US/period-decimal numbers (`es-US`) until WX-138 wires `NumberFormat`. Built + unit-tested (8 renderer tests, imperial vs metric + per-day grid + en/es + no-obs); **not yet wired into the send path** — `email_body` stays the sent artifact until WX-130. MINOR: additive renderer, dormant, backwards-compatible |
| 1.19.0  | d3dc50a | 2026-06-08 | WX-142 (recipient `PrecipUnit` + `NumberFormat` columns — precursor to the WX-129 deterministic renderer): add two `Recipient` columns in one `AddRecipientPrecipUnitAndNumberFormat` migration (light table, startup auto-apply). **`PrecipUnit`** (`"in"` / `"mm"`, default `"in"`; EF's scaffolded `defaultValue: ""` hand-edited to `"in"` so pre-existing US recipients backfill to inches — the WX-133 Timezone precedent) is consumed by WX-129 to convert the structured report's canonical millimetre precipitation (`{q:precip_mm:…}`) per recipient — an explicit field, not derived from `TempUnit` (Fork-2 decision). **`NumberFormat`** (nullable .NET culture name, e.g. `"es-US"`) is added now but deliberately **unread**: WX-129 formats all recipients with US / period-decimal conventions, and WX-138 later wires this preference into the renderer (decoupling number format from language). Folding it into this migration avoids a second `Recipients` table rewrite. WxManager's Recipients editor gains a Precipitation (in / mm) ComboBox mirroring the existing unit controls; `NumberFormat` has no UI yet (WX-138). On edit the existing entity is loaded and only named fields are mutated, so the un-surfaced `NumberFormat` rides through `SaveChanges` untouched. MINOR: additive schema + WxManager UI, backwards-compatible |
| 1.18.0  | c2e6fa7 | 2026-06-08 | WX-140 (AWC silent bbox truncation): METAR/TAF fetching stops sharing the CONUS-sized `Fetch:Region*` bounds with the GFS fetch — the AWC data API **silently truncates** a continent-sized bbox at roughly 550–600 reports (verified live: ~560 of 2,000+ CONUS stations; our 525 distinct stations were the cap's fingerprint), so stations for any locality outside the arbitrary surviving subset were never ingested. Verlen (Watonga, OK) received GFS-only reports — no KJWG/KEND METARs, no KEND TAF — since being added; WxReport's "no station within 30 mi" fallback searched a table the truncation had starved. Fix: **(1)** observation fetching keeps the configured wide region (the WxVis **synoptic analysis map is METAR-driven over the full CONUS extent** — review caught that shrinking obs geometry to locality neighborhoods would have starved it) and defeats the cap with **adaptive splitting**: any bbox response reaching the cap threshold splits into quadrants and refetches recursively, self-tiling the region to AWC's cap and restoring full-station coverage the single capped query never had; per-locality boxes (± new `Fetch:LocalityBoxDegrees`, default 2°, `ObsFetchPlanner`, pure + unit-tested) are appended for small-region configs, and new `Fetch:ObsRegion*` keys can decouple obs geometry from the GFS `Fetch:Region*` when needed; **(2)** new **by-station TAF direct fetch** (`TafFetcher.FetchAndInsertByStationAsync`) for every TAF station referenced by a locality or recipient — the rescue path TAFs never had (METARs had `AlwaysFetchDirect`); **(3)** a **station gap fill with evidence-gated self-tuning promotion** (`StationGapFiller`, shared with WxManager's WX-141 save flow): every defined station within the fallback radius (now the shared `StationCoverage.MaxFallbackDistanceKm`, referenced by both the fetch layer and `WxInterpreter` so the radii cannot drift) of a locality centroid or locality-less recipient — plus every locality/recipient-named station regardless of distance (Watonga's KEND sits ~68 km from its centroid) — is freshness-checked; gaps are direct-fetched in batched `ids=` calls over the same 3-hour window that defines a gap (review caught a 1h-fetch/3h-test mismatch that made slow-cadence stations permanent phantom gaps); promotion to `AlwaysFetchDirect` requires the returned observation to predate the bbox pass (review caught a top-of-hour race that would promote bbox-reliable stations forever), and silent airfields back off hours between retries instead of refetching every cycle; **(4)** a **truncation tripwire** WARNs when any METAR/TAF bbox response reaches ~500 reports, so this failure class is loud next time; **(5)** inline cleanup: repeated identical METAR parse failures (the every-cycle `MYGF` missing-date/time WARN-spam) now WARN once per station+error and DEBUG thereafter. Interim production mitigation predated the fix (KJWG/KEND/KCLK/KPWA flagged `AlwaysFetchDirect` via WxManager Look Up, 2026-06-08 ~01:40). MINOR: fetch-architecture change + new config key + new TAF capability, backwards-compatible |
| 1.17.0  | acbe8bb | 2026-06-07 | WX-128 (unit-neutral structured forecast output — the prompt-engineering heart of the WX-123 locality-batching story): the Claude reconciliation now emits a **fourth artifact**, `structured_report` (`StructuredReportBody`, schema at `docs/structured-report-schema.md`): a language-free, salience-ranked `changes` array (tier / phenomenon / direction / UTC window / canonical-unit quantities / `chN` anchor — `wind_shift`+`shifting` cover the WX-111 vector case) plus a `narrative` map keyed by ISO 639-1 code, one entry per language among the recipients served (Spanish is live in production — multi-language was promoted from non-goal to in-scope at grooming when the recipient table contradicted the ticket), whose prose carries quantities only as `{q:...}` substitution tokens (canonical units: °C / kt / hPa / mm; `{q:time:...}` for instants; prompt forbids unit-bound idioms like "low 90s"). The WX-129 deterministic renderer will turn this into each recipient's report — units, locale, language — with **no further LLM call**, the lever that makes one-Claude-call-per-locality viable (WX-122/123). **Additive transition**: `email_body` is still emitted and sent; the structured report is persisted to the new nullable `CommittedSends.StructuredReport` column (light `AddCommittedSendStructuredReport` migration, startup auto-apply) but unread until WX-129/130. Validation fails closed through the existing retry-then-skip/Failure path: intrinsic invariants in `StructuredReportBody.Validate` (token grammar via `ReportTokens`; every change narrated in every language's `changeSummary`, no dangling anchors, anchors only there) plus per-call contracts in `ForecastReconciler` (every requested language present; WX-120-style per-language visible-length floor). Schema versions move in **lockstep**: `ForecastSnapshotBody` 2→3 (shape unchanged; the envelope gained a sibling) and `StructuredReportBody` born at 3; `CommittedSend` row shape v1→2; reconciliation `max_tokens` 16384→32768 for the per-language narratives. KDWH replay fixtures re-recorded per their README (material prompt change). MINOR: new feature, additive schema, backwards-compatible |
| 1.16.0  | 2efab06 | 2026-06-06 | WX-134 (WxManager fixes from first field use — supersedes the unreleased 1.15.1): **(1) Zombie-preview race fix**: `LocalityCombo_SelectionChanged` awaits a DB lookup before applying the preview + field lock; a keystroke can clear a text-search prefix auto-selection while the lookup runs, and the preview then resumed as a zombie — locking the four mirrored fields with a stale locality's values under unrelated combo text, which a later Save would have persisted onto the recipient. The preview now bails unless `LocalityCombo.SelectedItem` is still the exact item it started with. **(2) Tab-return reload** (ratified design): switching back to the Recipients tab reloads the displayed recipient from the database (and the Localities tab likewise reloads its list + loaded locality with members/unassigned pool), deliberately discarding unsaved edits — changes made on the other tab are now immediately visible, ending the stale-pane confusion where a member edited elsewhere still displayed its old lock state. **(3) Locality Delete redesigned**: the Delete button now always raises the confirmation popup, naming the consequence when members exist ("its N member(s) will be unassigned and keep their current settings"); Yes unassigns all members and deletes the locality in one atomic SaveChanges (the FK's `ON DELETE RESTRICT` is satisfied deliberately) — replacing the blocked-while-members-exist banner, which read as if Delete acted on a member. **(4) Click-path diagnostics**: the Members Add/Remove handlers log on entry and banner+log on every guard bail — a field debugging session was needlessly mysterious because a click could die invisibly. **(5) Save-button dirty-tracking across all tabs** (ratified addition): Save Configuration, both Save buttons, and Announcement's Send are disabled until something has changed and is eligible (a shared `DirtyTracking` helper wires change events; programmatic loads are suppressed so only user edits count), and return to disabled after a successful save/send — Announcement's Send re-enables on a failed send since the text is retained for retry. **(6) Unassignment clears the locality label** (ratified field finding): every unassign path (Localities-tab Remove, locality deletion, Recipients-tab combo-clear) keeps the member's mirrored stations/timezone/hours but blanks `LocalityName` — a lingering label read as still-assigned during the smoke test. Also ratifies the Done-regression rule into WORKFLOW.md §12. MINOR: behavior changes + defect fix, UI-only, no schema change |
| 1.15.0  | 7356c6e | 2026-06-06 | WX-127 (Localities management UI + locality write-path coherence): new WxManager **Localities tab** — master-detail like Recipients: shared fields (Name, METAR/TAF, Timezone, Scheduled Hours; read-only centroid) with Save propagating edits to every member via `SyncMembers`, a Members section whose Add/Remove act immediately (assign/unassign + `LocalityCentroid.Recompute`, centroid visibly updates), and UI-blocked deletion while members exist (the `ON DELETE RESTRICT` rule as words). The Recipients tab's free-text locality label becomes a single editable **Locality ComboBox** doing membership + label duty: select an existing locality to assign on Save, **type a new name to create one** (confirmation-gated; seeded from the founding member's stations/timezone/hours, centroid at their coordinates — then re-rendered through the normal load path so the mirrored fields lock automatically), clear to unassign (member keeps last-synced values). For members the four mirrored fields are **read-only** on the Recipients tab with a "managed by the locality" tooltip — the locality is the only write path; deleting a member also recomputes its locality's centroid. Two coherence fixes ride along: `RecipientResolver` now **skips station write-back for locality members** (station responsibility belongs to the Locality; previously a stationless locality's members would have been independently re-resolved onto different stations), and the comma-separated-hours format contract is extracted to a single shared **`ScheduledSendHoursFormat`** (lenient `Parse` for the WxReport.Svc runtime — behavior unchanged; strict `TryValidate` for both WxManager tabs), collapsing what were three independent copies. MINOR: new UI feature + additive `RecipientConfig.LocalityId`, backwards-compatible, no schema change |
| 1.14.0  | 8391083 | 2026-06-06 | WX-133 (shared-schedule invariant): report scheduling becomes a **locality** property. The per-locality Claude reconciliation (WX-130, upcoming) judges "what changed and does it matter?" against the previous **locality** snapshot, and that shared baseline is only coherent when every member's reports go out at the same instants — a member on its own schedule would receive deltas judged against reports it never got (or would force a per-member Claude call, defeating the locality-batching lever). The shared schedule is the **(timezone, hours)** pair, so both move: new `Localities.Timezone` (required, default `UTC` — a locality spans exactly one timezone, its members being co-located; review caught that mirroring hours alone leaves identical hour strings firing at different UTC instants when a member's timezone diverges) and nullable `Localities.ScheduledSendHours` (same comma-separated local-hours shape and null-falls-back-to-service-default semantics as `Recipients.ScheduledSendHours`), added by the `AddLocalityScheduleAndTimezone` migration (light, startup auto-apply; apply/revert cycle verified against local WeatherData; scaffolded backfill default hand-edited `""`→`"UTC"`). `LocalityAssignment` now mirrors timezone + hours **verbatim** alongside display name and stations on both `Assign` and `SyncMembers` (the WX-125 propagation pattern — one private mirror method, so assignment-overwrite and locality-edit-repropagation stay in lockstep). Locality-less recipients keep their per-recipient timezone and hours: the runtime reader (`ReportWorker`'s `recipient.ScheduledSendHours ?? default`) is deliberately untouched; the due-now check moves to the locality in WX-130. MINOR: additive schema + propagation behaviour, backwards-compatible |
| 1.13.3  | 16957fe | 2026-06-05 | WX-120 (content-less report guard): a recipient received a WxReport email with **no forecast content** — Claude called `submit_reconciled_report` when its own reasoning concluded `skip_send`, supplying only an HTML comment (`<!-- placeholder: skip_send was the correct tool... -->`) as the `email_body`. `ForecastReconciler` validated only that the field was a non-empty string (`RequireString`), so the placeholder sailed through to a send (3 occurrences over 2026-06-02..05; a recurring model tool-misfire that **predates** WX-114, not a regression from it). Fix: a deterministic guard computes the `email_body`'s **visible-text length** (`VisibleTextLength` — strips HTML comments + tags, decodes entities, collapses whitespace) and, below a hardcoded floor (`MinVisibleBodyChars = 200`; real reports ~1,700 visible chars, a degenerate body ~0), throws `DegenerateEmailBodyException` so it routes through the existing WX-110 retry. On exhaustion it falls **safe**: `NotNews` (skip, keeping Claude's reasoning trace) on an `allowSkip` cycle — matching Claude's own intent — or `Failure` (self-heal) on a guaranteed send. PATCH: defect fix, no schema change |
| 1.13.2  | 66a823b | 2026-06-04 | WX-114 (TAF-blindness fix): the deterministic significance gate evaluates a **GFS-only** provisional body, but its prior baseline is the last *sent* (Claude-reconciled, TAF+METAR-aware) snapshot — so a new or amended **TAF with flat GFS** produced a byte-identical provisional and was wrongly suppressed, dropping real short-term updates (first enforce-mode data: 38/132 suppressions had fresh guidance, the TAF cluster being the concern). `SignificanceGate.Evaluate` gains a `freshTafSinceLastSend` parameter and returns significant up front (the `taf-fresh` criterion) when set; `ReportWorker` derives it from `ChangedSourcesSince(LastSentInputHash).Contains(Taf)` — the same signal `freshGuidanceSinceLastSend` already used for the WX-108 backstop. GFS/METAR-driven cycles still gate normally (the 71%-of-suppressions cost win is preserved); the audit SQL lives at `Code/tools/wx114-audit/`. PATCH: corrects a suppression defect against WX-114's own "no loss of material updates" criterion; no schema change |
| 1.13.1  | 723a639 | 2026-06-03 | WX-114 (instrumentation + dashboard hygiene): add a per-call **token-usage DEBUG line** to `ReportWorker` (`LogClaudeTokens`) at all three Claude-call sites (startup, not-news, reconciled), logging input/output/cache-read/cache-write tokens per call — the OTel counters carried these totals but not per-call, so the text log now gives per-call cost visibility for the WX-114 investigation. Grafana `wxreport.json`: drop the embedded ticket number from the "Claude Cost & Volume (WX-110)" row title, and generalize the WX-110/WX-80 references in the Claude Cost Rate / Pre-filter Skip Ratio / Triggers-by-Type panel descriptions (a standing dashboard shouldn't embed ticket numbers), and add a "Significance Gate Skips (per hour)" panel (skips by mode) plus a severe-flip suppression line on Gate Outcomes. PATCH: logging-only code change plus Grafana dashboard updates, no behaviour or schema change |
| 1.13.0  | 9fae5d8 | 2026-06-03 | WX-114 (significance gate): add a deterministic **cost pre-filter** between the WX-80 input-identity pre-filter and the Claude reconciliation call. `SignificanceGate.Evaluate` compares the current deterministic provisional snapshot against the last *sent* forecast (what the recipient last saw) and, when nothing has changed materially, **skips the Claude call entirely** — the input advanced (so the WX-80 pre-filter let the cycle through) but the derived forecast did not. Compares per local calendar day (daily high/low magnitude; a **directional** freeze/thaw at 32 °F — freeze when the low falls strictly below 32, thaw when it rises strictly above, 32 itself staying in the prior state per the latent-heat dead band; heat-advisory crossing) and per 6-hour block (precip occurrence + frozen/freezing *type*, severe flag, sustained-wind advisory + magnitude) across four horizon tiers (0–24/24–48/48–72/72–120 h, thresholds loosening with range; day 6 narrative-only), with eager far-reaching onsets, lazy near-term-only cessations, and always-pass safety-floor rows (freeze/frozen/severe onset, wind reaching advisory). Reuses the already-persisted last-sent body — **no DB column or migration**. Config-driven (`Report:SignificanceGate`) with a three-state `Mode` — `Off` / `Shadow` (evaluate + log but always call Claude, for safe validation) / `Enforce` (skip the Claude call), defaulting to **`Enforce`** so the saving is live and measurable; skips counted by mode (`wxreport.suppressed.significance_gate.total`). Suppress-only and conservative — it can only withhold a Claude call (errs toward calling on every boundary case), never compel or replace one, so Claude still owns every send decision (WX-47-consistent). Wind-direction-shift (deferred to WX-111) and precip-intensity rows are out of v1 (no snapshot field yet). MINOR: new opt-in feature, no schema change |
| 1.12.0  | 2430914 | 2026-06-03 | WX-114 (first step): drop `GustOutlook` and `VisibilityExpectation` from the forecast snapshot (body schema **v2**). Both were coarse GFS-inferred heuristics — gust from sustained wind + CAPE (there is no real gust field), visibility from precip intensity only (so it could not represent fog at all) — and per forecaster judgment readers don't track visibility apart from thunderstorms (which already imply it), so neither earns its place in change-detection. Removed from `ForecastSnapshotBlock` (+ the two enums), `GfsSnapshotBuilder`, the Claude `submit_reconciled_report` tool schema, the reconciler change-signal guidance, and `ForecastSnapshotBody.MateriallyEquals`; `Obscuration`/fog and the severe-storm "damaging gusts" wording are retained. `ForecastSnapshotBody.SchemaVersionCurrent` 1→2 with a metadata-only migration bumping the `ForecastSnapshots.SchemaVersion` column default; old v1 bodies still deserialize (unknown JSON keys ignored). Trims what Claude reconciles against, ahead of the WX-114 deterministic significance gate. MINOR: behaviour + schema change, backward-compatible reads |
| 1.11.2  | 698899e | 2026-06-03 | WX-113: widen `GfsGrid.Id` from 32-bit `int` to 64-bit `bigint`. The IDENTITY counter advances once per row *ever inserted* (never reused on delete), and `GfsGrid` ingests millions of rows per GFS run across 4 runs/day, so it crossed `int.MaxValue` (2,147,483,647) on 2026-06-03 ~08:22 UTC even though only ~2 runs are retained at once — every GFS insert then failed with *"Arithmetic overflow error converting IDENTITY to data type int"*, stalling ingestion (~1,300 failures over ~3.5 h; WxReport degraded gracefully, silently serving the last good run). Mitigated immediately by reseeding the IDENTITY into the unused negative `int` range (`DBCC CHECKIDENT('GfsGrid', RESEED, -2147483648)`), which resumed ingestion. Permanent fix: the `WidenGfsGridIdToBigint` migration drops `PK_GfsGrid`, runs `ALTER COLUMN [Id] bigint` (the IDENTITY property is retained across the widening), then recreates the clustered PK — raw SQL, because EF's scaffolded bare `AlterColumn` cannot alter a column backing the clustered PK. `bigint` sizes the key to the lifetime issuance rate, not the live row count. An audit (filed as WX-116) confirmed `GfsGrid` was the only table at risk — the next-closest had ~150 years of `int` headroom. PATCH: defect fix + schema widen, no behaviour change |
| 1.11.1  | b842825 | 2026-06-03 | WX-112: make the WxReport Extended Forecast table render deterministically as **exactly one row per calendar day**. The per-recipient rendering prompt asked for a per-day `Date` row with the *daily* high/low but never told Claude to aggregate the snapshot's 6-hour blocks (00/06/12/18Z), so Claude rendered inconsistently cycle-to-cycle — sometimes one row per day, sometimes per-block rows labeled Morning/Afternoon/Evening/Overnight with semantically-weak per-block highs/lows (an "overnight high" that was just the evening carryover). The Extended Forecast rule now mandates one row per local calendar day (first row = remainder of today), forbids sub-period rows and duplicate dates, renders the Date cell as weekday+date only (no period label), and specifies each row's high/low is that day's overall max/min across its blocks. Prompt-only change; no send-logic or data change. PATCH: rendering-consistency fix |
| 1.11.0  | 94a6001 | 2026-06-03 | WX-110: cut WxReport reconciliation cost and unwarranted unscheduled sends, keeping the "is it news?" judgment with Claude. (1) The WX-80 pre-filter's METAR component is now a coarse *material* signature — station + public-meaningful wind / visibility / sky / ~5 °F-temperature bands + present-weather tokens — instead of the raw observation timestamp, so a meteorologically-identical hourly re-observation pre-filter-skips with **no** Claude call, closing the dominant cost leak (metar-only arrivals were ~62% of gate calls). (2) The reconciliation gate prompt gains an explicit default-to-skip decision rule (a fresh TAF/GFS issuance that reconciles to a materially-unchanged forecast is not news), and the WX-108 redundancy backstop's wind comparison is coarsened to the same impact bands (same-band drift no longer re-sends). (3) `MinGapMinutes` 60 → 90, after send-gap analysis showed 61% of per-recipient gaps pinned at the 60-minute floor — the rate limit, not the weather, was setting the cadence. (4) `ForecastReconciler` now retries a malformed (non-truncation) reconciliation up to 3 attempts — residual to WX-109: Claude intermittently omits an advisory-`required` tool_use field even when `stop_reason != max_tokens`; a max_tokens truncation and a guaranteed-send `skip_send` are not retried. (5) New Grafana cost/volume panels (`observability/grafana/dashboards/wxreport.json`) surface the existing Claude token counters as $/day plus trigger-mix and gate-outcome diagnostics. MINOR: send-behaviour change + new observability, backwards-compatible |
| 1.10.2  | 40bd65a | 2026-06-02 | WX-109: raise the reconciliation `max_tokens` from 8192 to 16384 and detect output-token truncation. The `submit_reconciled_report` tool returns three artifacts in one tool_use call (HTML `email_body` + up to a 24-block `final_snapshot` + `reasoning_trace`); on a large six-day day that exceeded 8192 output tokens, so Anthropic returned `stop_reason` `max_tokens` with a *truncated* partial tool_use input — a trailing required field (usually `email_body`) dropped — which WX-104 then reported, accurately but misleadingly, as "missing required field 'email_body'". `ClaudeClient` now carries `stop_reason` on its result and `ForecastReconciler` short-circuits a `max_tokens` response into an explicit "response truncated at the output-token cap" failure *before* the field-presence check, so the operator log names truncation instead of a phantom missing field. The recipient still self-heals next cycle (provisional `CommittedSend` left in place). PATCH: defect fix, no change to send behaviour |
| 1.10.1  | 7efc31b | 2026-06-02 | WX-104: `ForecastReconciler` reads Claude's tool_use fields via `TryGetProperty`, so a field Claude omits (or returns null) is reported as a typed, field-named validation failure instead of an unhandled `KeyNotFoundException` mislabelled "Schema validation failed". The operator log now names the missing field and no exception escapes. Orphaned provisional `CommittedSend` rows from a failed reconcile are deliberately retained as failed-attempt audit rows (`SentAtUtc` null → never a prior, never delivered; the next cycle self-heals). PATCH: diagnosability/robustness, no change to send behaviour |
| 1.10.0  | 0ae83b5 | 2026-06-02 | Tame WxReport unscheduled over-sends on active-weather days (WX-108). After the WX-47 finale, frequent METAR/TAF arrivals drove an hourly stream of updates whose severe-weather assessment whipsawed on unchanged model guidance. Fix: (1) a new additive `RecipientState.LastSentInputHash` lets the reconciler tell Claude which inputs are genuinely newer than the last *delivered* report (`changed_since_last_sent_report`); (2) anti-reversal + fresh-TAF-isn't-news prompt guidance so Claude won't flip a `severeFlag`/precip tier on an observation alone; (3) a deterministic backstop in `ReportWorker` — `ForecastSnapshotBody.MateriallyEquals` suppresses a re-send materially identical to the last sent report, and one-directional severe-flag hysteresis suppresses a lone severe *de-escalation* on an observation-only advance (a newly appearing severe hazard always sends); (4) a confidence-language rule so prose never reads as flatly certain even when a block is `certain`. New telemetry: `wxreport.suppressed.redundant.total`, `wxreport.suppressed.severe_flip.total`. MINOR: additive column + telemetry + send-behaviour change, backwards-compatible |
| 1.9.1   | 8132338 | 2026-06-01 | Final WX-47 cleanup (WX-83): drop the vestigial `RecipientState.LastSnapshotFingerprint` column (unused since WX-80's trigger consolidation) via the `DropRecipientStateSnapshotFingerprint` EF migration, and bring DESIGN.md up to date with the rearchitected WxReport flow — the two-pass Claude reconciliation, the persistence model, and the unified input-identity pre-filter — plus a fail-forward migration/cutover note. Removes the last orphan references to the retired `SnapshotFingerprint`. PATCH: schema cleanup, no behaviour change |
| 1.9.0   | a8f7e1f | 2026-06-01 | Significance-tier prompting for the WX-47 reconciliation pass (WX-81): the reconciler system prompt now calibrates "worth sending" across three tiers (safety-critical / plans-affecting / ambient-interest), encodes the directional-asymmetry rule and a 34 kt tropical-storm-force wind bright line, and carries eight worked examples mapping each tier to `submit_reconciled_report` / `skip_send`. Also adds a version-consistency guard (shared script + pre-push hook + CI step) and corrects a missed bump: 1.8.0 (WX-80) never bumped `Directory.Build.props`, so 1.8.0 binaries self-report 1.7.1 — 1.9.0 is the first build carrying a ≥1.8 version string |
| 1.8.0   | c0ff6d6 | 2026-05-31 | Trigger consolidation (WX-47 epic): a unified input-identity pre-filter plus a Claude invalidation gate replace the observation-to-observation `SnapshotFingerprint` behind the 2026-04-21 KDWH double-send. All four trigger sources (METAR/TAF/GFS arrival, scheduled) flow through one path; Claude can now decline to send via a two-tool choice (`submit_reconciled_report` / `skip_send`). New additive `RecipientState.LastClaudeInputHash` column drives the pre-filter. New trigger/skip telemetry; dead `SignificantChangeConfig` removed (WX-80) |
| 1.7.1   | 2064964 | 2026-05-29 | Fix WX-79 regression: the reconciliation Claude call inherited the 100s default `HttpClient.Timeout`, but the pass routinely generates for 60-100s, so ~1-in-3 reports were silently dropped (`TaskCanceledException`). Give Claude its own `HttpClient` with a tunable `Claude:TimeoutSeconds` ceiling (default 300s), kept off the shared geocoding/airport-lookup client so those still fail fast. A timeout fails the pass and recovers next cycle (not retried — retrying a multi-minute call would stall the cycle). Histogram boundaries for `wxreport.claude.duration.seconds` extended past the old 60s cap that hid the latency (WX-100) |
| 1.7.0   | 5845742 | 2026-05-28 | Claude two-pass forecast reconciliation (WX-79): GFS-derived deterministic provisional snapshot now feeds Claude as input to a tool-use reconciliation pass; Claude returns refined snapshot + HTML email body + reasoning trace, all persisted. Replaces the observation-to-observation fingerprint logic that produced the 2026-04-21 KDWH double-send. New `ForecastReconciler` class; `ClaudeClient` simplified to a thin Anthropic wrapper. Six new OTel instruments for token + outcome telemetry |
| 1.6.2   | 8a5d5ba | 2026-05-27 | GFS → provisional snapshot builder for the WX-47 rearchitecture: new `GfsSnapshotBuilder.Build(GfsHourlyForecast) → ForecastSnapshotBody` projects a GFS hourly forecast into the uniform 6-hour blocks defined by WX-76; `GfsInterpreter` gains `GetHourlyForecastAsync` alongside the existing daily-summary API. No runtime change yet — the builder is library-only until WX-79 wires it into the report path (WX-77) |
| 1.6.1   | aa01722 | 2026-05-26 | Close two WX-78 audit-trail gaps surfaced by the 1.6.0 deploy log smoke test: `SendStartupReportAsync` now writes the `CommittedSend` lifecycle (was skipping it entirely); the two persistence boundary log lines promoted from Debug to Info so they appear in `wxreport-svc.log` at default log level (WX-86) |
| 1.6.0   | 88a1625 | 2026-05-26 | Persistence layer for the WX-47 rearchitecture: new `CommittedSend` entity, FK to `ForecastSnapshot`, and a write-before-Claude / overwrite-on-success / leave-on-failure lifecycle in `ReportWorker`. Schema lands an `AddCommittedSends` migration; cycles now persist one row per send (WX-78) |
| 1.5.5   | 6252d32 | 2026-05-25 | Forecast snapshot schema: `ForecastSnapshot` entity, `ForecastSnapshotBody` JSON record, and `AddForecastSnapshots` migration. Foundation for the WX-47 rearchitecture; no user-visible behaviour change yet (WX-76) |
| 1.5.4   | a92618f | 2026-05-19 | Adopt EF Core Migrations; replace `EnsureCreated` + hand-written idempotent DDL with a baseline migration and a `MigrateAsync` startup path (WX-72) |
| 1.5.3   | 8ad5cd4 | 2026-05-01 | Set `ContentTransferEncoding` to `ContentEncoding.Base64` explicitly on inline meteogram parts to fix WX-60 regression that emptied the image body in transit (WX-70) |
| 1.5.2   | 01867e7 | 2026-05-01 | Bump `MailKit` 4.9.0 → 4.16.0 and `OpenTelemetry` family 1.15.2 → 1.15.3 to clear remaining NU1902 advisories (WX-60) |
| 1.5.1   | 5c9dd30 | 2026-04-30 | Bump `log4net` 2.0.17 → 3.3.1 to clear NU1902 advisory CVE-2026-40021 (WX-24) |
| 1.5.0   | f5fa31d | 2026-04-27 | Recipient address accepts What3Words `///word.word.word` and direct `lat, lon` decimal entry alongside street address (WX-52) |
| 1.4.1   | 1395754 | 2026-04-26 | Fix Maps-tab control press jumping to Meteograms tab via WPF focus-scope redirect (WX-46) |
| 1.4.0   | a47732a | 2026-04-25 | WxReport.Svc Claude calls now carry a cached author-persona prefix from `AboutPaul.md` (WX-50) |
| 1.3.8   | 79ec475 | 2026-04-23 | Promote TabControl.SelectionChanged diagnostic to permanent logging (WX-48) |
| 1.3.7   | 726c01d | 2026-04-18 | Analysis map MSLP now uses proper temperature-based reduction from altimeter (WX-35) |
| 1.3.6   | fa6c0c3 | 2026-04-18 | GFS pipeline switched from WSL-invoked wgrib2 to native Windows wgrib2.exe (WX-33) |
| 1.3.5   | 115d037 | 2026-04-17 | Retry DB connection at service startup with per-attempt WARN + final ERROR (WX-28, partial) |
| 1.3.4   | d3996df | 2026-04-16 | Retry with exponential backoff on transient upstream fetch failures (WX-20, WX-21) |
| 1.3.3   | 5205064 | 2026-04-16 | Defensive idempotent ALTER for Metars/Tafs ReceivedUtc (WX-22) |
| 1.3.2   | 5569f78 | 2026-04-16 | Geographic nearest-neighbour METAR fallback within 30 mi (WX-19) |
| 1.3.1   | d9ec9fc | 2026-04-15 | Forecast temperatures formatted on two labeled lines (WX-14) |
| 1.3.0   | d70f708 | 2026-04-15 | WxStations country/region columns (WX-13) |
| 1.2.1   | 83b9a29 | 2026-04-14 | WxViewer zoom-swap fix; numpad zoom controls |
| 1.2.0   | 211282c | 2026-04-13 | Metrics instrumentation across all services; report footer branding |
| 1.1.1   | a0a3b5c | 2026-04-13 | Bug fixes, logging audit, meteogram cosmetic fix |
| 1.1.0   | f6fd52c | 2026-04-12 | Multi-zoom map rendering with interactive zoom/pan |
| 1.0.2   | d55384e | 2026-04-12 | Meteogram temp-unit bug fix; log format cleanup |
| 1.0.1   | ef49de4 | 2026-04-11 | Version display and startup logging |
| 1.0.0   | 7a2a268 | 2026-04-07 | Initial versioned release |

---

## 1.9.1 — WX-47 finale: drop the vestigial fingerprint column + DESIGN.md refresh (2026-06-01)

- **WX-83 (WX-47 epic), the last child** — closes the rearchitecture of WxReport's update-decision logic.
- **Schema cleanup.** Drops `RecipientState.LastSnapshotFingerprint`, vestigial since WX-80 unified the triggers behind the Claude invalidation gate (nothing has read or written it since). EF migration `DropRecipientStateSnapshotFingerprint`; applied automatically on next startup via `MigrateAsync`. Fail-forward only (no rollback), consistent with the WX-47 deployment model.
- **DESIGN.md refresh.** The §2.4/§4.2 data flow, the Key-classes table, the persona-prefix call path, and the §6 ER diagram now describe the shipped design: GFS-derived provisional snapshot → input-identity pre-filter → Claude two-pass reconciliation (`submit_reconciled_report` / `skip_send`) → persisted `CommittedSend` + `ForecastSnapshot` + reasoning trace. Significance is judged by Claude against the committed forecast (WX-81 tiers), not C# thresholds. Added a migration/cutover note (additive schema, graceful no-prior-snapshot first boot, fail-forward).
- **PATCH.** Schema cleanup + docs; no behaviour change for recipients.

## 1.9.0 — Significance-tier prompting + version-consistency guard (2026-06-01)

- **WX-81 (WX-47 epic).** Teaches the reconciliation pass *when* a change is worth sending — calibrating the `submit_reconciled_report` / `skip_send` gate that WX-80 wired. Pure prompt engineering: the whole change is a new "Significance hierarchy" section in `ReconcilerPrompts.ReconciliationGuidanceText`, no schema and no C# control flow. Significance judgment stays in Claude's reasoning rather than a C# `ClassifyChange`.
- **Three tiers.** Safety-critical (severe storms, dense fog, ice/freezing precip, and newly-forecast sustained non-thunderstorm winds ≥ 34 kt / tropical-storm force — send at any horizon); plans-affecting (precip vs. dry, notable winds, temperature swings, ordinary non-severe storms — send near-horizon, ride the next scheduled send when distant); ambient-interest (pleasant-day signals, light breezes — scheduled sends only).
- **Severe vs. ordinary storms.** A change to scattered thunderstorms reaches the safety tier only when storm energy points to strong-to-severe storms (`severeFlag` / convective signals); otherwise it is plans-affecting. Severity, not the word "thunderstorm," escalates.
- **Directional asymmetry + worked examples.** Encodes that unpromised weather arriving is more newsworthy than promised weather not yet arriving, plus eight worked examples (3 safety-critical, 3 plans-affecting, 2 ambient) anchoring the calibration. The 2026-04-21 KDWH case is one of them.
- **Version-consistency guard.** `WxServices/tools/check-version-consistency.sh` asserts `Directory.Build.props <Version>` equals the top VERSIONS.md row; run by a tracked `pre-push` hook (activated with `git config core.hooksPath WxServices/tools/hooks`) and a CI step. Belt-and-suspenders: fast local fail before push, non-bypassable gate at merge.
- **1.8.0 correction (folded in).** WX-80 added the 1.8.0 row but never bumped `Directory.Build.props`, so 1.8.0 binaries self-report 1.7.1. History is not rewritten; 1.9.0 is the first build carrying a ≥1.8 version string, and the new guard makes the mismatch unrepeatable.
- **MINOR bump.** New behavior (different send/skip calibration), backwards-compatible; no schema or data migration.

## 1.8.0 — Trigger consolidation: unified pre-filter + Claude invalidation gate (2026-05-31)

- **WX-80 (WX-47 epic).** Replaces the observation-to-observation `SnapshotFingerprint` — the logic behind the 2026-04-21 KDWH double-send, which fired an unscheduled update for rain the prior forecast had already predicted — with a two-part pipeline that all four trigger sources (METAR/TAF/GFS arrival, scheduled) flow through.
- **Cheap C# pre-filter.** Each cycle computes a raw *input identity* (METAR obs-time + station, TAF issuance, GFS model run), persisted per recipient in the new `RecipientState.LastClaudeInputHash` column. If nothing advanced since the last Claude call, the cycle is a no-op and no tokens are spent. Significance is no longer judged in C# — the deleted fingerprint's thresholds were exactly the judgment-in-C# that misfired on KDWH.
- **Claude invalidation gate.** When an input has advanced, Claude chooses between two tools: `submit_reconciled_report` (send) or `skip_send` (not news). Scheduled/first/startup sends force the submit tool and can never be skipped. On a "not news" result the input hash is recorded and the reasoning trace persisted, but no email is sent and the committed snapshot does not advance — so the anchor always equals "what we last told the recipient" (the prior-snapshot lookup already filters on `SentAtUtc`).
- **Schema.** Additive `RecipientState.LastClaudeInputHash` via EF migration `AddRecipientStateClaudeInputHash`; applied automatically on next startup. The now-vestigial `LastSnapshotFingerprint` column is left for WX-83 to drop.
- **Cleanup / observability.** Deleted `SnapshotFingerprint` (and its `WxInterp.Tests`); extracted the `ChangeSeverity` enum (dropped the now-meaningless `Minor`; `Alert` reserved for WX-81's tiering). Removed the dead `SignificantChangeConfig` and its appsettings block. New OTel instruments: `wxreport.triggers.total` (tagged by `trigger.type`), `wxreport.prefilter.skips.total`, `wxreport.claude.not_news.total`.
- **MINOR bump.** New feature, backwards-compatible; the additive column needs no data migration.

## 1.7.1 — Fix reconciliation timeout dropping ~1-in-3 reports (2026-05-29)

- **WX-100 (regression from WX-79).** The reconciliation pass shipped in 1.7.0 reused the `"WxReport"` `HttpClient`, which never set `Timeout` and so inherited .NET's 100s default. Production logs from the first full day (2026-05-29) showed the reconciliation call naturally runs **60-100s** — successes clustered at 84-97s — so the timeout sat flush against the operation's own latency. Result: **11 of 33 reconciliations failed (~33%)**, every failure a `TaskCanceledException` at exactly 100s, and each failure silently skipped that recipient's email for the cycle.
- **Headroom on a dedicated client.** New tunable `ClaudeConfig.TimeoutSeconds` (config key `Claude:TimeoutSeconds`, default 300s, ~3× the observed worst case) is applied to a **separate `"Claude"` `HttpClient`** registered in `Program.cs`. The reconciliation call previously shared the `"WxReport"` client with geocoding and airport-lookup calls; raising *that* client's timeout would have slowed those fast-fail paths from 100s to 300s. Splitting the client keeps the long ceiling scoped to Claude. `TimeoutSeconds` is read through the typed `ClaudeConfig` (single source of truth). A non-positive configured value is rejected at startup — logged at WARN and replaced with the 300s default — rather than crashing on `HttpClient.Timeout`'s `ArgumentOutOfRangeException` for zero/negative `TimeSpan`s.
- **A timeout fails the pass; it is not retried.** `ClaudeClient` keeps its connection-error retry (`HttpRequestException`, 3 attempts with backoff) and now classifies a genuine host-shutdown cancellation cleanly (Info log, no retry). An `HttpClient` timeout (`TaskCanceledException`, request token not signalled) deliberately falls through to fail the pass and recover next cycle: at a 300s ceiling a timeout signals a real stall, and retrying a multi-minute call up to 3× would serialize into a ~15-minute-per-recipient cycle stall that could trip WxReport's heartbeat-stale monitor. (An interim cut that reclassified timeouts as retryable was caught by a second-pass code review and reverted.)
- **Observability fix folded in.** The `wxreport.claude.duration.seconds` histogram topped out at a 60s bucket, so every 60-100s reconciliation landed in the overflow bucket — which is *why the latency wall was invisible in telemetry*. Boundaries extended to `[1, 2, 5, 10, 20, 30, 60, 90, 120, 180, 300, 360]`; the 360 sits above the default 300s ceiling so a timed-out call is distinguishable from a near-ceiling success rather than both vanishing into overflow.
- **Tests.** Three cases in `WxReport.Tests` driving `ClaudeClient.InvokeReconciliationAsync` directly: an `HttpClient` timeout fails the pass on a single attempt (no retry); a connection error is retried and recovers; a mid-flight shutdown cancellation aborts on the first attempt with no retry. Each carries a wall-clock `[Fact(Timeout)]` so a propagation regression fails fast instead of hanging the suite.
- **Follow-up.** WX-101 will switch the reconciliation call to streaming (SSE), which removes the wall-clock ceiling entirely and supersedes the overall-timeout knob with an idle/read timeout.
- **PATCH bump.** Bug fix, no behaviour change for recipients (beyond getting the reports they were already supposed to get).

## 1.7.0 — Claude two-pass forecast reconciliation (2026-05-28)

- **WX-79 (LLM-native reconciliation):** Closes the WX-47 two-pass protocol. GFS-derived provisional snapshot (WX-77) now feeds Claude as input to a tool-use reconciliation pass; Claude reconciles against current TAF, current METAR observation, and the prior committed snapshot, returning a refined snapshot + HTML email body + reasoning trace via a forced `submit_reconciled_report` tool. Replaces the observation-to-observation fingerprint that produced the 2026-04-21 KDWH double-send.
- **Reconciliation procedure** groomed with Paul (former forecaster):
  1. For block start times within the TAF's validity window, prefer TAF over GFS *only when* the TAF's issuance time is later than the GFS model run time; otherwise GFS is the fresher signal.
  2. An observation at hour X overrides the provisional for hour X or X+1 only; beyond that, the model projection prevails.
  3. Diff against the prior committed snapshot to render the "is this news?" judgment; surface meaningful divergences in the email body, suppress trivial drift. "Trivial" is left as Claude's judgment for now; WX-81 may tighten if calibration drifts.
- **New `ForecastReconciler` class** orchestrates the pass. Builds a three-block system prompt (persona prefix + reconciliation guidance + per-recipient rendering rules), forces the tool, validates the response via `ForecastSnapshotBody.Deserialize` (which enforces shape and the precipPhenomenon-iff-non-none invariant). Returns a discriminated `ReconcileResult.Success`/`Failure`. Malformed-output policy (skip the send + log, never un-commit) catches schema violations and HTTP failures; a `Failure` keeps the provisional row + `CommittedSend` in place as an auditable "we tried, Claude failed" record.
- **`ClaudeClient` simplified** to a thin Anthropic Messages API wrapper exposing one method, `InvokeReconciliationAsync`, plus two public records (`TokenUsage`, `ClaudeReconciliationResult`) the caller consumes. The pre-WX-79 `GenerateReportAsync` and its `BuildFooterHtml` + `BuildCurrentConditionsSubtitle` helpers were deleted; the email-envelope wrapping moved into `ReportWorker.WrapAsEmailHtml` as post-reconciler bookkeeping.
- **`WeatherSnapshot` extension.** New `TafIssuanceUtc` and `TafValidToUtc` nullable timestamps populated by `WxInterpreter` from the active TAF record, needed by step 1 of the reconciliation procedure to compare against the GFS model run time.
- **`GfsSnapshotBuilder` wiring** replaces the previous WX-78 stub. `BuildProvisionalForecastSnapshotAsync` calls `GfsInterpreter.GetHourlyForecastAsync` at the recipient's coordinates and feeds the result through `GfsSnapshotBuilder.Build`. Each recipient now gets their own provisional row (the previous per-station stub-cache was dropped — bodies are interpolated at the recipient's exact coordinates, so two recipients sharing a station no longer share a row).
- **Persistence shape extends.** A successful reconciliation inserts a *second* `ForecastSnapshot` row (the refined body, separate from the provisional for audit), repoints `CommittedSend.ForecastSnapshotId` at it, and populates `ReasoningTrace` alongside `EmailBody`. The provisional row stays in the table for forensic diff against the reconciled.
- **OTel additions.** Six new `Counter<long>` instruments on the `WxReport.Svc` meter — `wxreport.claude.tokens.input.total`, `tokens.output.total`, `cache.read.total`, `cache.write.total`, `tool_use.success.total`, `malformed_output.total`. The malformed-output counter is the calibration signal the grooming policy promised to revisit.
- **Caching: conservative plan.** The reconciliation guidance block carries `cache_control: ephemeral`, so the persona + guidance prefix is cacheable. Smoke-test observation showed `cache_read = 0` despite `cache_write` firing — cycles spaced beyond the 5-minute ephemeral TTL and per-recipient personas mean we pay the write premium without capturing reads. A follow-up ticket will revisit (either 1-hour extended cache or restructuring to enable cross-recipient hits).
- **Tests.** New `WxReport.Tests` project with eight cases driving `ForecastReconciler` against stubbed Anthropic responses: happy path + transport failure (HTTP 400) + no tool_use block + missing each of three required tool-input fields + garbage `final_snapshot` + precipPhenomenon-iff-non-none violation. Full solution 268/268 tests passing; `dotnet format --verify-no-changes` clean.
- **Smoke test on Paul's PC (2026-05-28).** Verified end-to-end: paired provisional + reconciled `ForecastSnapshots` rows (5065 bytes each, populated body); `CommittedSends` rows show `ReasoningTrace` populated with a detailed three-step audit log (verified by hand: TAF-vs-GFS by issuance time, observation override on first block, prior-snapshot diff); emails delivered correctly and matched pre-WX-79 visual structure; `wxreport.claude.tool_use.success.total > 0` and `malformed_output.total = 0`.
- **MINOR bump.** New feature — the reconciliation pass — that's backwards-compatible from the recipient's perspective (emails look identical). Materially changes the data picture (`ReasoningTrace` column populated for the first time; second `ForecastSnapshot` row per cycle).

## 1.6.1 — Close WX-78 audit-trail gaps surfaced by the 1.6.0 deploy (2026-05-26)

- **WX-86 (defect fix):** Two gaps in 1.6.0's `CommittedSend` persistence flow surfaced when Paul truncated `wxreport-svc.log` before deploying 1.6.0 and observed that the new lifecycle boundaries were nowhere in the log even after a successful startup-report send and a full normal cycle.
- **Gap 1: `SendStartupReportAsync` skipped the new persistence.** 1.6.0 added the provisional-write / overwrite / `SentAtUtc` lifecycle to `RunCycleAsync` only. The one-shot at-startup send (called once per service launch) bypassed it entirely — emails went out without producing a `CommittedSend` row, violating the WX-47 audit-trail invariant that every successful send anchors to a snapshot and a persisted row. The startup path now mirrors the cycle path exactly: stub `ForecastSnapshot` + provisional `CommittedSend` before Claude, overwrite on success, `SentAtUtc` stamped + saved independently after the SMTP send. `OperationCanceledException` is filtered out of both startup-path catches (consistent with the cycle path after WX-78's CR round).
- **Gap 2: persistence boundary logs at Debug level.** 1.6.0's two new boundary lines (`wrote provisional CommittedSend Id=N`, `overwrote CommittedSend Id=N with Claude body`) were `Logger.Debug`. WxReport.Svc runs at Info level in production, so the lines were filtered out — exactly the visibility hook Paul wanted for first-deploy smoke testing was invisible. Both lines promoted to `Logger.Info` and added to the startup path symmetrically.
- **No schema or entity change.** Pure-code defect closure on the persistence flow.
- **Test suite.** Build clean, 218/218 tests green, format check clean. Behavior-level tests still blocked on the same DI refactor noted in WX-78's PR; the visible-log boundaries become the manual verification surface until that lands.
- **PATCH bump.** Defect closure on 1.6.0's stated behavior, not a new feature.

## 1.6.0 — Persistence layer for the WX-47 rearchitecture (2026-05-26)

- **WX-78 (persistence):** Introduced the `CommittedSend` entity and the write-before-Claude lifecycle that anchors each report-send to a `ForecastSnapshot` and carries the Claude-rendered email body forward. Second child of the WX-47 umbrella, downstream of WX-76's schema and independent of WX-77's deterministic snapshot builder. Per the WX-47 two-pass protocol, persisting the provisional row *before* Claude runs is the audit-trail guarantee: a Claude failure still leaves a record of what we were about to send.
- **Entity.** New `MetarParser.Data.Entities.CommittedSend` POCO with `Id`, `ForecastSnapshotId` + navigation, `RecipientId`, nullable `ReasoningTrace` and `EmailBody`, `CreatedAtUtc`, nullable `SentAtUtc`, and `SchemaVersion`. The non-nullable FK to `ForecastSnapshot` is wired with `OnDelete(DeleteBehavior.Restrict)` so a snapshot can't be silently deleted out from under a committed row. The composite index `IX_CommittedSends_Recipient_CreatedAt` on `(RecipientId, CreatedAtUtc DESC)` supports the natural "most recent send to recipient X" query that WX-79 and WX-80 will lean on.
- **Column nullability captures lifecycle state.** `EmailBody IS NULL` separates Claude-completed-but-not-sent rows from sent ones in concert with `SentAtUtc IS NULL`, without a dedicated status enum: provisional row = `EmailBody NULL ∧ SentAtUtc NULL`; Claude completed = `EmailBody NOT NULL ∧ SentAtUtc NULL`; sent = `EmailBody NOT NULL ∧ SentAtUtc NOT NULL`. `ReasoningTrace` stays `NULL` end-to-end in this ticket; WX-79 wires Claude tool-use returns and fills the column.
- **Migration.** `20260526180744_AddCommittedSends.cs` creates the `CommittedSends` table with the columns, the restricted FK, the auto-generated `IX_CommittedSends_ForecastSnapshotId` index that EF emits for the FK, and the composite recipient/created-at index above. Generated by `dotnet ef migrations add AddCommittedSends` per WORKFLOW.md §"Schema changes"; reviewed and accepted as-emitted.
- **`ReportWorker` lifecycle.** `RunCycleAsync` now persists per recipient: (1) before invoking Claude, write a provisional `CommittedSend` anchored to a stub `ForecastSnapshot` (one stub per station per cycle, deduped through a per-cycle `Dictionary` keyed by ICAO so co-recipients on the same station share a snapshot); (2) on Claude success, fill `EmailBody = report` and `SaveChangesAsync` so the Claude artifact is durable independently of the subsequent email send; (3) on email send success, stamp `SentAtUtc = DateTime.UtcNow` (persisted alongside the existing `RecipientState` write); (4) on either failure mode, leave the row in place — the existing failure logs cover the why, and the row's nullability shape carries the where.
- **WX-77 swap point.** A small private `BuildStubForecastSnapshot(stationIcao, generatedAtUtc)` helper currently constructs a `ForecastSnapshot` whose `Body` is a serialized empty `ForecastSnapshotBody`. WX-77 lands the real `GfsSnapshotBuilder`; the swap is a one-line change at the helper's call site. The stub strategy keeps `CommittedSend.ForecastSnapshotId` non-nullable today, so WX-77 doesn't have to ship a follow-up migration.
- **Tests.** Three new tests in `WxInterp.Tests/CommittedSendTests.cs` cover the entity's default shape (`SchemaVersion = 1`, all nullable fields null, `RecipientId` is the empty string), the schema-version constant, and an empty-body `ForecastSnapshotBody` round-trip that guards the stub-builder serialization path. Behavior-level tests of the full `ReportWorker` persistence lifecycle are gated on a DI refactor — `ClaudeClient` and `SmtpSender` are currently `new`'d inline at lines 291–292, not injected, so the flow cannot be cleanly exercised by a unit test without architectural changes. Filed as a follow-up rather than expanding WX-78 scope.
- **Build + tests + format.** Full solution build clean. Full test suite passes locally (218/218). `dotnet format WxServices.CI.slnf --verify-no-changes` exits clean.
- **MINOR bump.** WX-76 was PATCH because nothing read or wrote the table; WX-78 starts writing on every cycle. The behavior change is invisible to recipients but materially changes the data picture (new rows accumulate from this version onward), which is the SemVer threshold between PATCH and MINOR.

## 1.5.5 — Forecast snapshot schema (2026-05-25)

- **WX-76 (foundation):** Introduced the `ForecastSnapshot` entity and the `ForecastSnapshotBody` JSON body type — the persisted anchor that future report cycles will diff against to decide whether the forecast has been invalidated. First child of the WX-47 umbrella rearchitecture; the table is created but not yet read or written by any service (WX-77 populates it, WX-78 persists it, WX-79 reasons over it).
- **Entity.** New `MetarParser.Data.Entities.ForecastSnapshot` POCO with `Id`, `StationIcao`, `GeneratedAtUtc`, `SchemaVersion`, `Body`. Single composite unique index `UX_ForecastSnapshots_Station_GeneratedAt` on `(StationIcao, GeneratedAtUtc)` — covers both "no duplicate snapshots at the same instant for a station" and the "most recent snapshot for station X" lookup. `StationIcao` is an `nchar(4)` string column (not a database FK) per the convention used by `MetarRecord` and `TafRecord`. `SchemaVersion` carries a DB-level default of 1 alongside the C# property default, so raw SQL inserts that omit the column still produce a valid row.
- **Body type.** New `ForecastSnapshotBody` record in the same `Entities` folder: schema-versioned envelope around an ordered list of uniform 6-hour `ForecastSnapshotBlock`s aligned to 00/06/12/18Z (up to 24 blocks = six days). Per-block fields capture sky state, obscuration, temperature/wind ranges, gust outlook, precipitation expectation/phenomenon, severe-weather flag, and visibility expectation. Enums are serialized via `JsonStringEnumConverter(JsonNamingPolicy.SnakeCaseLower)` so `FreezingPrecip` ↔ `"freezing_precip"`; properties via `[JsonPropertyName]` camelCase. All non-nullable fields use the `required` modifier so a malformed body fails deserialization rather than silently filling in defaults. `Serialize` and `Deserialize` additionally call a private `Validate()` that enforces the precipPhenomenon-iff-non-none invariant, so neither persistence nor reads can produce semantically invalid bodies.
- **Why JSON in a single column.** Recorded at WX-76 grooming: no obvious query need for per-block relational access, and the "Claude's working memory" framing of WX-47 favours a single self-describing artifact. Revisit only if relational queries become necessary downstream.
- **`freezing_precip` bucketing.** Freezing rain, freezing drizzle, and sleet ride one enum value rather than three. They are equally road-slickening and equally safety-critical; the distinction between them rarely changes the reader's decision.
- **Migration.** `20260525205858_AddForecastSnapshots.cs` creates the `ForecastSnapshots` table with the columns and unique index above. Generated by `dotnet ef migrations add AddForecastSnapshots` per WORKFLOW.md §"Schema changes"; reviewed and accepted as-emitted.
- **Schema documentation.** New `WxServices/docs/forecast-snapshot-schema.md` is the human-readable specification of the body shape. The C# `ForecastSnapshotBody` record remains the source of truth; the Markdown is for design reviewers and downstream ticket authors.
- **Tests.** Ten new tests in `MetarParser.Tests/ForecastSnapshotBodyTests.cs` covering round-trip fidelity (single + multi-block), canonical JSON form (camelCase properties + snake_case enums + null-phenomenon omission), strictness (unknown enum value rejected, missing required field rejected, precip-invariant violations rejected in both directions), and defaults. Full test suite 215/215 green.
- **No runtime behaviour change.** No service-level code reads the new table yet. The `MigrateAsync` startup path creates the table on the next service boot; no data movement, no reads, no writes. PATCH bump per the WX-72 precedent.

## 1.5.4 — Adopt EF Core Migrations (2026-05-19)

- **WX-72 (tech-debt / infra):** Replaced the previous schema-management hybrid — `EnsureCreatedAsync` plus a growing list of hand-written `IF NOT EXISTS` DDL blocks in `DatabaseSetup.cs` — with EF Core Migrations. New schema changes are now captured as versioned migration files under `src/MetarParser.Data/Migrations/` and applied automatically at service startup via `MigrateAsync`. The `__EFMigrationsHistory` table tracks what's been applied, which the previous pattern had no equivalent for.
- **Why now.** The investigation phase of WX-71 surfaced that the existing `IF NOT EXISTS (table)`-wrapped `CREATE INDEX` for `IX_GfsGrid_Run_Hour` only fires on first-time table creation; an installation whose `GfsGrid` table predated the index would never have picked it up. That class of silent gap — indexes, constraints, defaults applied at table-grain idempotency instead of object-grain — is exactly what proper migrations make impossible. The hybrid was workable for early prototyping but had outgrown its scope.
- **No runtime behaviour change.** The migration is internal-only — same tables, same indexes, same columns, same data on every existing installation. PATCH bump.
- **Tooling.** Added a local-manifest dotnet tool entry for `dotnet-ef` pinned to `8.0.0` (matching the `Microsoft.EntityFrameworkCore.SqlServer` package version) at `.config/dotnet-tools.json`. New developers run `dotnet tool restore` once after clone. The repo's `Directory.Build.props` redirects `obj/` to `C:\HarderWare\BuildCache\...` on Windows, so EF tooling calls need `--msbuildprojectextensionspath` pointed at that redirected location.
- **Design-time factory.** `WeatherDataContextDesignTimeFactory` implements `IDesignTimeDbContextFactory<WeatherDataContext>` so the EF CLI can introspect the model without needing a running service or a real connection string — it just needs the SQL Server dialect.
- **Baseline migration.** `20260519174837_Baseline.cs` captures the schema as it stood at the cutover: 14 tables, all indexes (including the one that motivated this work), all foreign-key cascades. Generated faithfully from `OnModelCreating`. Spot-audited against the previous hand-written DDL; cosmetic drifts (column-level `DEFAULT` constraints, the `GlobalSettings` seed insert, the `GlobalSettings.Id` IDENTITY shape on existing prod) are tolerated rather than fixed because modifying the entity model is out of scope for this ticket.
- **Baselining existing databases.** `DatabaseSetup.BaselineExistingSchemaIfNeededAsync` detects the case where a database created by the old pattern has the schema but no migration history, and inserts a row marking the baseline as already-applied before `MigrateAsync` runs. The detection logic short-circuits cleanly for the brand-new-machine case (no database) and the steady-state case (history already present). The history-table creation plus marker insert run inside a single transaction with `BEGIN TRY`/`BEGIN CATCH` swallowing the specific race-loser errors (`2714 (object already exists)`, `2601`/`2627 (duplicate key)`) so a partial-success state cannot wedge a future restart.
- **Serializing concurrent migrations across services.** All four services race into `EnsureSchemaAsync` at Windows boot.  EF Core 8 has no built-in migration lock (that arrived in 9.0), so the baseline-and-migrate sequence is wrapped in a `sp_getapplock` named lock with `LockOwner = 'Session'` so a crashed service auto-releases.  Without this, a fresh install or a future migration release would see one service apply the schema while the others fail with `2714` on parallel `CREATE TABLE` attempts — annoying-but-recoverable today (SCM restart converges), but actively dangerous as soon as the migration list grows past the baseline.  The `CREATE DATABASE` race that precedes any DB-scoped lock is handled by classifying `SqlException 1801` as transient, so the WX-28 retry loop converges on the first-install case too.
- **`DatabaseSetup.cs` simplified.** All `ExecuteSqlRawAsync` blocks that previously retrofitted additive changes are removed. The method now consists of the WX-28 retry loop, a single `BaselineExistingSchemaIfNeededAsync` call, and a single `MigrateAsync` call. No belt-and-suspenders fallback layer — a divergence between the migration history and a parallel idempotent-DDL path would be a worse bug than what we're solving.
- **Docs.** `DESIGN.md` §9 (Database setup) rewritten to describe the migration pattern, the baselining behaviour, and the known drifts. `WORKFLOW.md` gains a new "Schema changes" section documenting the `dotnet ef migrations add <Name>` procedure so future schema work follows the same shape.
- **`.editorconfig` exemption.** Auto-generated migration files trip the project's strict using-group formatting rules every time EF regenerates them. Added a `[**/Migrations/*.cs]` block setting `generated_code = true` so style analyzers leave the EF output alone.
- **Build clean.** `dotnet build WxServices.sln` reports zero warnings, zero errors.
- **Tests pass.** 205 tests across `MetarParser.Tests`, `TafParser.Tests`, `WxInterp.Tests`, `WxMonitor.Tests` — all green.
- **Pre-merge verification required on Paul's Windows host.** The baselining logic depends on real SQL Server behaviour (`OBJECT_ID(...)` introspection, DDL inside a transaction) that the test suite does not exercise. Before merging: run one service binary against a backup copy of the prod DB and confirm logs show the baselining marker insert plus a clean `MigrateAsync` no-op; separately drop the database and confirm a fresh service start logs `MigrateAsync` creating the schema from the baseline migration.

## 1.5.3 — Restore inline meteogram in WxReport emails (2026-05-01)

- **WX-70 (regression fix):** WX-60's MailKit 4.9.0 → 4.16.0 bump emptied the inline meteogram body in transit. The `multipart/related` part landed at recipients with the correct `Content-Id`, `Content-Type: image/png`, and `Content-Transfer-Encoding: base64`, but **zero body bytes** — so the HTML's `cid:meteogramAbbrev` reference resolved to a broken image.
- **Cause.** When `MimePart.ContentTransferEncoding` is unset, MimeKit calls a "best encoding" inspector that reads the entire content stream to choose between 7bit/8bit/base64. In MimeKit 4.16 this inspector no longer rewinds the stream after reading, so by the time MailKit serializes the part for transmission the underlying `FileStream` is at EOF and the base64 encoder emits nothing. The PNG file on disk was always intact; the corruption was purely in the encoding-inspection path.
- **Fix.** Set `ContentTransferEncoding = ContentEncoding.Base64` explicitly on the inline `MimePart` constructed in `WxServices/src/WxServices.Common/SmtpSender.cs`. PNG is binary-safe under base64; choosing it explicitly does not change the wire output for any valid send — it only removes the side-effect that's currently breaking serialization. One-line addition (plus a short comment explaining the WHY).
- **No alternative paths affected.** `SmtpSender` is the only place we construct `MimePart` from a stream; the plain-text and HTML body parts use `TextPart` with a string `Text` setter, which goes through a different encoding path that was not regressed.
- **Pending post-redeploy verification.** Once redeployed, a live paul-en report should carry a non-empty base64-encoded PNG body matching the bytes of the corresponding meteogram file on disk, with the meteogram rendering inline in the recipient's mail client.

## 1.5.2 — MailKit and OpenTelemetry bumps to clear remaining NU1902 advisories (2026-05-01)

- **WX-60 (security):** Cleared the two NU1902 advisories that remained after WX-24's `log4net` upgrade.
  - **MailKit 4.9.0 → 4.16.0** in `WxServices/src/WxServices.Common/WxServices.Common.csproj`. Closes `GHSA-9j88-vvj5-vhgr` (CVE-2026-41319) — a STARTTLS Response Injection vulnerability where the protocol stream's read buffer was not flushed when swapping to `SslStream`, allowing a MitM to inject pre-TLS bytes that would later be processed as trusted post-TLS responses, enabling SASL mechanism downgrade.
  - **OpenTelemetry family 1.15.2 → 1.15.3** (`OpenTelemetry`, `OpenTelemetry.Exporter.OpenTelemetryProtocol`, `OpenTelemetry.Extensions.Hosting`) in all four service csprojs (`WxParser.Svc`, `WxMonitor.Svc`, `WxReport.Svc`, `WxVis.Svc`). Closes `GHSA-mr8r-92fq-pj8p` (CVE-2026-40891) — unbounded `grpc-status-details-bin` parsing in OTLP/gRPC retry handling that could allow a hostile collector to trigger excessive memory allocation. Although only the `Exporter.OpenTelemetryProtocol` package is named in the advisory, the OTel .NET SDK ships as a coordinated family release; bumping in lockstep avoids inter-package compatibility risk.
- **Exposure assessment.** WxReport.Svc uses `SecureSocketOptions.StartTls` against Gmail on port 587 — exactly the code path the MailKit advisory targets. The OTel exporter ships metrics over OTLP/gRPC to the local Prometheus-translating collector, which is trusted, so the OTel DoS surface was not realistically exploitable in our deployment; the advisory is still cleared because leaving NU1902 warnings normalizes the practice of ignoring build noise.
- **No code changes required.** `SmtpSender.cs` does not use `ServicePointManager.ServerCertificateValidationCallback` (the global hook MailKit 4.13.0 stopped honoring on .NET Core), does not depend on NTLM/GSSAPI mechanism selection (changed in 4.12.0), and does not embed CRLF in addresses (mitigated in 4.15.1). Standard PLAIN-over-STARTTLS to Gmail works unchanged.
- **Build clean.** `dotnet build WxServices.sln` reports zero warnings and zero errors. With WX-24's `log4net` bump already on master, all NU1902 advisories are now resolved.
- **Tests pass.** All four test projects (`MetarParser.Tests`, `TafParser.Tests`, `WxInterp.Tests`, `WxMonitor.Tests`) pass — 205 tests, zero failures.
- **OTel endpoint behavior preserved.** The `1.15.x` SDK quirk requiring full `/v1/metrics` URLs in the OTLP exporter endpoint config is unchanged in 1.15.3 (patch-only release); no `appsettings.shared.json` updates required.

## 1.5.1 — log4net 2.0.17 → 3.3.1 to clear NU1902 advisory (2026-04-30)

- **WX-24 (security):** Bumped `log4net` from `2.0.17` to `3.3.1` in `WxServices/src/WxServices.Logging/WxServices.Logging.csproj`. Closes the NU1902 build warning for `GHSA-4f7c-pmjv-c25w` (CVE-2026-40021), a vulnerability in `XmlLayout` / `XmlLayoutSchemaLog4J` around XML 1.0 character sanitization in MDC fields. WxServices uses neither `XmlLayout` nor any MDC / `ThreadContext.Properties` manipulation, so we were never actually exposed to the attack — but the warning fired on every build, and 2.x is past its support window regardless.
- **Single-file change.** Only the `<PackageReference>` version in `WxServices.Logging.csproj` moved. `log4net.shared.config` (PatternLayout + RollingFileAppender, the only layout we use) is unchanged. `Logger.cs` (`LogManager.GetLogger`, `ILog.Info/Warn/Error/Fatal/Debug`, `GlobalContext.Properties["LogFile"]`) is unchanged — log4net 3.x preserves all the API surface we touch.
- **Verified format preservation.** A containerized smoke-run of `WxMonitor.Svc` (built against log4net 3.3.1, run inside `mcr.microsoft.com/dotnet/runtime:8.0` with a writable bind-mount for the log directory) writes log lines in the pre-upgrade format: `<utcdate> <level> [<file>::<method>:<line>] <message>`. CallerInfo capture via `[CallerFilePath]` / `[CallerMemberName]` / `[CallerLineNumber]` produces the same `Program.cs::<Main>$:N` shape, and ERROR-level entries with attached exceptions still serialize the stack trace correctly.
- **Build clean.** `dotnet build` no longer emits NU1902 for log4net. The remaining NU1902 warnings (`MailKit 4.9.0`, `OpenTelemetry.Exporter.OpenTelemetryProtocol 1.15.2`) are pre-existing and tracked separately in WX-60.
- **Tests pass.** All four test projects (`MetarParser.Tests`, `TafParser.Tests`, `WxInterp.Tests`, `WxMonitor.Tests`) pass with no regressions.
- **No installer changes.** `HarderWare_WxServices.iss` and `Build-Release.ps1` reference `log4net.shared.config`, not the DLL — the DLL comes via NuGet/MSBuild — so no Inno Setup updates required. Build + deploy on Paul's Windows host produces a working release bundle.

## 1.5.0 — Recipient address: three input paths plus manual escape (2026-04-27)

- **WX-52 (feature):** The "Address" field on the WxManager → Recipients tab and the `Address` column on database recipient rows now accept three input forms, distinguished by leading characters:
  - `///word.word.word` — a What3Words address. Resolved against `https://api.what3words.com/v3/convert-to-coordinates` using `What3Words:ApiKey` from `appsettings.local.json`. The `///` form is faster to type than a full street address (example: W3W's reserved test address `///filled.count.soap`).
  - `lat, lon` — two decimal-degree numbers separated by a comma (e.g. `30.07, -95.55`). Parsed locally with no API call. `LocalityName` is left for the user to fill in.
  - Anything else — passed to the existing Nominatim geocoder (unchanged behaviour).
- **Dispatcher pattern.** `AddressGeocoder.LookupAsync(address, httpClient, w3wApiKey)` inspects the input, routes to the matching path, and returns `(lat, lon, locality)?`. The previous Nominatim-only body is now a private `LookupNominatimAsync`. `What3WordsClient` is a new sibling static class in `MetarParser.Data` that wraps the official `what3words.dotnet.wrapper` v4.1.0 NuGet package — `What3WordsV3.ConvertToCoordinates(words).RequestAsync()` returns an `APIResponse<Address>` whose `Error.Code`/`Error.Message` fields surface specific W3W diagnostics (`QuotaExceeded`, `BadOrigin`, etc.) directly into our log. The SDK manages its own internal `HttpClient`, so the `httpClient` parameter on `AddressGeocoder.LookupAsync` is now only consumed by the Nominatim path. A `///` input with a missing `What3Words:ApiKey` short-circuits with a logged error rather than touching the network. A `lat, lon` input with out-of-range values (lat outside ±90, lon outside ±180) returns null without parsing further.
- **Latitude/Longitude stay read-only — direct entry routed through the Address field.** The `lat, lon` input form on the Address field already covers direct-coordinate entry *and* triggers the nearby-stations pipeline; making LatBox/LonBox individually editable would have created a redundant escape that bypasses station discovery. Locality, METAR ICAO, and TAF ICAO remain directly editable so individual fields can be corrected without re-running geocoding. The Look-Up failure message points at those manual fields: *"Address could not be resolved. Check the format and try again, or fill in Locality and METAR/TAF ICAO directly."*
- **Locality preservation on `lat, lon` entry.** When `AddressGeocoder` returns an empty locality (the `lat, lon` direct-entry path), the LookUp handler no longer overwrites a locality the user already typed.
- **Config plumbing.** `What3Words:ApiKey` is read from `appsettings.local.json`. `WxManager.App` exposes it as `App.What3WordsApiKey` (parallel to `App.ClaudeApiKey`); `WxReport.Svc.RecipientResolver` takes it as a constructor parameter, sourced from `_config["What3Words:ApiKey"]` at the two construction sites in `ReportWorker.cs`.
- **Observability — every lookup leaves a trail.** `AddressGeocoder.LookupAsync` now logs one INFO line on entry naming the input and the chosen path (What3Words / lat-lon / Nominatim) and one INFO line on success (with resolved coordinates and locality) or one WARN line on failure. Path-specific clients (What3WordsClient, the Nominatim block) continue to log their own detailed error context — HTTP status, W3W `Error.Code`, etc. — on top of the dispatcher's entry/exit pair. Operators can correlate the dispatcher's pair with the path-specific detail by timestamp.
- **Tests.** New `AddressGeocoderRoutingTests` in `MetarParser.Tests` cover the dispatcher: `lat, lon` parsing across signed/whitespace/range cases (verified with an `HttpClient` whose handler throws if called, proving no HTTP traffic on the direct-parse path); `///` short-circuits cleanly when the API key is empty or null; plain text routes to Nominatim (verified by asserting the host of the captured request).
- **Docs.** `DESIGN.md` §4.7 updated to describe the three input paths. `INSTALL.md` §4 (Secrets) and §7 (Add Recipients) document the optional `What3Words:ApiKey` and the input forms. `DEVELOPER-README.md`'s `appsettings.local.json` sample picks up the new section.
- **Defensive UX, surfaced by deployment.** While testing the W3W path, a missing comma between sibling objects in `appsettings.local.json` caused WxManager to exit silently — `ConfigurationBuilder.Build()` threw a `JsonException`, and `App.OnStartup` had no try/catch around it. The only evidence was a "starting" log line followed by nothing. Fix: wrap the config build in a try/catch that logs the parser's own message (which already names file and line) and shows a `MessageBox` pointing at both candidate config files (`appsettings.shared.json` next to the executable, `appsettings.local.json` under `InstallRoot`) before calling `Shutdown(1)`. Pattern matches the existing missing-`ConnectionStrings:WeatherData` handler. Scope is WxManager only — the four headless services have a similar gap, but Windows surfaces "service failed to start" for them, so the silent-exit UX hazard is unique to the WPF app.

## 1.4.1 — Maps-tab focus-redirect tab-switch fix (2026-04-26)

- **WX-46 (bug fix):** On the Maps tab, releasing the mouse on a `Button` (Reset Zoom, Step ◀/▶, Play/Pause) was switching the selected tab to Meteograms. The control's own action still executed, but the user had to re-click the Maps tab header before the next interaction. The bug was sequence-dependent — it only triggered after the user had clicked the Meteograms tab header at least once during the session — which is why earlier reproduction attempts on cold-started instances failed.
- **Root cause:** `ButtonBase.OnLostMouseCapture` calls `Keyboard.Focus(...)` to keep keyboard focus on the button after the click. The focus call passes through the Window focus scope's class handler `FrameworkElement.OnPreviewGotKeyboardFocus`, which redirects focus to whatever is stored as the scope's `FocusedElement`. Once the user had clicked the Meteograms tab header, `TabItem.SetFocus` registered the Meteograms `TabItem` as that stored element. Subsequent button clicks therefore triggered `TabItem.OnPreviewGotKeyboardFocus` on the Meteograms `TabItem`, whose default behavior is to auto-select the tab on focus. The smoking-gun stack capture was logged to `wxviewer.log` on 2026-04-26 in two near-identical events, both bottoming out in `ButtonBase.OnMouseLeftButtonUp` → `OnLostMouseCapture` → `KeyboardDevice.Focus` → `FrameworkElement.OnPreviewGotKeyboardFocus` → `TabItem.OnPreviewGotKeyboardFocus(Meteograms)` → `IsSelected = true`.
- **Fix layer A — guard `SelectionChanged`.** A `PreviewMouseLeftButtonDown` handler on each `TabItem` sets `_userClickedTabHeader = true`. `RootTabControl_SelectionChanged` reverts any selection change that fires without that flag set, by synchronously assigning `prev.IsSelected = true` inside a `_reverting` re-entry guard. The handler exits to ensure the synthetic selection event raised by the revert short-circuits at the guard. This is the defensive backstop — independent of any future WPF focus-routing edge case.
- **Fix layer B — stop poisoning the Window focus scope.** A zero-size `Border` named `FocusSentinel` (Focusable, `KeyboardNavigation.IsTabStop="False"`) lives directly under the outer `DockPanel`, outside any `ToolBar` or `TabControl`. After every legitimate tab-header click, `Keyboard.Focus(FocusSentinel)` moves focus there so the Window scope's stored `FocusedElement` is never a `TabItem`. Future focus-scope redirects then land on the sentinel — a benign, non-`TabItem`, non-focus-scope element — and trigger no auto-select side effects.
- **Logging now self-classifies.** The existing `TabControl.SelectionChanged` log line (kept permanent in WX-48 for forensic value) is reformatted as `TabControl.SelectionChanged [cause]: from → to, focus was Type 'Name'`, with `cause` ∈ {`init`, `user`, `spurious-reverted`}. One log entry per event covers all three states; the previous companion stack-trace log is dropped now that the underlying mechanism is identified. Any future regression in either layer of the fix will surface as a `[spurious-reverted]` line in normal operation.
- **Acceptance criteria covered.** Maps-pane controls (Buttons, Sliders, ComboBoxes, ToggleButton) execute their action without changing the selected tab. Existing keyboard navigation (arrow keys, Ctrl+arrows, numpad zoom) continues to work because the `OnKeyDown` handler ignores focus when it is on a `Slider`, `ComboBox`, or `Selector` and the sentinel is none of those. ComboBox dropdowns continue to return keyboard focus to the Window via the `ComboBox_DropDownClosed` → `Keyboard.Focus(this)` path established in commit `aa82837`; with the sentinel in place, that path's redirect now lands on the sentinel rather than on a `TabItem`.

## 1.4.0 — Author-persona cached prefix on WxReport Claude calls (2026-04-25)

- **WX-50 (feature):** Every Claude API call from `WxReport.Svc` now opens with an author-persona prefix — the contents of `AboutPaul.md` at the repo root — sent as the first `system` content block with `cache_control: { type: "ephemeral" }` attached. The intent is voice continuity: generated reports should read as a continuation of Paul's thinking and judgment (forecaster authority, voice register, the matrix of when poetry is and is not appropriate, etc.) rather than a generic-Claude rendering of a weather forecast.
- **`AboutPaul.md` location.** Lives at the repo root rather than under `WxServices/src/WxReport.Svc/` so future HarderWare services that generate voice-bearing output can consume the same source of truth. The file is curated to be public-safe by design — its top section codifies an inclusion/exclusion rule (*"would Paul be comfortable seeing this quoted back in a generated report?"*) so it can sit in a public repo without harm. Deployed to each consuming service via a `<Content Include="..\..\..\AboutPaul.md"><Link>AboutPaul.md</Link><CopyToOutputDirectory>PreserveNewest</CopyToOutputDirectory></Content>` in the .csproj — same pattern WxReport.Svc already uses for `appsettings.shared.json`.
- **Loading.** `Program.cs` reads the file once at service startup via `File.ReadAllText(Path.Combine(AppContext.BaseDirectory, "AboutPaul.md"))`, wraps it in a small `PersonaPrefix` record, and registers that as a singleton in DI. A missing file is a fatal startup error: silent fallback to generic-Claude output is a worse failure mode than refusing to start.
- **API shape.** `ClaudeClient.GenerateReportAsync` previously sent `system` as a string. It now sends `system` as a two-element content-block array: the persona block first (with `cache_control: ephemeral`), the existing dynamic per-recipient prompt second. The cache breakpoint covers everything up to and including the block it is attached to, so this layout caches the persona alone — the per-recipient instructions vary by language, locality, and severity and must remain uncached.
- **Cache eligibility.** Anthropic's minimum cacheable size is 1024 tokens for Sonnet/Opus and 2048 tokens for Haiku. `AboutPaul.md` measures ~2400 tokens (observed in the Phase 2 test's `cache_creation_input_tokens`), which clears both thresholds — the persona block alone is large enough to engage caching on any current Claude model. The production model (`Claude:Model` in `appsettings.shared.json`) is `claude-sonnet-4-6`, and the cache breakpoint engages from the first call.
- **Refresh discipline.** `AboutPaul.md` lists the memory files it was synthesized from. When report output drifts from Paul's voice, or when one of the source memory files changes materially, a follow-up Jira ticket opens a refresh PR through the normal workflow — so CodeRabbit sees the diff and the audit trail is preserved.
- **No changes to the existing systemPrompt content.** The HTML-structure instructions, severity-conditional welcome/change-summary text, and aviation-terminology backstop list at `ClaudeClient.cs:182` are all unchanged. The persona block sits in front of them, layering authorial context onto the existing report-format contract.

## 1.3.8 — TabControl.SelectionChanged diagnostic promoted to permanent (2026-04-23)

- **WX-48 (instrumentation):** The diagnostic `SelectionChanged` handler developed on `WX-46-maps-tab-jump` (commit `59b2d9c`, originally marked *"branch-only, no PR"*) is now part of master. Feature branches forked from master will inherit the instrumentation automatically, and any WX-46 recurrence in any installed build will leave a forensic record in the log.
- **What's captured.** On each genuine `TabControl` selection change (the `OriginalSource` guard filters bubbled events from nested ComboBoxes and similar), the handler emits two log lines: a compact one-liner with the added/removed `TabItem` and its `Header`, the current `Keyboard.FocusedElement` type and `x:Name`, and both `e.OriginalSource` and `e.Source` types; and a full managed `StackTrace(fNeedFileInfo: true)` so the caller into `Selector.OnSelectionChanged` is captured. Frames of interest include `TabItem.OnGotKeyboardFocusWithin` (focus-traversal theory), `TabItem.OnRequestBringIntoView` (a Meteograms descendant asked to scroll into view), any of our own code (programmatic `SelectedIndex` set), or pure WPF internals with no obvious trigger (dispatcher-queued operation).
- **Why ship to master now.** WX-46 is an intermittent bug. Keeping the diagnostic confined to the `WX-46-maps-tab-jump` branch meant any WxViewer build derived from master would not log recurrences — exactly the scenario where recurrence data is most likely to appear (sustained daily use of the installed product). Shipping the diagnostic to master closes that gap.
- **No user-visible change.** Output is log-only; no UI, data, or behaviour shift.
- **WX-46 ticket.** Remains open, awaiting the next recurrence — the installed build now has the diagnostic wired in, so the next trigger will produce a stack trace for root-cause analysis.

## 1.3.7 — Analysis-map MSLP: proper temperature-based reduction (2026-04-18)

- **WX-35 (bug fix):** `WxVis/synoptic_map.py::_altimeter_to_slp_hpa` was adding `h_meters / 8.5` hPa on top of the METAR altimeter setting. That barometric approximation is the formula for reducing **station pressure** to MSL — but the altimeter setting (QNH) is *already* reduced to MSL using the ISA temperature profile. The extra reduction inflated the plotted "SLP" in direct proportion to elevation: at KDEN (5280 ft) the function returned ≈1205 hPa for a 30.00 inHg altimeter; at KLXV (9927 ft) it returned ≈1372 hPa. Barnes interpolation then built a fake 300 hPa dome over the Rockies and 4 hPa isobar contours proliferated across the middle of the map, producing the visibly denser isobar field than the companion GFS f000 map for the same valid time.
- **Fix:** new `_altimeter_to_mslp_hpa(altimeter_value, altimeter_unit, elevation_ft, temp_c)` performs the physically correct two-step reduction — QNH → station pressure via the ISA polytropic formula `P_stn = QNH × (1 - 0.0065·h/288.15)^5.2561`, then station pressure → MSLP via the hypsometric equation `MSLP = P_stn × exp(g·h / (R_d · T_mean))` with `T_mean = T_surface_K + 0.00325·h` (6.5 K/km standard lapse for the fictitious surface-to-MSL layer). The `AirTemperatureCelsius` column is already materialised on the plot DataFrame at `prepare_plot_data`, so the call site just threads it through.
- **Degenerate cases.** Station at or below MSL (including missing/non-finite `ElevationFt`), or missing/non-finite `AirTemperatureCelsius`, fall back to returning QNH directly — the error vs. true MSLP is <1 hPa at low elevations and the analysis remains self-consistent across stations.
- **Verified end-to-end.** KCXO (245 ft, 29.88 inHg, 20°C): 1011.7 hPa (was 1020.6). KDEN (5280 ft, 30.00 inHg, 10°C): 1012.2 hPa (was 1205.3). KLXV (9927 ft, 30.00 inHg, 0°C): 1009.5 hPa (was 1371.9). The Rockies no longer anchor an artificial high; isobar density on the 06Z analysis map now matches the companion GFS f000 map.
- **Non-goals.** The 3-digit station-model encoding `(slp_hpa * 10) % 1000` at `_encode_slp` is untouched (it is standard WMO format and was always correct — it just inherited wrong inputs). Forecast-side MSLP handling in `forecast_map.py` reads MSLP in Pa from GRIB and was never affected.

## 1.3.6 — GFS pipeline switched from WSL to native Windows wgrib2 (2026-04-18)

- **WX-33 (bug fix):** 1.3.5 introduced a regression when WX-31 Phase 1 moved `WxParser.Svc` off `.\PaulH` onto the `NT SERVICE\WxParserSvc` virtual account. `GribExtractor` invoked `wgrib2` via `wsl.exe` — but WSL distros are per-Windows-user, and virtual service accounts have no WSL of their own. Every `wgrib2 -small_grib` call exited `-1`, every forecast hour produced zero subgrid data, and the 2026-04-17 18Z GFS run was lost entirely (`GfsFetcher: run 2026-04-17 18Z is 0/121 hours complete`). `ForecastMapWorker` correctly idled for lack of new data, so forecast-map output stopped after the 12Z run rendered at 19:10 UTC.
- **Fix:** switched to the NOAA native Windows build of `wgrib2` (Cygwin-compiled, ships `wgrib2.exe` + `cygwin1.dll`). `GribExtractor.ExtractAsync` now spawns `wgrib2.exe` directly with Windows paths — no `wsl.exe` wrapper, no `/mnt/c/...` path translation. Works under any identity because the Windows exe has no per-user prerequisites.
- **Config key renamed:** `Gfs:Wgrib2WslPath` → `Gfs:Wgrib2Path`. Clean rename with no back-compat shim — the old key in an existing `appsettings.local.json` will be ignored and the default from `WxPaths.Wgrib2DefaultPath` (`{InstallRoot}\wgrib2\wgrib2.exe`) will apply. Operators updating the new key in WxManager's Configure tab will write the new key name going forward.
- **`WxPaths.Wgrib2BundledWslPath` → `Wgrib2DefaultPath`.** Default resolves to `{InstallRoot}\wgrib2\wgrib2.exe` (previously a WSL path under `{InstallRoot}/tools/`). The `ToolsDir` property and the internal `ToWslPath` helper were retired along with the WSL code path.
- **WSL retired as a prerequisite.** It was only there to host `wgrib2`. `PrerequisiteChecker.Requires.Wsl` and `CheckWslAsync` are gone; `WxParser.Svc/Program.cs` no longer probes WSL at startup; WxManager's Setup tab drops the WSL checklist row. The `Requires` enum keeps its bit-pattern values so any persisted config referencing old flags cannot collide with new meanings.
- **WxManager Configure tab:** label "wgrib2 WSL Path" → "wgrib2.exe Path". Field renamed `TxtWgrib2WslPath` → `TxtWgrib2Path`. Default suggested value now the Windows path from `WxPaths.Wgrib2DefaultPath`.
- **Ops work (one-time on the HarderWare PC):** download the NOAA Windows wgrib2 build, extract to `C:\HarderWare\wgrib2\`, grant `NT SERVICE\WxParserSvc` RX on that folder, rename the `Wgrib2WslPath` key in `appsettings.local.json` to `Wgrib2Path` (or remove it to let the default apply), redeploy WxParser.Svc, observe the next GFS cycle ingests 121/121 hours.

## 1.3.5 — Retry DB connection at service startup (2026-04-17)

- **WX-28 (partial):** All four services boot with Windows, and after a Windows-Update-driven reboot they race SQL Server's own service start. Before this release, the first `SqlException error 26` ("A network-related or instance-specific error occurred while establishing a connection to SQL Server … error: 26 – Error Locating Server/Instance Specified") was terminal — `DatabaseSetup.EnsureSchemaAsync` threw, each service's outer `try/catch` logged `ERROR Fatal error during startup`, and the services exited without restarting. The 2026-04-17 overnight log showed WxMonitor.Svc died at 04:00:15 and again at 04:32:06 from exactly this pattern; WxParser, WxReport, and WxVis had the same stack traces. When the operator logged in at 12:34, all four came back up successfully on the next manual start, confirming the post-reboot race.
- **Fix:** `DatabaseSetup.EnsureSchemaAsync` now retries transient SQL Server connection errors according to a configurable schedule. Defaults are 12 attempts with delays 5 s, 10 s, 20 s, 30 s, 30 s, 30 s, 30 s, 30 s, 30 s, 30 s, 30 s — roughly 5 minutes of patience before giving up. Each transient failure logs at `WARN` (`Database not ready (attempt N/12): … — retrying in Xs.`); only after every attempt has failed does the service throw `DatabaseUnavailableException` and let the existing outer catch log `ERROR` so Windows SCM recovery actions can restart it.
- **Transient vs. permanent classification.** Only `SqlException` numbers known to indicate a connection-layer condition are retried: –2 (timeout), 20, 26, 40, 53, 64, 121, 233, 258, 1205, 1222, 10053, 10054, 10060, 10061, 11001. Login failures (18456), permission errors, schema conflicts, and malformed configuration all propagate immediately so real bugs still fail fast instead of spinning for five minutes.
- **Scope — all four services.** `WxParser.Svc`, `WxReport.Svc`, `WxMonitor.Svc`, and (now) `WxVis.Svc` all route DB-schema setup through the retry path. WxVis.Svc previously skipped `EnsureSchemaAsync` entirely and allowed its workers to crash with unhandled exceptions if SQL was not yet reachable; it now performs the same startup check as the other three.
- **Configurable without rebuild.** New `Database:StartupRetry` section in `appsettings.shared.json`:
    ```json
    "Database": {
      "StartupRetry": {
        "MaxAttempts": 12,
        "DelaySecondsSchedule": [ 5, 10, 20, 30, 30, 30, 30, 30, 30, 30, 30 ]
      }
    }
    ```
    Any missing element falls back to the in-code default. The delay schedule wraps on its last element, so `MaxAttempts` can be increased without also lengthening the array.
- **First-run database creation is preserved.** `db.Database.EnsureCreatedAsync` (which connects to `master` to create the `WeatherData` database on first run) is inside the retry loop, so new-developer installs where SQL Server is up but the database has not yet been created still work end-to-end — the retry simply waits for SQL Server to answer, then `EnsureCreatedAsync` creates the database and the schema in the normal way.
- **Deferred to follow-up WX-28 PRs:** declaring `DependOnService=MSSQL$SQLEXPRESS` on each service (belt-and-suspenders with this retry), the full Windows service-configuration audit for `INSTALL.md`, and moving `WxParser.Svc` off the personal login it currently runs under.

## 1.3.4 — Retry with exponential backoff on transient upstream fetch failures (2026-04-16)

- **WX-20 / WX-21:** The METAR, TAF, and GFS fetchers previously treated every non-2xx HTTP response and every network-level exception as a terminal failure — `catch (Exception) → Logger.Error → return` or `break`. This overnight produced 12 `ERROR` log entries from `MetarFetcher` (upstream HTTP 502 Bad Gateway / 504 Gateway Time-out from aviationweather.gov) and one from `GfsFetcher` (SSL handshake failure for a specific GFS forecast hour). WxMonitor's alert pipeline treats every `ERROR` as actionable, so transient upstream hiccups were paging the operator for conditions that would have self-resolved within seconds.
- **Fix:** new `HttpFetchRetry.GetStringWithRetryAsync` extension method in `MetarParser.Data` wraps `HttpClient.GetStringAsync` with a 3-attempt exponential-backoff retry (2 s → 4 s → 8 s). Transient failures — HTTP 5xx, 429, SSL/TLS handshake errors, network-level `IOException`, request-timeout `TaskCanceledException` — are retried and each retry logs at `WARN`. Only when all three attempts have failed does the caller log at `ERROR`. Permanent failures (4xx other than 429) throw immediately without retry, so `GfsFetcher`'s existing 404/301/302 treatment ("forecast hour not yet published, stop the loop") is preserved exactly.
- **Applied to:** `MetarFetcher.FetchUrlAndInsertAsync`, `TafFetcher.FetchAndInsertAsync` (companion fix — same aviationweather.gov upstream, same failure pattern), and `GfsFetcher`'s per-forecast-hour `.idx` download. Call sites are one-liners: `await httpClient.GetStringWithRetryAsync(url, "METAR")` / `"TAF"` / `$"GFS f{fhStr} index"`.
- **Context:** these errors are *partially* related to WX-19 (the wrong-station fallback from earlier today) in that they contributed noise to the incident-triage process, but they were never the root cause of the wrong-station bug. Handling them cleanly reduces the signal-to-noise ratio of the WxMonitor alert stream enough that the planned WX-25 rate-based alerting becomes much easier to calibrate.

## 1.3.3 — Defensive idempotent ALTER for Metars/Tafs ReceivedUtc (2026-04-16)

- **WX-22:** The `ReceivedUtc` column on `Metars` and `Tafs` has been a NOT NULL EF property populated by the mappers since commit `a70d81f` (2026-03-30), but no corresponding idempotent `ALTER TABLE ... IF NOT EXISTS` block was ever added to `DatabaseSetup.EnsureSchemaAsync`. Fresh installs pick up the column via EF Core's initial `OnModelCreating`, but a fresh clone on a PC holding an older backup, or any future container/cloud deployment that restores from pre-2026-03-30 data, would see the mapper try to write a column that doesn't exist.
- **Fix:** Added two idempotent `ALTER TABLE ... ADD [ReceivedUtc] datetime2 NOT NULL CONSTRAINT ... DEFAULT SYSUTCDATETIME()` guards in `DatabaseSetup.EnsureSchemaAsync`, matching the pattern used for the WX-13 country/region columns, `Municipality`, `AlwaysFetchDirect`, and `LastMetarIcao`. The DEFAULT only fires to backfill pre-existing rows at ALTER time; EF always supplies an explicit value on insert, so it's a no-op for new rows.
- **Context:** Today's WX-19 investigation relied heavily on `ReceivedUtc` to separate station-outage causes (KAUS silent for 6 h) from fetcher-outage causes (would show as large ObservationUtc→ReceivedUtc lag). The column's diagnostic value is real; this PR just closes the schema-migration gap so that value survives a restore or a fresh deploy.

## 1.3.2 — Geographic nearest-neighbour METAR fallback (2026-04-16)

- **Bug fix (WX-19):** when the recipient's preferred METAR station(s) had no data within the last 3 hours, `WxInterpreter.GetSnapshotAsync` previously fell back to "the single most recently inserted METAR anywhere in the database." Every recipient in the same cycle received the same fallback regardless of geography, and the chosen station drifted as unrelated stations reported — producing observations from Earlton, Ontario (CYXR) for an Austin recipient and Mineola, TX (KJDD) for a Spring, TX recipient this morning.
- **New fallback behaviour:** the interpreter now picks the geographically *nearest* station with a recent METAR, capped at a 50 km (≈30 mi) radius, on a per-recipient basis. Anchored on the recipient's own lat/lon. A lat/lon bounding-box derived from the radius prefilters candidates so the per-row haversine runs only on the handful of stations actually in range.
- **No qualifying station → forecast-only report with an honest note.** If no station within the radius has recent data, the snapshot is still built but carries `ObservationAvailable = false` and an `ObservationUnavailableNote`. The Current Conditions section is rendered as a short italic paragraph explaining that no recent observation is available from a station within about 30 miles, and the report continues with the TAF and GFS forecast sections. The report footer swaps the station/timestamp line for "No current observation". The recipient is only skipped entirely when none of METAR, TAF, and GFS produced data.
- **Change-detection safety.** Observation-less snapshots cannot reliably feed into significant-change detection (the observation fingerprint fields would default to "calm / good visibility / no phenomena" and could fire a false "conditions cleared" alert). `ShouldSend` now suppresses the change-triggered branch when `ObservationAvailable` is false; `RecipientState.LastSnapshotFingerprint` and `LastMetarIcao` are left untouched on forecast-only sends so change-detection resumes cleanly against the last genuine observation when data returns.
- **Fallback warning now includes distance.** The existing "preferred station(s) had no data — fell back to KXYZ" warn now reports the fallback station's distance from the recipient in statute miles, e.g. *"fell back to KHYI (18 mi away)"*. A distinct warn is emitted for the observation-less path so forecast-only sends show up clearly in the log.

## 1.3.1 — Forecast temperatures on two labeled lines (2026-04-15)

- **Extended Forecast table column reformatted** (WX-14). Previously the daily forecast cell read e.g. `85°/72°F`, with the unit suffix ambiguously applying to only the second value. The cell now contains two labeled lines separated by `<br/>` — `High: 85°F` above `Low: 72°F` — with explicit unit suffixes on both. The column header changes from `High/Low` to `Temperatures` to match the new cell format.
- Implemented as a prompt-only change in `WxReport.Svc/ClaudeClient.cs`; no model, schema, or data-pipeline changes.

## 1.3.0 — WxStations country/region columns (2026-04-15)

- **Six new nullable columns on `WxStations`** (WX-13): `Region`, `RegionCode`, `RegionAbbr`, `Country`, `CountryCode`, `CountryAbbr`. Populated from OurAirports' `airports.csv`, `countries.csv`, and `regions.csv`. `CountryAbbr` uses a small override table (currently `GB`→`UK`, `US`→`USA`); all other countries default to their ISO 3166-1 alpha-2 code.
- **Schema migration is idempotent.** `DatabaseSetup.EnsureSchemaAsync` appends six `ALTER TABLE ... IF NOT EXISTS` blocks; fresh installs pick up the columns from `OnModelCreating`.
- **WxManager Nearby Stations grid** now displays `"{Municipality}, {RegionAbbr}, {CountryAbbr}"` (e.g. `Brenham, TX, USA`), falling back to the airport name when location parts are unavailable.
- **Importer enhancements.** `AirportDataImporter` now downloads `countries.csv` and `regions.csv` alongside `airports.csv`, builds in-memory lookups, splits `iso_region` at the hyphen for the short region abbreviation, and upserts all six fields on every refresh cycle — providing automatic backfill of existing rows on the next run.
- **Expected coverage.** Of ~39 300 stations imported, only the ~45 stub rows inserted by `MetarFetcher` for ICAOs not present in OurAirports remain with NULL location fields; the station-grid display handles those gracefully.

## 1.2.1 — WxViewer zoom-swap fix; numpad zoom controls (2026-04-14)

- **Zoom-level transitions no longer jump the view.** The Z1↔Z2 and Z2↔Z3 swaps previously multiplied the WPF scale and translate by 0.5/2.0 to compensate for what was assumed to be a 2x-sized bitmap. Because the `Image` controls use `Stretch="Uniform"`, a bitmap of any Z-level fills the same viewport at `ScaleX=1`, so the compensation was *introducing* the jump rather than undoing one. The swap now leaves scale and translate untouched — the point under the cursor stays put and magnification is monotonic across transitions.
- **Level-dependent swap thresholds.** Each Z-level has 2x the pixel density of the one below it, so the "stretched enough to look blurry" scale doubles per level: Z1→Z2 at `ScaleX ≥ 3.0`, Z2→Z3 at `≥ 6.0`; swap-down coincides with the previous level's up threshold.
- **Direction-aware swap checks.** A zoom-in step can only trigger a swap-up, a zoom-out step only a swap-down. Up and down thresholds can therefore coincide without ping-pong, and the same keypress that triggered a swap-up also reverses it.
- **Crossfade tuning.** Duration raised from 200 ms to 1000 ms with starting opacity dropped to 0.1, so the transition reads as a fade rather than a flash.
- **Numpad zoom controls.** NumPad 8 zooms in one step (×1.15, matching one mouse-wheel click), NumPad 2 zooms out one step, anchored on the cursor when it's over a viewport and mirrored to the linked pane when Link Panes is enabled. Implemented via a `WM_KEYDOWN` WndProc hook that inspects the scan-code extended-key bit in `lParam`, so the numpad keys work regardless of NumLock state and do not hijack the dedicated arrow-block Up/Down keys.

## 1.2.0 — Metrics instrumentation across all services (2026-04-13)

- **OpenTelemetry metrics added to all four services**, exporting via OTLP to the existing Prometheus/Grafana observability stack. All services share the `Telemetry:Enabled` and `Telemetry:OtlpEndpoint` settings in `appsettings.shared.json`.
- **WxParser.Svc (GFS):** Added `wxparser.gfs.cycles.total`, `wxparser.gfs.failures.total`, and `wxparser.gfs.cycle.duration.seconds` (histogram, buckets 30s–30min) to the existing METAR/TAF metrics.
- **WxReport.Svc:** New meter with `wxreport.cycles.total`, `wxreport.sends.total`, `wxreport.send.failures.total`, `wxreport.claude.calls.total`, `wxreport.cycle.duration.seconds`, and `wxreport.claude.duration.seconds`.
- **WxVis.Svc:** New meter with `wxvis.analysis.renders.total`, `wxvis.analysis.failures.total`, `wxvis.forecast.renders.total`, `wxvis.forecast.failures.total`, and `wxvis.render.duration.seconds`.
- **WxMonitor.Svc:** New meter with `wxmonitor.cycles.total` and `wxmonitor.alerts.total`.
- **Report footer branding:** Email footer now reads "HarderWare WxServices 1.2.0" instead of "WxServices 1.2.0".

## 1.1.1 — Bug fixes, logging audit, meteogram cosmetic fix (2026-04-13)

- **Bug fix:** ComboBoxes (Analysis selector, GFS Run selector, speed selectors, meteogram run/recipient selectors) retained keyboard focus after selection, trapping arrow keys. All six ComboBoxes now return focus to the main window via `DropDownClosed` + `Keyboard.Focus(this)`.
- **WxViewer logging and error-handling audit:**
  - Global exception handlers: `DispatcherUnhandledException`, `AppDomain.UnhandledException`, and `TaskScheduler.UnobservedTaskException` registered at startup so unhandled exceptions are logged before the process terminates.
  - All previously-silent catch blocks now log via `Logger.Warn`: settings parsing, database queries, image loading, manifest parsing, and `FileSystemWatcher` errors.
  - `MainWindow.OnClosed` unsubscribes ViewModel event handlers and stops the quality-restoration timer.
  - `MainViewModel.Dispose` unsubscribes `MapFileScanner.DirectoryChanged` (converted from lambda to named method).
  - Highlight timer reused as a single instance with a named tick handler instead of being recreated on each highlight call.
- **Meteogram cosmetic fix:** Right-axis tick labels now rendered in green to match the RH line and axis label.

## 1.1.0 — Multi-zoom map rendering with interactive zoom/pan (2026-04-12)

- **Multi-zoom rendering pipeline:** `synoptic_map.py` and `forecast_map.py` accept `--zoom-level N`. Each level doubles figure size and halves station density. Font sizes and contour line widths scale proportionally. DPI is 150 for z1, 100 for z2+ to manage file sizes.
- C# workers (`AnalysisMapWorker`, `ForecastMapWorker`) loop over configurable zoom levels (default 3) per render cycle. New `WxVisConfig.ZoomLevels` property. Filenames include `_z{N}` suffix. `MapFileScanner` updated with new regex patterns and `ZoomPaths` dictionary.
- **WxViewer zoom/pan UX:** Mouse-wheel zoom toward cursor, click-drag pan, double-click to reset. Zoom-in at 3.0× crossfades to the next higher-resolution image; zoom-out at 1.4× swaps back. Link Panes toggle synchronizes zoom/pan between both panes. Reset Zoom button and zoom-level indicator (Z1/Z2/Z3) in toolbars.
- CONUS map extent expanded from (-126, -65, 22, 50) to (-136, -60, 17, 55) to prevent Lambert Conformal clipping at edges
- Low-quality bitmap scaling during active interaction for responsiveness, restored to high-quality after 200 ms idle
- Maximized window constrained to working area to avoid taskbar overlap

## 1.0.2 — Meteogram temp-unit bug fix; log format cleanup (2026-04-12)

- **Bug fix:** Recipients configured for Celsius were receiving Fahrenheit meteograms because (1) PNG filenames did not include the temperature unit, causing C and F renders to overwrite each other, and (2) `FindMeteogramAbbrevPath` matched only on ICAO + timezone, ignoring `TempUnit`. Fixed by adding a `{F|C}` tag to filenames and a `tempUnit` parameter to the finder.
- Standardized all ReportWorker recipient log lines to the format `{Id} {Email} ({Name})`
- GFS forecast summary log now respects the recipient's temperature unit preference

## 1.0.1 — Version display and startup logging (2026-04-11)

- Version number shown in WxManager and WxViewer custom title bars (styled smaller and dimmer)
- Version and short git commit hash logged at startup for all six programs (e.g. `WxReport.Svc 1.0.1 (commit ef49de4) starting.`)
- Suppressed the SDK `+hash` suffix from `InformationalVersion` so title bars and email footers display a clean version string
- Added Versioning section to DESIGN.md

## 1.0.0 — Initial versioned release (2026-04-07)

- Introduced system-wide version number via `Directory.Build.props`, replacing per-project versioning
- All six programs (four services, WxManager, WxViewer) share a single version source of truth
- Git commit hash embedded as `AssemblyMetadata("GitCommit", ...)` for runtime diagnostics
