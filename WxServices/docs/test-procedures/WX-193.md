# WX-193 — Functional test procedure

**Ticket:** WX-193 — Diagnostic reports: suppress the "What's changed" band except for a
near-term (local Day 1-3) severe onset (fixes a WX-189 regression).
**Change under test:** a diagnostic (deploy-verification) report's "What's changed" band is
now suppressed unless a **new severe hazard** appears in the near term — exactly the WX-178
rule already applied to scheduled reports. WX-189 made the change set deterministic, so the
band renders from the computed `changes[]` via the fallback even when Claude wrote no
`changeSummary`; but the WX-178 strip was `Scheduled`-only, and the diagnostic path
(`ReportWorker.SendStartupReportAsync`) never stripped at all — so a diagnostic surfaced a
band for ordinary non-severe changes ("rain developing", a rain→thunderstorm upgrade with no
near-term severe). The fix:

1. **Guard extended** — `ReportWorker.SuppressesScheduledChangeBand` now accepts
   `ReportKind.Diagnostic` alongside `Scheduled`. Its first-send, near-term severe-onset, and
   bandless short-circuits were already kind-agnostic, so the diagnostic case rides the same
   deterministic decision.
2. **Strip applied on the diagnostic path** — `SendStartupReportAsync` now computes the
   suppression and applies `WithoutChangeBand` before rendering (and before persisting
   `StructuredReport`), mirroring the scheduled path in `ProcessLocalityAsync`.

A diagnostic now carries the band **only** for a newly-appearing near-term severe onset,
identical to a scheduled report; severe still shows in the grid, the subject, and the closing.

**Released in:** 1.32.1 · **Component:** WxReport.Svc

## Deterministic proof (the primary verification)

The decision is a pure function, proven by unit tests in
`tests/WxReport.Tests/ScheduledChangeBandTests.cs` (run with `dotnet test WxServices/WxServices.sln`):

- `Diagnostic_NonSevereChange_SuppressesBand` — the WX-189 regression case: an ordinary
  non-severe change on a diagnostic is stripped.
- `Diagnostic_NearTermSevereOnset_KeepsBand` — a Day 1-3 not-severe→severe onset keeps the band.
- `Diagnostic_ReportHasNoBand_NothingToSuppress` — nothing to strip when no band is present.
- The `Scheduled_*` cases (unchanged) confirm the shared logic the diagnostic case now reuses;
  `HasSevereEscalationOver_*` covers the near-term cutoff, incl. the exact `< cutoff` boundary.

Because the logic is deterministic and snapshot-driven (no per-recipient or locale variance in
the decision), these tests are the authoritative correctness check. The live step below
confirms the **deployed binary and the wiring** behave — a smoke check, not the proof.

## Post-deploy verification (§13)

Script: `docs/test-procedures/WX-193-verify.sh` (version-pinned to 1.32.1 via `deploy-info.sh`).
A deploy restarts the service, which sends exactly one startup (diagnostic) report — the path
the fix lives on — so the verify can run as soon as that diagnostic is persisted.

The fix changes rendered OUTPUT, which leaves no service-log fingerprint, so the evidence lives
in `CommittedSends.EmailBody` (the band heading renders as "What's changed:"). The script reads
diagnostic sends (`IsDiagnostic = 1`) since the deploy boundary and gates on:

- **PRECONDITION / EXERCISED:** at least one diagnostic send in the window (a deploy emits one).
- **REGRESSION (fail) signature:** a diagnostic whose body carries the band **and contains no
  severe indicator at all** — a band on an ordinary non-severe diagnostic, the exact WX-189
  regression. Expect **0**.
- **MANUAL-JUSTIFY (reported, not gated):** a diagnostic band on a body that **does** contain
  "Severe". This *might* be a legitimate near-term onset or a stale far-day / pre-existing-severe
  band (onset = `severeFlag` false→true needs the prior snapshot, which the body alone can't
  establish). For any such row, dump the body and confirm a **Severe day in the first ~3 grid
  rows** (local Day 1-3), as WX-178 §"Live spot-check" does:

  ```bash
  powershell.exe -NoProfile -Command "sqlcmd -S .\SQLEXPRESS -d WeatherData -E -C -y 0 \
    -Q \"SET NOCOUNT ON; SELECT EmailBody FROM CommittedSends WHERE Id=<Id>\""
  ```

```bash
bash docs/test-procedures/WX-193-verify.sh
```

*Expect:* once a startup diagnostic has shipped post-deploy with no non-severe band → **PASS**.
A band on a non-severe diagnostic → **FAIL** at any horizon. No diagnostic in the window yet →
**WAIT**.

## Result fields

- **Test Proc:** `docs/test-procedures/WX-193.md`
- **Test Result:** PASS `yyyy-mm-dd @ 1.32.1` once the unit tests are green on the deployed
  revision and the post-deploy diagnostic carried no non-severe band (any banded-with-severe row
  justified per the manual step). Legitimately recorded after the first post-deploy startup
  diagnostic.
