# WX-246 — Anchor the free narrative on CultureInfo day names via prompt injection

**Story · verifies the injected `day_name_reference` reaches the reconciler prompt and the free narrative binds each forecast date to the correct localized day name.**

## The bug

The free-composed narrative (`changeSummary` / `closing`) had no authoritative day-name reference, so Claude could name the **wrong day** for a date. The WX-214 Albanian (`sq`) QA caught it: English "Tuesday" rendered as `e mërkurën` (Wednesday) — the wrong day **root** (`mërkur-`/Wed vs `mart-`/Tue). The deterministic grid/obs were already correct (they render day names from `CultureInfo`); only the narrative drifted.

## The fix

At reconcile time, `DayNameReference.Build` computes each narrative language's `CultureInfo` day name for every **local** date the forecast spans and injects a `day_name_reference` block into the reconciler's **uncached** user message (`ForecastReconciler.BuildUserMessage`), instructing the LLM to use those exact day words, **inflected for grammatical agreement** (WX-246 comment 12875). Day names are deterministic facts from `CultureInfo` — computed per report, **never persisted as tokens** (the pivot away from the original controlled-vocabulary approach). Grid and prose therefore draw from the same source and agree by construction.

## Preconditions / sequence

1. **PR merged and CodeRabbit review complete.**
2. **No migration / no DB change** — this is a pure prompt change. Deploy the rebuilt `WxReport.Svc`.
3. Rebuild the `WxReport.Tools.TranslationQa` tool from the merged code — it renders scenarios through the **same** `ForecastReconciler` path, so the injected `day_name_reference` is exercised.

## Procedure

1. Re-run the translation-QA generation + judge for **`sq`** (the language that surfaced the bug), plus **`es`** for regression (Windows PowerShell, from `C:\Code\HarderWare\WxServices`):

   ```powershell
   dotnet run --project src\WxReport.Tools.TranslationQa -- --lang sq --judge gemini
   dotnet run --project src\WxReport.Tools.TranslationQa -- --lang es --judge gemini
   ```

   Each writes a fresh run under `C:\HarderWare\translation-qa\<lang>.<stamp>\`.

2. **Read the newest `sq` rendered target report(s).** For every weekday the narrative names, confirm the day name matches that date's `CultureInfo` `sq` day name (`e diel, e hënë, e martë, e mërkurë, e enjte, e premte, e shtunë` for Sun–Sat, inflected as the sentence requires). Cross-check the date→day binding against the deterministic **grid** in the same report — they must agree.

## Pass criteria

- The `sq` narrative names the **correct day for each date** it references — no wrong-day-root emission (the `e mërkurën`-for-Tuesday class is gone).
- The narrative's day names **agree with the deterministic grid** for the same dates (no grid↔prose contradiction).
- **`es` regression:** no new wrong-day emissions.

This is prompt-anchoring — **probabilistic by design** (as with WX-238). A clean re-run with correct date→day binding is **PASS**. If a wrong day recurs, strengthen the `day_name_reference` wording/instruction and re-run. No `verify.sh`: the check is a semantic date↔day-name read, not a fixed-string grep.
