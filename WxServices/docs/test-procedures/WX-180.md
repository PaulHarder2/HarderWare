# WX-180 — post-deploy verification (windKt clamp + degrade cost fix)

WX-180 fixed the ~$45/day reconciler retry-storm: a folded gust in `windKt.max` is
now **clamped** to the sustained ceiling instead of rejected → retried → degraded, so
a gusty forecast converges and sends rather than degrading every cycle.

> Authored after merge (the cost hotfix shipped before its §13 artifact existed) and
> carried on the WX-181 branch as documented out-of-scope work.

## Automated check

```
docs/test-procedures/WX-180-verify.sh
```

Version-pinned to **1.27.1 / WxReportSvc** via `deploy-info.sh`; reads
`wxreport-svc.log`; shares the `verify-lib.sh` scaffold. PASS requires a full active
day of cycles **and** the clamp path exercised, with no failure-signature line.

- **Fingerprint (expect):** `clamping windKt.max … (WX-180)` present; **0** old-reject
  lines (`windKt.max is … SUSTAINED`); **0** degradations citing `windKt`.
- **Failure signature → FAIL:** any old-reject line, or any windKt-cited degradation
  (the clamp isn't taking).
- **Context (not gating):** total degradations (the residual is WX-165 time-word, a
  separate ticket); Watonga scheduled generations (was ~32 / 6 h pre-fix — the
  retry-storm loop should be gone).

## Manual confirmation

The acceptance criterion *"daily cost returns to baseline"* is confirmed on the
**Grafana WxReport cost panel** once the averaging window clears (a few hours
post-deploy); the log fingerprint above is the deterministic proxy.

## Result

Record in WX-180's **Test Result** field: `PASS yyyy-mm-dd @ 1.27.1` once the script
reports PASS and the cost panel is back at baseline.
