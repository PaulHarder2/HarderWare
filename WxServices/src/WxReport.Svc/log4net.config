<?xml version="1.0" encoding="utf-8"?>
<log4net>
  <appender name="RollingFile" type="log4net.Appender.RollingFileAppender">
    <!-- Log file location. Change this path to move logs elsewhere. -->
    <file value="C:\HarderWare\Logs\wxreport-svc.log" />
    <appendToFile value="true" />
    <lockingModel type="log4net.Appender.FileAppender+MinimalLock" />
    <rollingStyle value="Size" />
    <!-- Maximum size of a single log file before rolling over. -->
    <maximumFileSize value="10MB" />
    <!-- Maximum number of backup (rolled) files to keep. Oldest are deleted. -->
    <maxSizeRollBackups value="10" />
    <staticLogFileName value="true" />
    <layout type="log4net.Layout.PatternLayout">
      <conversionPattern value="%utcdate{yyyy-MM-dd HH:mm:ss.fff} %-5level %message%newline" />
    </layout>
  </appender>
  <root>
    <!-- Minimum log level: DEBUG, INFO, WARN, ERROR, FATAL -->
    <level value="INFO" />
    <appender-ref ref="RollingFile" />
  </root>
</log4net>
